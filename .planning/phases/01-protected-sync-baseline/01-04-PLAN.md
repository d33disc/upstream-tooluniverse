---
phase: 01-protected-sync-baseline
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - .github/workflows/tests.yml
  - scripts/capture_sync_baseline.py
  - tests/unit/test_sync_baseline_ci.py
autonomous: true
requirements: [BASE-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A commit containing the Phase 1 implementation defines Python 3.10–3.14 compatibility jobs, with Python 3.12 as the comprehensive D-04 lane."
    - "The baseline orchestrator can locate and validate a GitHub Actions run only when its `headSha` equals the requested local commit."
    - "Exact-commit CI evidence cannot be green unless exactly `Python 3.10 compatibility`, `Python 3.11 compatibility`, `Python 3.12 compatibility`, `Python 3.13 compatibility`, and `Python 3.14 compatibility` are present and successful; the 3.12 job also serves as the comprehensive lane."
    - "The CLI accepts exactly two modes sharing required `--repo` and `--worktree-dir`: disposable requires only `--output-dir`, while final requires all of `--ci-evidence`, `--publish-root`, and `--result-json`; partial, mixed, and mode-less invocations fail."
  artifacts:
    - path: ".github/workflows/tests.yml"
      provides: "Python 3.10–3.14 compatibility and comprehensive Python 3.12 jobs"
    - path: "scripts/capture_sync_baseline.py"
      provides: "Exact-head GitHub Actions evidence collector"
      exports: ["validate_capture_mode", "collect_ci_evidence", "validate_ci_jobs"]
    - path: "tests/unit/test_sync_baseline_ci.py"
      provides: "Fail-closed CI run/job parsing tests"
  key_links:
    - from: "collect_ci_evidence"
      to: "GitHub Actions run"
      via: "`gh run list --commit <head-sha>` followed by exact `headSha` equality and `gh run watch --exit-status`"
      pattern: "headSha"
  flagged_assumptions:
    - requirement: "BASE-02"
      status: unresolved
      source: "spec-less edge probe returned unclassified"
      statement: "Hosted-run discovery and matrix-job edge behavior remains unclassified; absent, duplicate, cancelled, stale-head, or incomplete runs must block rather than select a convenient green run."
  prohibitions:
    - statement: "Compatibility evidence MUST NOT claim success from a workflow run whose head SHA differs from the exact implementation commit."
      status: resolved
      verification: test
---

<objective>
Land the compatibility workflow and exact-commit CI evidence contract before remote certification.

Purpose: D-04 requires the full Python 3.10+ range, and final evidence must be tied to the exact commit that contains the workflow and baseline implementation.
Output: A commit-producing CI plan with tested `gh` run/job validation; no push, PR, or remote mutation occurs in this plan.
</objective>

<execution_context>
@/Users/davis/.codex/gsd-core/workflows/execute-plan.md
@/Users/davis/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-protected-sync-baseline/01-CONTEXT.md
@.planning/phases/01-protected-sync-baseline/01-RESEARCH.md
@.planning/phases/01-protected-sync-baseline/01-VALIDATION.md
@pyproject.toml
@pytest.ini
@tests/README.md
@.github/workflows/tests.yml
@scripts/capture_sync_baseline.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the declared Python floor through the hosted-workflow contract</name>
  <reversibility rating="costly">The matrix expresses a public compatibility promise; reducing it later weakens the declared Python contract.</reversibility>
  <files>.github/workflows/tests.yml, tests/unit/test_sync_baseline_ci.py</files>
  <read_first>
    - .github/workflows/tests.yml
    - pyproject.toml
    - pytest.ini
    - tests/README.md
    - .planning/phases/01-protected-sync-baseline/01-CONTEXT.md
    - .planning/phases/01-protected-sync-baseline/01-RESEARCH.md
    - .planning/phases/01-protected-sync-baseline/01-VALIDATION.md
  </read_first>
  <behavior>
    - Python 3.10, 3.11, 3.12, 3.13, and 3.14 each receive a bounded uv-managed compatibility job.
    - Python 3.12 alone receives the comprehensive targeted, broad offline, explicitly excluded-suite, catalog, and five-surface lanes.
    - The five exact job display names are `Python 3.10 compatibility`, `Python 3.11 compatibility`, `Python 3.12 compatibility`, `Python 3.13 compatibility`, and `Python 3.14 compatibility`; there is no sixth comprehensive job.
  </behavior>
  <action>Extend the existing `actions/setup-python@v5` workflow without adding actions or package managers. Define string matrix values `3.10`, `3.11`, `3.12`, `3.13`, `3.14` and make the matrix job display expression yield exactly `Python 3.10 compatibility`, `Python 3.11 compatibility`, `Python 3.12 compatibility`, `Python 3.13 compatibility`, and `Python 3.14 compatibility`. Use uv and the committed lockfile; run the new Git, normalization, and smallest representative registry/discovery tests on every version with explicit timeouts, strict markers/config, and version-keyed JUnit artifacts. Make `Python 3.12 compatibility` also run the comprehensive D-01 lane and explicitly selected default-excluded `tests/tools`, `tests/examples`, and `tests/api` offline suites; do not create a sixth comprehensive job. Add `tests/unit/test_sync_baseline_ci.py` to assert the exact version set, exact five display names, comprehensive steps inside the 3.12 matrix job, and artifact naming. Do not add or claim `workflow_dispatch`; feature-branch dispatch is not assumed to work before the workflow exists on default.</action>
  <verify>
    <automated>uv run pytest -o addopts='' tests/unit/test_sync_baseline_ci.py -q -k workflow --strict-markers --strict-config --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - Workflow tests assert exactly five supported Python strings from 3.10 through 3.14.
    - The workflow yields exactly the five approved `Python 3.x compatibility` display names and no separate comprehensive job.
    - Each compatibility job uses uv, has a timeout, and emits version-keyed JUnit evidence; `Python 3.12 compatibility` alone also runs comprehensive lanes including suites hidden by repository addopts.
    - The workflow has no newly asserted `workflow_dispatch` dependency and retains existing checkout/cache/artifact conventions.
  </acceptance_criteria>
  <done>The implementation commit contains a deterministic Python 3.10–3.14 workflow contract suitable for exact-head remote certification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed exact-head Actions evidence collection</name>
  <files>scripts/capture_sync_baseline.py, tests/unit/test_sync_baseline_ci.py</files>
  <read_first>
    - scripts/capture_sync_baseline.py
    - tests/unit/test_sync_baseline_ci.py
    - .github/workflows/tests.yml
    - .planning/phases/01-protected-sync-baseline/01-RESEARCH.md
  </read_first>
  <behavior>
    - Run discovery is constrained by workflow file and exact requested commit SHA.
    - Zero matching runs, multiple ambiguous matching runs, or any mismatched `headSha` blocks.
    - The selected run is watched to completion with exit-status propagation.
    - CI is green only when the exact five approved display names are present once each, completed, and successful; `Python 3.12 compatibility` carries the comprehensive result.
    - Both modes require `--repo` and `--worktree-dir`: disposable accepts `--output-dir` and forbids every final flag; final requires `--ci-evidence`, `--publish-root`, and `--result-json` together and forbids `--output-dir`.
    - No mode flags, one or two final flags, or any mixture of `--output-dir` with final flags exits with an argparse usage error before baseline work starts.
  </behavior>
  <action>Add `EXPECTED_CI_JOB_NAMES` with exactly the five strings from Task 1, plus `collect_ci_evidence(repo, head_sha, run_command)` and `validate_ci_jobs(run, expected_job_names=EXPECTED_CI_JOB_NAMES)` using argv-only `gh` calls. Discover with `gh run list --workflow tests.yml --commit <head_sha> --json databaseId,headSha,status,conclusion,event,url,name,createdAt`; require an unambiguous run whose `headSha` equals the full requested SHA; wait using `gh run watch <databaseId> --exit-status`; retrieve `gh run view <databaseId> --json headSha,conclusion,jobs,url,event`. Require each exact expected name once, no missing or duplicate expected name, and success for all five; `Python 3.12 compatibility` is the comprehensive lane, not an additional job. Extend `validate_capture_mode(args)` from Plan 01-01 into exactly two mutually exclusive modes with common required `--repo` and `--worktree-dir`. Preserve disposable mode as `capture_sync_baseline.py --repo <repo> --worktree-dir <worktree> --output-dir <output>`, forbidding `--ci-evidence`, `--publish-root`, and `--result-json`. Add final mode as `capture_sync_baseline.py --repo <repo> --worktree-dir <worktree> --ci-evidence <ci.json> --publish-root <directory> --result-json <path>`, requiring all three final flags and forbidding `--output-dir`; validate exact-head CI, publish to `<publish-root>/<captured-full-fork-oid>/`, and write result JSON containing `fork_oid` and `output_dir`. In `tests/unit/test_sync_baseline_ci.py`, execute both exact Plan 01-05 command shapes with temporary fixtures: disposable `uv run python scripts/capture_sync_baseline.py --repo "$(pwd)" --worktree-dir "$tmp_root/worktree" --output-dir "$tmp_root/evidence"`, and final `uv run python scripts/capture_sync_baseline.py --repo "$(pwd)" --worktree-dir "$tmp_root/worktree" --ci-evidence "$tmp_root/ci.json" --publish-root .planning/phases/01-protected-sync-baseline/evidence --result-json "$tmp_root/result.json"`. Assert rejection for no mode flag, each one-flag and two-flag partial final set, and every mixed `--output-dir` plus final-flag combination. This task implements collection/validation interfaces only; it must not push a branch, open a PR, or claim hosted success before Plan 01-05 supplies remote availability.</action>
  <verify>
    <automated>uv run pytest -o addopts='' tests/unit/test_sync_baseline_ci.py -q -k 'exact_head or run or jobs or disposable_mode or final_mode or partial_mode or mixed_mode' --strict-markers --strict-config --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - Tests reject missing, ambiguous, stale-head, cancelled, failed, and incomplete workflow results.
    - Tests accept only an exact full-SHA run containing the exact five approved successful display names, with comprehensive evidence carried by `Python 3.12 compatibility` and no sixth expected job.
    - Tests execute the exact Plan 01-05 disposable command `uv run python scripts/capture_sync_baseline.py --repo "$(pwd)" --worktree-dir "$tmp_root/worktree" --output-dir "$tmp_root/evidence"` and final command `uv run python scripts/capture_sync_baseline.py --repo "$(pwd)" --worktree-dir "$tmp_root/worktree" --ci-evidence "$tmp_root/ci.json" --publish-root .planning/phases/01-protected-sync-baseline/evidence --result-json "$tmp_root/result.json"` successfully with bounded fixtures.
    - Tests prove common arguments are required, disposable mode forbids all final flags, final mode requires all three final flags and forbids `--output-dir`, and no-mode, partial-final, and mixed-mode invocations fail before work begins.
    - Final-mode tests reject wrong SHA/jobs and require result JSON `output_dir` equal `<publish-root>/<fork_oid>`.
    - Every `gh` call is an argv array with captured streams and a timeout; diagnostics are bounded and sanitized.
    - No code path performs `git push`, creates a PR, or mutates GitHub state.
  </acceptance_criteria>
  <done>The baseline can collect exact-commit hosted evidence after remote availability is explicitly authorized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local commit → GitHub run selection | A similarly named or stale run must not be attributed to the implementation commit. |
| GitHub run/jobs → baseline JSON | Remote status and job names are untrusted structured input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15 | Spoofing | Actions run selection | high | mitigate | Require exact full `headSha`, workflow identity, unambiguous run selection, and exact job-name/version set. |
| T-01-16 | Tampering | CI result parsing | high | mitigate | Parse `gh --json` output, reject absent/duplicate/incomplete jobs, and propagate `gh run watch --exit-status`. |
| T-01-17 | Elevation of Privilege | GitHub mutation | high | mitigate | This autonomous plan performs read-only `gh` operations only and reserves push/PR creation for Plan 01-05's checkpoint. |
| T-01-SC | Tampering | Package installation | low | accept | No packages or actions are added; existing uv and GitHub Actions dependencies are reused. |
</threat_model>

## Artifacts this phase produces

- `tests/unit/test_sync_baseline_ci.py`
- Python 3.10–3.14 workflow matrix and stable job names
- `collect_ci_evidence(repo, head_sha, run_command)`
- `validate_ci_jobs(run, expected_job_names=EXPECTED_CI_JOB_NAMES)`
- `EXPECTED_CI_JOB_NAMES`
- `validate_capture_mode(args)` with mutually exclusive disposable and final command contracts
- Baseline CLI common flags `--repo`, `--worktree-dir`; disposable flag `--output-dir`; final flags `--ci-evidence`, `--publish-root`, and `--result-json`
- Generated exact-head `ci.json` contract

<verification>
Run the CI unit file, validate workflow YAML through the repository's existing parser, and run Ruff on the script and test.
</verification>

<success_criteria>
- D-04 is represented by commit-ready Python 3.10–3.14 workflow code.
- Exact-head collection cannot green on stale or incomplete Actions results.
- CLI mode validation rejects missing, partial, or mixed destination contracts before execution.
- No remote mutation occurs before explicit authorization.
</success_criteria>

<output>
Create `.planning/phases/01-protected-sync-baseline/01-04-SUMMARY.md` when done.
</output>
