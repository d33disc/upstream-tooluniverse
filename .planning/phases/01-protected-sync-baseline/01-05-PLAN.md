---
phase: 01-protected-sync-baseline
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/baseline.json
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/git.json
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/preservation.json
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/environment.json
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/ci.json
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/tests/
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/probes/
  - .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/SHA256SUMS
autonomous: false
requirements: [BASE-01, BASE-02, PRES-01]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The local Python 3.12 baseline is fully green across every D-01/D-02/D-03 required lane before publication."
    - "A maintainer explicitly makes the implementation commit available on a remote branch or PR; planning does not silently authorize that external mutation."
    - "The watched Actions run and exactly five approved `Python 3.x compatibility` jobs belong to the exact local head SHA and are recorded before the baseline can green; `Python 3.12 compatibility` is also the comprehensive lane."
    - "The final full-fork-OID evidence directory contains immutable provenance, complete preservation, normalized probes, exact-head CI, and verified checksums."
    - "Task 1 uses only the disposable CLI mode, while Task 3 uses only the final CLI mode; both supply the common required repository and worktree arguments."
  artifacts:
    - path: ".planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/baseline.json"
      provides: "Final green gate over local, provider, preservation, and hosted evidence"
    - path: ".planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/ci.json"
      provides: "Exact-head Actions run and Python-version job evidence"
    - path: ".planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/SHA256SUMS"
      provides: "Integrity manifest for all committed evidence"
  key_links:
    - from: "local HEAD"
      to: "remote PR/run headSha"
      via: "explicit checkpoint followed by exact full-SHA `gh` checks"
      pattern: "headRefOid|headSha"
    - from: "baseline.json"
      to: "ci.json"
      via: "required hosted-CI stage that cannot be omitted or defaulted green"
      pattern: "ci"
  flagged_assumptions:
    - requirement: BASE-01
      status: unresolved
      source: "spec-less edge probe returned unclassified"
      statement: "The upstream target is refreshed exactly once at execution start and then frozen; later movement cannot silently retarget evidence."
    - requirement: BASE-02
      status: unresolved
      source: "spec-less edge probe returned unclassified"
      statement: "Any required skip, provider mapping, hosted job, or stage not explicitly recognized remains blocking."
    - requirement: PRES-01
      status: unresolved
      source: "spec-less edge probe returned unclassified"
      statement: "The repaired links and every preservation class must remain green; no target content may be invented or user-untracked content opened."
  prohibitions:
    - statement: "The phase MUST NOT declare synchronization-ready without exact-head successful Python 3.10–3.14 hosted CI evidence."
      status: resolved
      verification: test
    - statement: "The final evidence MUST NOT include credentials, user-owned untracked contents, unbounded provider payloads, or results from a moving revision."
      status: resolved
      verification: test
---

<objective>
Certify and publish the protected baseline only after local, provider, preservation, and exact-commit hosted gates are green.

Purpose: This closes BASE-01, BASE-02, PRES-01 and D-01 through D-07 without assuming authority to push or open a PR.
Output: A checksummed evidence bundle keyed by the full fork OID and backed by exact-head Python 3.10–3.14 CI.
</objective>

<execution_context>
@/Users/davis/.codex/gsd-core/workflows/execute-plan.md
@/Users/davis/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-protected-sync-baseline/01-CONTEXT.md
@.planning/phases/01-protected-sync-baseline/01-RESEARCH.md
@.planning/phases/01-protected-sync-baseline/01-VALIDATION.md
@scripts/capture_sync_baseline.py
@tests/unit/test_sync_baseline_git.py
@tests/unit/test_sync_baseline_normalize.py
@tests/unit/test_sync_baseline_ci.py
@tests/integration/test_sync_baseline_surfaces.py
@.github/workflows/tests.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the full local Python 3.12 gate into a disposable evidence bundle</name>
  <files>/tmp/tooluniverse-phase1.<unique>/worktree/, /tmp/tooluniverse-phase1.<unique>/evidence/</files>
  <read_first>
    - scripts/capture_sync_baseline.py
    - .planning/phases/01-protected-sync-baseline/01-CONTEXT.md
    - .planning/phases/01-protected-sync-baseline/01-RESEARCH.md
    - .planning/phases/01-protected-sync-baseline/01-VALIDATION.md
    - tests/unit/test_sync_baseline_git.py
    - tests/unit/test_sync_baseline_normalize.py
    - tests/integration/test_sync_baseline_surfaces.py
    - pytest.ini
  </read_first>
  <action>Refresh canonical upstream main exactly once, record and freeze its full OID, then run the complete orchestrator locally under Python 3.12 against a detached worktree at the pre-sync fork OID. Use one `/tmp/tooluniverse-phase1.XXXXXX` root with `worktree/` and `evidence/` children. Invoke the disposable CLI mode exactly as `capture_sync_baseline.py --repo <repo> --worktree-dir <worktree> --output-dir <output>`; do not supply `--ci-evidence`, `--publish-root`, or `--result-json`. Install a best-effort trap only for failure/interruption; on success, explicitly remove the registered worktree with failure propagation, assert its exact path is absent from `git worktree list --porcelain`, delete only the exact temporary root, and then disarm the trap. Run preflight/symlink integrity, targeted high-risk, catalog load, deterministic Python/CLI, real MCP stdio/HTTP and REST, broad offline, explicit tools/examples/API, configured-provider, normalization, secret-scan, and checksum stages per D-01 through D-07. This tracer publishes only to the disposable evidence directory; final repository evidence waits for exact-head hosted CI.</action>
  <verify>
    <automated>tmp_root=$(mktemp -d /tmp/tooluniverse-phase1.XXXXXX) && trap 'git worktree remove --force "$tmp_root/worktree" >/dev/null 2>&1 || true; rm -rf -- "$tmp_root"' EXIT HUP INT TERM && uv run python scripts/capture_sync_baseline.py --repo "$(pwd)" --worktree-dir "$tmp_root/worktree" --output-dir "$tmp_root/evidence" && test -f "$tmp_root/evidence/SHA256SUMS" && { if git worktree list --porcelain | grep -Fqx "worktree $tmp_root/worktree"; then git worktree remove --force "$tmp_root/worktree"; fi; } && ! git worktree list --porcelain | grep -Fqx "worktree $tmp_root/worktree" && rm -rf -- "$tmp_root" && test ! -e "$tmp_root" && trap - EXIT HUP INT TERM</automated>
  </verify>
  <acceptance_criteria>
    - Local Python 3.12 has zero required failures, unexplained skips, unmapped configured providers, unresolved preservation classes, or broken protected links.
    - Upstream is refreshed once and the full selected OID is reused unchanged by every stage.
    - The disposable bundle contains normalized summaries and only bounded sanitized raw excerpts needed for failure diagnosis.
    - The invocation uses common `--repo` and `--worktree-dir` plus disposable-only `--output-dir`, with no final-mode flags.
    - Failure/interruption uses the best-effort trap; successful cleanup propagates worktree-removal failure, proves the exact path absent from `git worktree list --porcelain`, deletes the exact temporary root, and disarms the trap.
  </acceptance_criteria>
  <done>The full local gate is green in a disposable bundle and ready to be paired with exact-head hosted evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Make the exact implementation commit available to hosted CI</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/tests.yml
    - .planning/phases/01-protected-sync-baseline/01-CONTEXT.md
  </read_first>
  <action>Read the full local HEAD SHA and use read-only `gh pr view --json headRefOid,url,state` plus remote-ref inspection to determine whether an open PR or remote branch already points to that exact SHA. If it does, present the URL/SHA for confirmation. Otherwise pause and ask the maintainer to push this exact commit and open or update a PR that triggers `tests.yml`; do not perform the push, PR creation, or branch mutation automatically.</action>
  <instructions>First run the read-only exact-SHA checks. If a matching open PR already exists, provide its URL and confirm that its head SHA equals local HEAD. Otherwise push the exact implementation commit and open or update a PR that triggers `.github/workflows/tests.yml`, then provide the resulting PR URL. Do not amend or replace the implementation commit while satisfying this checkpoint.</instructions>
  <verify>
    <automated>head_sha=$(git rev-parse HEAD) && test "$(gh pr view --json headRefOid --jq .headRefOid)" = "$head_sha"</automated>
  </verify>
  <verification>Use `gh pr view --json headRefOid,url,state` and require `state=OPEN` plus `headRefOid` equal to the full local `git rev-parse HEAD` value.</verification>
  <acceptance_criteria>
    - An open PR or authorized remote branch resolves to the exact full local HEAD SHA.
    - The remote event is eligible to trigger the committed `tests.yml` workflow without relying on feature-branch `workflow_dispatch` availability.
    - No agent action silently pushes commits, creates a PR, or changes remote repository state.
  </acceptance_criteria>
  <what-built>Commit-ready Python 3.10–3.14 workflow and complete local baseline implementation.</what-built>
  <how-to-verify>Confirm the displayed PR/remote URL resolves to the exact local HEAD SHA and has triggered `tests.yml`; if absent, push/open the PR, then provide its URL.</how-to-verify>
  <resume-signal>Provide the PR URL and confirm its head SHA matches local HEAD.</resume-signal>
  <done>The exact implementation commit is remotely available and authorized for hosted CI observation.</done>
</task>

<task type="auto">
  <name>Task 3: Watch exact-head CI and publish the final checksummed baseline</name>
  <precondition>An open PR or authorized remote ref points to the exact local HEAD SHA and has triggered `tests.yml`.</precondition>
  <files>.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/baseline.json, .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/git.json, .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/preservation.json, .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/environment.json, .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/ci.json, .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/tests/, .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/probes/, .planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/SHA256SUMS</files>
  <read_first>
    - scripts/capture_sync_baseline.py
    - tests/unit/test_sync_baseline_ci.py
    - .github/workflows/tests.yml
    - .planning/phases/01-protected-sync-baseline/01-VALIDATION.md
  </read_first>
  <action>Resolve `HEAD_SHA` from local Git and independently confirm the PR `headRefOid` equals it. Create one bounded `/tmp/tooluniverse-phase1-final.XXXXXX` root and install a failure/interruption cleanup trap. Use `gh run list --workflow tests.yml --commit "$HEAD_SHA" --json databaseId,headSha,status,conclusion,event,url,name,createdAt`, require one exact-`headSha` run, execute `gh run watch <databaseId> --exit-status`, and materialize `$tmp_root/ci.json` from `gh run view`. Validate exactly `Python 3.10 compatibility`, `Python 3.11 compatibility`, `Python 3.12 compatibility`, `Python 3.13 compatibility`, and `Python 3.14 compatibility` once each and successful; the 3.12 job also carries comprehensive evidence, with no sixth job expected. Run a fresh final `capture_sync_baseline.py` in final mode exactly as `capture_sync_baseline.py --repo <repo> --worktree-dir <worktree> --ci-evidence <ci.json> --publish-root <directory> --result-json <path>`; do not supply `--output-dir`. From result JSON, require `output_dir` basename equals its full `fork_oid`; require `baseline.json` overall green and named stages `git`, `preservation`, `environment`, `targeted_tests`, `catalog`, `python_probe`, `cli_probe`, `mcp_stdio_probe`, `mcp_http_probe`, `rest_probe`, `broad_offline`, `excluded_suites`, `configured_providers`, `ci`, `secret_scan`, and `checksums` all green. Require `git.json`, `preservation.json`, `environment.json`, `ci.json`, `tests/`, `probes/`, and `SHA256SUMS`, then verify checksums. Commit normalized summaries and bounded sanitized excerpts only. On success, strictly remove and de-register the exact worktree, delete the exact temp root, and disarm the trap. Any mismatch/failure leaves `baseline.json` non-green and blocks synchronization.</action>
  <verify>
    <automated>tmp_root=$(mktemp -d /tmp/tooluniverse-phase1-final.XXXXXX) && trap 'git worktree remove --force "$tmp_root/worktree" >/dev/null 2>&1 || true; rm -rf -- "$tmp_root"' EXIT HUP INT TERM && head_sha=$(git rev-parse HEAD) && pr_sha=$(gh pr view --json headRefOid --jq .headRefOid) && test "$pr_sha" = "$head_sha" && run_id=$(gh run list --workflow tests.yml --commit "$head_sha" --json databaseId,headSha --jq "map(select(.headSha == \"$head_sha\")) | if length == 1 then .[0].databaseId else error(\"expected one exact-head run\") end") && gh run watch "$run_id" --exit-status && gh run view "$run_id" --json headSha,conclusion,jobs,url,event > "$tmp_root/ci.json" && uv run python scripts/capture_sync_baseline.py --repo "$(pwd)" --worktree-dir "$tmp_root/worktree" --ci-evidence "$tmp_root/ci.json" --publish-root .planning/phases/01-protected-sync-baseline/evidence --result-json "$tmp_root/result.json" && final_dir=$(jq -er .output_dir "$tmp_root/result.json") && fork_oid=$(jq -er .fork_oid "$tmp_root/result.json") && test "$(basename "$final_dir")" = "$fork_oid" && test "$(jq -er .fork_oid "$final_dir/git.json")" = "$fork_oid" && jq -e --argjson required '["git","preservation","environment","targeted_tests","catalog","python_probe","cli_probe","mcp_stdio_probe","mcp_http_probe","rest_probe","broad_offline","excluded_suites","configured_providers","ci","secret_scan","checksums"]' '. as $root | $root.overall_status == "green" and ($required | all(. as $stage | $root.stages[$stage].status == "green"))' "$final_dir/baseline.json" >/dev/null && test -f "$final_dir/git.json" && test -f "$final_dir/preservation.json" && test -f "$final_dir/environment.json" && test -f "$final_dir/ci.json" && test -d "$final_dir/tests" && test -d "$final_dir/probes" && test -f "$final_dir/SHA256SUMS" && (cd "$final_dir" && shasum -a 256 -c SHA256SUMS) && { if git worktree list --porcelain | grep -Fqx "worktree $tmp_root/worktree"; then git worktree remove --force "$tmp_root/worktree"; fi; } && ! git worktree list --porcelain | grep -Fqx "worktree $tmp_root/worktree" && rm -rf -- "$tmp_root" && test ! -e "$tmp_root" && trap - EXIT HUP INT TERM</automated>
  </verify>
  <acceptance_criteria>
    - PR head, Actions run `headSha`, and local HEAD are the same full SHA.
    - `ci.json` records exactly the five approved successful display names, with `Python 3.12 compatibility` also serving as the comprehensive lane.
    - Final `baseline.json` is green only when all local, provider, preservation, surface, CI, secret, and integrity stages are green.
    - The final directory is named by the full pre-sync fork OID and `SHA256SUMS` verifies every included file except itself.
    - Automated verification performs the fresh final capture with materialized `ci.json`, checks every required named stage and artifact, and proves `git.json` fork OID equals the output-directory basename.
    - The invocation uses common `--repo` and `--worktree-dir` plus all three final-only flags, with no `--output-dir`.
    - Evidence includes no credential values, user-owned untracked contents, or unbounded raw provider payloads.
  </acceptance_criteria>
  <done>Maintainers have an exact-commit, cross-runtime, fully green protected baseline from which synchronization may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repository → remote branch/PR | Publishing commits changes external state and requires explicit maintainer authorization. |
| GitHub Actions → final evidence | Remote results must match exact local source and complete job expectations. |
| Disposable evidence → committed bundle | Only complete, bounded, secret-safe, checksummed evidence may be published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Elevation of Privilege | Push/PR boundary | high | mitigate | Non-autonomous checkpoint; agent performs read-only checks and never silently pushes or opens a PR. |
| T-01-19 | Spoofing | Exact-head CI | high | mitigate | Triple equality across local HEAD, PR `headRefOid`, and run `headSha`, then exact job-set validation. |
| T-01-20 | Information Disclosure | Final evidence | high | mitigate | Value-free environment data, normalized summaries, bounded sanitized excerpts, and pre-publication secret scan. |
| T-01-21 | Tampering | Final green gate | high | mitigate | Required named stages plus verified SHA256SUMS; missing or non-green stages cannot default to pass. |
| T-01-22 | Denial of Service | Local/remote long-running gates | medium | mitigate | Fail-fast ordering, explicit timeouts, finite retries, `gh run watch --exit-status`, and safe temporary cleanup trap. |
| T-01-SC | Tampering | Package installation | low | accept | No new dependencies are installed; existing uv, gh, Git, and project libraries are used. |
</threat_model>

## Artifacts this phase produces

- Exact-head remote availability checkpoint record
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/baseline.json`
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/git.json`
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/preservation.json`
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/environment.json`
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/ci.json`
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/tests/`
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/probes/`
- `.planning/phases/01-protected-sync-baseline/evidence/<full-fork-oid>/SHA256SUMS`

<verification>
Run the complete local Python 3.12 capture with safe temporary cleanup, verify exact-head hosted jobs through `gh`, then validate the final checksum and secret gates.
</verification>

<success_criteria>
- BASE-01 records frozen provenance and separates user-owned checkout metadata from synchronization work.
- BASE-02 is fully green locally and across exact-head Python 3.10–3.14 hosted jobs.
- PRES-01 includes proven repaired links and complete protected-asset evidence.
- D-01 through D-07 are represented in executable gates with no failure waiver.
</success_criteria>

<output>
Create `.planning/phases/01-protected-sync-baseline/01-05-SUMMARY.md` when done.
</output>
