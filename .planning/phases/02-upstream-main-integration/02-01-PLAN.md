---
phase: 02-upstream-main-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit_upstream_merge.py
  - tests/unit/test_audit_upstream_merge.py
  - .planning/phases/02-upstream-main-integration/evidence/staging/union.json
  - .planning/phases/02-upstream-main-integration/evidence/staging/SHA256SUMS
autonomous: true
requirements: [SYNC-02]

estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every src/tooluniverse/data/*.json file touched on BOTH the fork side (4d668698..e0755067) and the upstream side (4d668698..56adcfd9) has a recorded union verdict in union.json — not only the 2 files git reported as conflicted (D-08, SYNC-02)."
    - "For every such file present on all three sides, the merged tool-name set at f81448f2 equals fork_names | upstream_names exactly, with zero duplicate name values (D-08 union rule made mechanically falsifiable)."
    - "Every file upstream deleted is recorded as an explicit upstream_deleted verdict carrying the lost tool names and a relocation search result — never silently counted as passing."
    - "union.json plus a sorted SHA256SUMS exist under evidence/staging/ and shasum -a 256 -c passes."
  artifacts:
    - scripts/audit_upstream_merge.py
    - tests/unit/test_audit_upstream_merge.py
    - .planning/phases/02-upstream-main-integration/evidence/staging/union.json
    - .planning/phases/02-upstream-main-integration/evidence/staging/SHA256SUMS
  key_links:
    - "audit_upstream_merge.run_git is the run_git loaded from scripts/capture_sync_baseline.py via importlib.util.spec_from_file_location — not a reimplemented subprocess wrapper (scripts/ has no __init__.py, so a plain import fails)."
    - "derive_both_sides_paths() re-derives the merge-base at runtime with git merge-base; the 4d668698 SHA in this plan is a re-verification target, never a hardcoded input."
    - "classify_union() is a pure function over three name sets plus a presence triple, so tests/unit/test_audit_upstream_merge.py can exercise every verdict without a git repository."
  prohibitions:
    - "MUST NOT push any ref to the upstream remote (mims-harvard/ToolUniverse). origin is the boundary."
    - "MUST NOT hardcode the both-sides file list; it is derived at runtime from git merge-base and two git diff --name-only calls."
    - "MUST NOT parse tool-definition JSON with any dynamic-evaluation builtin; the standard library JSON parser only."
    - "MUST NOT shell-interpolate git arguments; every git call goes through the argv-only run_git boundary."
    - "MUST NOT write file contents, API key values, or tool payloads into the evidence bundle; names, paths, counts and verdicts only."
    - "MUST NOT modify, stage, or delete .planning/config.json or ralph-specs/fleet/results/ — user-owned untracked paths."
    - "MUST NOT traverse or write through plugin/skills/*-workspace symlinks; inspect with lstat/readlink only."
    - "MUST NOT diff any Phase 1 probe JSON, catalog.json, or tests/*.json against a Phase 2 result (excluded by D-03/D-04)."
    - "MUST NOT introduce a package manager other than uv, or apply a global ruff config in place of the repository pyproject.toml settings."
---

<objective>
Build the phase's central mechanical check for SYNC-02 and prove it end-to-end: an entry-level tool-name union sweep over **every** `src/tooluniverse/data/*.json` file that both the fork side and the upstream side modified, comparing the pre-merge fork tree, the upstream tree, and what the landed merge actually produced.

Purpose: `git diff-tree --cc f81448f2` reports 22 hand-resolved files, but **213** `data/*.json` files were touched on both sides. Git auto-resolved 211 of them with no conflict marker and no human review. Per RESEARCH.md Pitfall 1, that is exactly the class of auto-resolution that can silently drop a fork-only tool entry or duplicate a `name` while producing syntactically valid JSON. A clean merge is not evidence of a correct merge. This sweep is the only thing that makes D-08's union rule falsifiable.

Output: `scripts/audit_upstream_merge.py` with a `union` subcommand, unit tests for its pure classifier, and a checksummed `union.json` evidence artifact under `evidence/staging/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-upstream-main-integration/02-CONTEXT.md
@.planning/phases/02-upstream-main-integration/02-PATTERNS.md
@.planning/phases/02-upstream-main-integration/02-RESEARCH.md
</context>

<verified_git_facts>
Re-verified live in `/Users/davis/code/ToolUniverse` on branch `docs/gsd-codebase-map` at HEAD `776c7e32` during planning. Re-derive each before relying on it; do not trust these values blind.

| Fact | Value |
|---|---|
| upstream/main | `56adcfd9c299078d0c40fde642b0be006510ccf3` |
| merge-base(HEAD, upstream/main) | `56adcfd9...` — upstream is already fully contained |
| landed merge under audit | `f81448f2047a6f35bd552956a0d9990019a39eb1` |
| its parents | `e0755067ebe7cc5374f033c5c28160980c5eddfa` (fork), `56adcfd9...` (upstream) |
| merge-base(e0755067, 56adcfd9) | `4d668698a1116a6aa18a8dfeef83b7c9715f7a8b` |
| files touched on BOTH sides | 297 total — 213 `src/tooluniverse/data/*.json`, 48 `src/tooluniverse/*.py`, 36 other |
| files git reported conflicted | 22 (`git diff-tree --cc f81448f2 --name-only \| tail -n +2`) |
| D-01/D-02 source-tree equality gate | HOLDS — `git diff --name-only 21945440 HEAD \| grep -v '^\.planning/'` is empty |
</verified_git_facts>

<flagged_assumptions>
Surfaced, not silently resolved. The edge-coverage engine returned `unclassified` / `unresolved` for all three phase requirements and per #1110 those are never auto-backstopped; they are recorded here instead.

| # | Assumption | Status | Disposition in this phase |
|---|---|---|---|
| EC-SYNC-01 | Edge coverage for SYNC-01 is `unclassified` / `unresolved` — no deterministic edge category was derived, and no backstop was authored. | UNRESOLVED (flagged) | Plan 02 provides the explicit containment check (`git diff --stat` of the re-merge stage against `e0755067`) as the sole SYNC-01 edge evidence. |
| EC-SYNC-02 | Edge coverage for SYNC-02 is `unclassified` / `unresolved`. | UNRESOLVED (flagged) | This plan's union sweep plus `tests/unit/test_registry_integrity.py` are the SYNC-02 edge evidence. |
| EC-PRES-02 | Edge coverage for PRES-02 is `unclassified` / `unresolved`. | UNRESOLVED (flagged) | Plans 03 and 04 provide the preservation join and the fresh custom-tool probes. |
| A1 (RESEARCH.md, MEDIUM) | D-08 names `default_config.py` and `_lazy_registry_static.py` as "structural files" taking the union; it does not explicitly classify JSON tool-definition arrays. Union vs. plain "upstream wins outright" differ in whether fork-only entries survive. | RESOLVED ON EVIDENCE, still flagged | **This phase applies union.** Measured during planning: `uspto_tools.json` carries two fork-only names (`USPTO_get_patent_assignment`, `USPTO_get_patent_transactions`) that survived `f81448f2` and persist at the pin; `literature_search_tools.json` has identical name sets on all three sides, so the two readings are observationally indistinguishable there. The landed merge already behaved as union, and the corrective set is empty under that reading. The union assertion stays two-sided so a duplicate `name` is caught alongside a drop. |
| A2 (CONTEXT.md D-03 factual error) | CONTEXT.md states every `preservation.json` entry carries `class: other_review_required`. | CONTRADICTED BY PRIMARY SOURCE | Measured: 1,392 paths distribute as `custom_code` 512, `documentation` 425, `plugin_asset` 240, `other_review_required` 84, `planning` 64, `test` 45, `skill` 8, `generated_asset` 5, `workflow` 5, `tool_definition` 4. Plan 03 re-classifies against the **real** distribution. Surfaced rather than silently overwriting a locked document. |
| A3 (comparison-base hazard) | `preservation.json`'s own header is `fork_oid: 21945440` (the pin) and `upstream_oid: 56adcfd9` — it is an upstream↔pinned-fork delta, **not** an upstream↔`e0755067` delta. | FLAGGED | The re-merge tree is comparable to `f81448f2`, 31 commits *before* the pin. Joining a re-merge finding to a `preservation.json` row therefore crosses two different comparison bases. This is the mechanism by which D-06a's self-heal recheck earns its keep. Plan 03 must treat the join as base-crossing, not trivially mechanical. |
| OQ1 (RESEARCH.md open question) | Is `pyproject.toml` in D-07's full-tree comparison scope? | DECIDED HERE | **Yes, mechanically** — D-07 says full-tree. But a dependency-version disagreement is pre-declared as **not** a criterion-2 finding; it routes to Phase 5 / COMP-01. Recorded so no executor relitigates it. |
| OQ2 (RESEARCH.md open question) | Does the re-merge worktree get a fresh `uv sync` or reuse the main checkout `.venv`? | DECIDED HERE | **Fresh `uv sync`, scoped to the tasks that need it.** The union sweep and the full-tree diff are pure git operations needing no Python environment. Only the `tu build` regeneration (Plan 02) and the criterion-4 probes (Plan 04) sync. |
</flagged_assumptions>

<assumption_delta_decision>
Detector fired `detected: true`, one `chosen` signal on the term "custom" in the ROADMAP goal line ("without losing its custom capabilities").

- **Noun now primary:** none changed. "Custom capability" is a pre-existing category of the fork, not a value that moved from derived to chosen.
- **Decision:** `no-change`. The signal is a lexical false positive: the goal sentence uses "custom" as an adjective describing existing fork assets, not as a newly-parameterized configuration decision. No identity model, primary key, or anchor field is generalizing in this phase.
- **Advisory only** — recorded, not blocking.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end three-tree tool-name union sweep over every both-sides data/*.json</name>

  <read_first>
    - `scripts/capture_sync_baseline.py` — read `run_git` (line ~634), `_oid` (~656), `_nul_records` (~663), `classify_preservation_path` (~853), `_canonical_json` (~1305), `_contains_secret` (~1312), `publish_evidence` (~1317), `verify_checksums` (~1372). Reuse `run_git` and the checksum idiom; do not reimplement subprocess or checksum handling.
    - `tests/unit/test_sync_baseline_git.py` lines 1-25 — the `importlib.util.spec_from_file_location` idiom used to load `scripts/capture_sync_baseline.py`. `scripts/` has no `__init__.py`, so a plain `import` fails.
    - `tests/unit/test_registry_integrity.py` — `_load_defined_tool_names()` (~lines 33-51) for the defensive `list`/`dict`/skip-on-decode-error shape to copy when loading tool names.
    - `src/tooluniverse/data/uspto_tools.json` — the `list[dict]` keyed on `"name"` shape the sweep must handle.
    - `.planning/phases/02-upstream-main-integration/02-PATTERNS.md` §1, §2 — the analog excerpts.
  </read_first>

  <files>scripts/audit_upstream_merge.py</files>

  <behavior>
    `classify_union(fork_names, upstream_names, merged_names, present)` is a pure function over three optional `set[str]` plus a `(fork_present, upstream_present, merged_present)` boolean triple, returning exactly one verdict string:
    - `union_ok` — all three present, `merged_names == fork_names | upstream_names`, and the merged array has no repeated `name`.
    - `net_removed_fork_entry` — all three present and `(fork_names | upstream_names) - merged_names` is non-empty and intersects `fork_names`.
    - `net_removed_upstream_entry` — same shape but the missing names come only from `upstream_names`.
    - `unexpected_added_entry` — `merged_names - (fork_names | upstream_names)` is non-empty.
    - `duplicate_name` — the merged array contains the same `name` value more than once.
    - `upstream_deleted` — fork present, upstream absent, merged absent.
    - `fork_deleted` — fork absent, upstream present, merged absent.
    - `not_an_array` — any side parses to something that is not a `list`.
    - `unparseable` — any side fails JSON decode.
    Test cases to write first in Task 2: each verdict above; the empty-set edge (`fork_names == upstream_names == merged_names == set()` yields `union_ok`); and the ordering-insensitivity case (same names, different array order, yields `union_ok`).
  </behavior>

  <action>
Create `scripts/audit_upstream_merge.py` as a standalone CLI script (no `scripts/__init__.py` exists; keep it directly runnable as `python3 scripts/audit_upstream_merge.py`). Load `run_git`, `GitCaptureError`, `_oid`, and `_canonical_json` from `scripts/capture_sync_baseline.py` using `importlib.util.spec_from_file_location`, exactly as `tests/unit/test_sync_baseline_git.py` does. Do not reimplement the subprocess boundary.

Implement these module-level names:

- Constants for the four audited refs, each overridable by a CLI flag and each resolved to a full 40-char OID via `_oid()` before use: `DEFAULT_FORK_OID = "e0755067ebe7cc5374f033c5c28160980c5eddfa"`, `DEFAULT_UPSTREAM_OID = "56adcfd9c299078d0c40fde642b0be006510ccf3"`, `DEFAULT_MERGED_OID = "f81448f2047a6f35bd552956a0d9990019a39eb1"`, `DEFAULT_PIN_OID = "21945440c9f2a15537ba878500a800d9e330eab0"`.
- `derive_both_sides_paths(repo, fork_oid, upstream_oid) -> tuple[str, list[str]]` — computes the merge-base at runtime with `run_git(["merge-base", fork_oid, upstream_oid], repo)`, then intersects `git diff --name-only <base> <fork_oid>` with `git diff --name-only <base> <upstream_oid>`. Returns `(base_oid, sorted_intersection)`. The expected base is `4d668698a1116a6aa18a8dfeef83b7c9715f7a8b` and the expected intersection size is 297 — assert against these as a re-verification tripwire and fail loudly with both values if either differs, rather than hardcoding the list.
- `load_json_at(repo, ref, path) -> tuple[bool, object | None]` — `git show <ref>:<path>` via `run_git`; returns `(False, None)` when the path is absent at that ref (catch `GitCaptureError`), `(True, parsed)` otherwise. Parse with the standard library JSON module.
- `tool_names(value) -> set[str] | None` and `tool_name_list(value) -> list[str] | None` — copy the defensive shape from `tests/unit/test_registry_integrity.py::_load_defined_tool_names`: accept `list`, wrap a bare `dict`, skip non-dict items and items with no `"name"` key, return `None` when the value is not a list or dict. The list form preserves repeats so `duplicate_name` is detectable.
- `classify_union(...)` — the pure function specified in `<behavior>`. No I/O.
- `search_relocated_names(repo, ref, names) -> dict[str, list[str]]` — for the `upstream_deleted` case only, walk `git ls-tree -r --name-only <ref> -- src/tooluniverse/data` and report, per lost name, which other JSON paths at that ref define it. This is what turns a silent deletion into an accounted-for relocation. Planning measured that `pathway_commons_tools.json` (`pc_search_pathways`, `pc_get_interactions`) and `soilgrids_tools.json` (`SoilGrids_get_properties`) were deleted by upstream and their definitions now live under `src/tooluniverse/data/broken_apis/` — that must surface as `relocated_to`, not as a pass.
- `sweep_data_json(repo, base, fork_oid, upstream_oid, merged_oid, paths) -> dict` — filters `paths` to entries matching `src/tooluniverse/data/` and ending `.json`, runs the per-file comparison, and returns `{"base_oid", "fork_oid", "upstream_oid", "merged_oid", "files": [...], "summary": {...}}`. Each `files` record carries `path`, `verdict`, `fork_name_count`, `upstream_name_count`, `merged_name_count`, `missing_names` (sorted list), `extra_names` (sorted list), `duplicate_names` (sorted list), and `relocated_to` (present only for `upstream_deleted`).
- The `summary` object carries exactly these keys: `files_checked`, `union_ok`, `net_removed_fork_entries`, `net_removed_upstream_entries`, `unexpected_added_entries`, `duplicate_name_files`, `upstream_deleted_files`, `fork_deleted_files`, `not_an_array_files`, `unparseable_files`, and `unrelocated_lost_names` (a sorted list of names lost to an `upstream_deleted` file whose `relocated_to` came back empty).
- `write_staging_artifact(out_dir, name, payload) -> Path` — writes canonical JSON (sorted keys, trailing newline) via the `_canonical_json` idiom, then regenerates a **sorted** `SHA256SUMS` covering every `*.json` file in `out_dir` and its subdirectories, in the `shasum -a 256` output format so `shasum -a 256 -c SHA256SUMS` verifies from inside `out_dir`. Emit names, paths, counts and verdicts only — never file contents, tool payloads, or credential values.
- `main(argv=None) -> int` with an `argparse` subparser named `union`, taking `--repo` (default `.`), `--fork`, `--upstream`, `--merged`, `--out` (default `.planning/phases/02-upstream-main-integration/evidence/staging`), and `--json`. Exit 0 on a clean sweep; exit 1 when `net_removed_fork_entries`, `net_removed_upstream_entries`, `unexpected_added_entries`, `duplicate_name_files`, `unparseable_files`, or `not_an_array_files` is non-zero, or `unrelocated_lost_names` is non-empty. `upstream_deleted_files` with a non-empty `relocated_to` for every lost name is a recorded, non-failing verdict.

Before touching git state, assert the working context: `git rev-parse --abbrev-ref HEAD` is `docs/gsd-codebase-map`, and `git status --porcelain` shows nothing beyond the two known user-owned untracked paths `.planning/config.json` and `ralph-specs/fleet/results/`. Concurrent Claude sessions share this checkout and auto-commit each other's edits; halt and report rather than staging over another session's work.

Format with `ruff format` using the repository `pyproject.toml` settings, then run the sweep and commit the script together with `union.json` and `SHA256SUMS`.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && python3 scripts/audit_upstream_merge.py union --repo . --out .planning/phases/02-upstream-main-integration/evidence/staging --json && jq -e '.summary.files_checked >= 213 and .summary.net_removed_fork_entries == 0 and .summary.net_removed_upstream_entries == 0 and .summary.unexpected_added_entries == 0 and .summary.duplicate_name_files == 0 and .summary.unparseable_files == 0 and (.summary.unrelocated_lost_names | length) == 0' .planning/phases/02-upstream-main-integration/evidence/staging/union.json && (cd .planning/phases/02-upstream-main-integration/evidence/staging && shasum -a 256 -c SHA256SUMS)</automated>
  </verify>

  <acceptance_criteria>
    - `python3 scripts/audit_upstream_merge.py union --repo . --json` exits 0.
    - `jq -r '.base_oid' union.json` equals `4d668698a1116a6aa18a8dfeef83b7c9715f7a8b`.
    - `jq -r '.summary.files_checked' union.json` is at least 213.
    - `jq -r '[.files[] | select(.path == "src/tooluniverse/data/uspto_tools.json")][0].verdict' union.json` equals `union_ok`, and that record's `merged_name_count` equals 8.
    - `jq -r '[.files[] | select(.verdict == "upstream_deleted")] | length' union.json` equals 2, and `jq -e '[.files[] | select(.verdict == "upstream_deleted") | .relocated_to | length > 0] | all' union.json` is true.
    - `jq -e '.summary.unrelocated_lost_names | length == 0' union.json` is true.
    - `shasum -a 256 -c SHA256SUMS` run from inside `evidence/staging/` reports OK for every listed file and exits 0.
    - `grep -v '^\s*#' scripts/audit_upstream_merge.py | grep -c 'subprocess\.' ` returns 0 — every git call goes through the imported `run_git`.
    - `ruff format --check scripts/audit_upstream_merge.py` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">The sweep is read-only against git history and writes only new evidence files; deleting `evidence/staging/` fully undoes it.</reversibility>

  <done>`union.json` records a verdict for every both-sides `src/tooluniverse/data/*.json` path, the summary shows zero net-removed fork entries and zero duplicates, both `upstream_deleted` files carry a non-empty `relocated_to`, and the bundle verifies against its sorted `SHA256SUMS`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for the pure union classifier and name loaders</name>

  <read_first>
    - `scripts/audit_upstream_merge.py` — the module written in Task 1; test against its actual signatures.
    - `tests/unit/test_sync_baseline_git.py` lines 1-30 — the `importlib.util.spec_from_file_location` loader block and the local `git()` / `repo()` fixture helpers to mirror.
    - `pytest.ini` — the default selection that excludes `tests/tools`, `tests/examples`, `tests/api` and the `slow` / `network` / `require_api_keys` markers. The new test must run under the default selection with no marker.
  </read_first>

  <files>tests/unit/test_audit_upstream_merge.py</files>

  <behavior>
    - `classify_union` returns `union_ok` when the merged names equal the union and no name repeats.
    - `classify_union` returns `union_ok` when the same names appear in a different array order (order is not semantically load-bearing).
    - `classify_union` returns `net_removed_fork_entry` when a fork-only name is missing from the merged set.
    - `classify_union` returns `net_removed_upstream_entry` when only an upstream-only name is missing.
    - `classify_union` returns `unexpected_added_entry` when the merged set contains a name present on neither side.
    - `classify_union` returns `duplicate_name` when the merged name list repeats a value even though the sets match.
    - `classify_union` returns `upstream_deleted` for the presence triple `(True, False, False)` and `fork_deleted` for `(False, True, False)`.
    - `classify_union` returns `not_an_array` when a side's parsed value is a string or an integer, and `unparseable` when a side is flagged undecodable.
    - `tool_names` skips items with no `"name"` key, wraps a bare `dict`, and returns `None` for a non-list non-dict value.
    - `tool_name_list` preserves repeated names so duplicates stay detectable.
    - `derive_both_sides_paths` against a small synthetic `tmp_path` repository (three commits: base, fork-side edit, upstream-side edit) returns the correct merge-base OID and exactly the paths edited on both sides.
  </behavior>

  <action>
Create `tests/unit/test_audit_upstream_merge.py`. Load the module under test with `importlib.util.spec_from_file_location("audit_upstream_merge", Path(__file__).parents[2] / "scripts/audit_upstream_merge.py")` and bind `classify_union`, `tool_names`, `tool_name_list`, and `derive_both_sides_paths` from it — copy the loader block verbatim from `tests/unit/test_sync_baseline_git.py` lines 9-13, since `scripts/` has no `__init__.py`.

Write the cases enumerated in `<behavior>`. Prefer `pytest.mark.parametrize` over one test per verdict so the verdict table stays readable as a single fixture list.

For `derive_both_sides_paths`, build a throwaway repository under `tmp_path` mirroring `tests/unit/test_sync_baseline_git.py`'s `repo()` helper: an initial commit touching `shared.json` and `fork_only.json`, a branch commit editing `shared.json`, and a second branch commit editing `shared.json` and `upstream_only.json`. Assert the returned base OID matches `git merge-base` computed directly and that the returned intersection is exactly `["shared.json"]`. Set deterministic committer identity via `-c user.name=` / `-c user.email=` argv flags so the test does not depend on ambient git config.

Add no pytest marker — this test must be collected by the default `pytest.ini` selection and must not require network, credentials, or the project virtualenv's heavy imports. Format with `ruff format` using the repository settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && .venv/bin/python -m pytest tests/unit/test_audit_upstream_merge.py -q && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py tests/unit/test_sync_baseline_git.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_audit_upstream_merge.py -q` exits 0 with at least 12 tests collected and zero skipped.
    - `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py tests/unit/test_sync_baseline_git.py -q` exits 0 — the new script has not regressed Phase 1 coverage.
    - `.venv/bin/python -m pytest tests/unit/test_audit_upstream_merge.py --collect-only -q` lists a case for each of the nine `classify_union` verdicts.
    - `ruff format --check tests/unit/test_audit_upstream_merge.py` exits 0.
  </acceptance_criteria>

  <done>Every `classify_union` verdict has a passing unit case, `derive_both_sides_paths` is proven against a synthetic repository, and the Phase 1 unit suites remain green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created across Phase 02 (excluded from drift verification — none of these exist before this phase):

**Scripts**
- `scripts/audit_upstream_merge.py` — subcommands `union`, `remerge`, `findings`, `reclass`; functions `derive_both_sides_paths`, `load_json_at`, `tool_names`, `tool_name_list`, `classify_union`, `search_relocated_names`, `sweep_data_json`, `write_staging_artifact`, `create_remerge_stage`, `resolve_data_json_conflict`, `union_default_config_keys`, `full_tree_diff`, `classify_finding`, `recheck_against_pin`, `join_preservation`, `main`; constants `DEFAULT_FORK_OID`, `DEFAULT_UPSTREAM_OID`, `DEFAULT_MERGED_OID`, `DEFAULT_PIN_OID`, `REMERGE_REF`.
- `scripts/probe_custom_tools.py` — functions `probe_tool_python`, `probe_tool_cli`, `run_probe_suite`, `assert_probe_contract`, `main`; constant `PROBE_SAMPLE`.

**Tests**
- `tests/unit/test_audit_upstream_merge.py`
- `tests/unit/test_probe_custom_tools.py`

**Evidence artifacts** (under `.planning/phases/02-upstream-main-integration/evidence/`)
- `staging/union.json`, `staging/remerge.json`, `staging/findings.json`, `staging/preservation-reclass.json`, `staging/probes/*.json`, `staging/SHA256SUMS`
- `<remerge-oid>/` — the published bundle with its own sorted `SHA256SUMS`

**Documents**
- `.planning/phases/02-upstream-main-integration/02-FINDINGS.md`

**Git refs** (never merged, never pushed)
- `refs/audit/remerge` — pins the throwaway re-merge commit so it survives garbage collection
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git object store → audit script | Ref names and file paths flow into `git` subprocess argv |
| repository JSON files → parser | Untrusted-shape tool-definition data is decoded |
| audit script → evidence bundle | Data leaves the process and is committed to a shared branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `load_json_at` / `tool_names` in `scripts/audit_upstream_merge.py` | medium | mitigate | Decode with the standard library JSON module only; no dynamic-evaluation builtin. Non-list and undecodable values return explicit `not_an_array` / `unparseable` verdicts instead of raising or silently passing. |
| T-02-02 | Tampering | git invocation boundary | medium | mitigate | Every git call goes through the imported argv-only `run_git` from `scripts/capture_sync_baseline.py`; no shell string interpolation. Acceptance criterion greps for zero direct `subprocess.` uses. |
| T-02-03 | Information Disclosure | `write_staging_artifact` evidence output | high | mitigate | The bundle records tool names, file paths, counts and verdicts only — never file contents, tool payloads, or credential values. Reuse the `_contains_secret` guard idiom from `scripts/capture_sync_baseline.py` before writing. |
| T-02-04 | Denial of Service | `sweep_data_json` over 213 files × 3 refs | low | accept | Bounded, read-only, single-pass over a fixed file set; measured under 60 seconds during planning. `run_git`'s existing 60s per-call timeout applies. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs zero packages. RESEARCH.md's Package Legitimacy Audit records no researcher-recommended package; the only dependency changes in scope arrived through the upstream project's own release process and are explicitly routed to Phase 5 / COMP-01. No install task exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `python3 scripts/audit_upstream_merge.py union --repo . --json` exits 0 and writes `evidence/staging/union.json`.
2. `shasum -a 256 -c SHA256SUMS` passes from inside `evidence/staging/`.
3. `.venv/bin/python -m pytest tests/unit/test_audit_upstream_merge.py tests/unit/test_registry_integrity.py tests/unit/test_sync_baseline_git.py -q` exits 0.
4. `ruff format --check scripts/audit_upstream_merge.py tests/unit/test_audit_upstream_merge.py` exits 0.
5. `git status --porcelain` shows no change to `.planning/config.json` or `ralph-specs/fleet/results/`.
</verification>

<success_criteria>
- Every `src/tooluniverse/data/*.json` file touched on both sides carries a recorded union verdict — 213 minimum, not the 22 git reported.
- Zero net-removed fork entries, zero net-removed upstream entries, zero duplicate `name` values across the sweep.
- Both upstream-deleted files are accounted for with a non-empty `relocated_to`; `unrelocated_lost_names` is empty.
- The evidence bundle is checksummed and verifies.
- All nine classifier verdicts have passing unit coverage.
</success_criteria>

<output>
Create `.planning/phases/02-upstream-main-integration/02-01-SUMMARY.md` when done.
</output>
