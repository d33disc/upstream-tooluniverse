---
phase: 02-upstream-main-integration
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - scripts/audit_upstream_merge.py
  - .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json
  - .planning/phases/02-upstream-main-integration/evidence/staging/SHA256SUMS
autonomous: true
requirements: [SYNC-02, PRES-02]

estimate:
  tokens: 105000
  raw_tokens: 52500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every conflicted Python adapter/core module resolves under D-08's upstream-canonical-plus-fork-additive rule, with the fork-only definition names retained and the shared definition names taken from upstream both recorded per file (SYNC-02, criterion 2)."
    - "Every conflicted test file resolves the same way, so fork-only test coverage is not silently dropped along with the definitions it guards."
    - "The re-merge commits cleanly inside the stage, its OID is pinned at refs/audit/remerge, and no branch contains it (D-06)."
    - "_lazy_registry_static.py is regenerated by tu build after the whole source tree is settled, never hand-resolved, and its key set is accounted for against the landed version."
    - "tests/unit/test_registry_integrity.py is green inside the stage with the stage's own fresh environment -- no ghost tool-name references and every JSON type maps to a live registry class (PRES-02)."
  artifacts:
    - scripts/audit_upstream_merge.py
    - .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json
  key_links:
    - "This plan resumes the merge-in-progress stage plan 02-02 left behind: MERGE_HEAD present, data/config layer staged, source layer still listed by git diff --diff-filter=U. Never abort that merge."
    - "tu build (src/tooluniverse/cli.py cmd_build, wrapping tooluniverse.generate_lazy_registry.main()) runs LAST, after every source conflict is settled, because AST discovery over a partially resolved tree produces a registry that reflects an inconsistent intermediate state."
    - "tests/unit/test_registry_integrity.py is the acceptance gate for the regenerated registry, not a line diff -- the file is generated, so its correct content is whatever discovery produces from a correct source tree."
  prohibitions:
    - "MUST NOT run git merge --abort in the stage; plan 02-02's merge-in-progress state is the handoff."
    - "MUST NOT hand-resolve conflict markers inside src/tooluniverse/_lazy_registry_static.py; regenerate it with tu build."
    - "MUST NOT run tu build before every source-layer conflict is resolved."
    - "MUST NOT merge, rebase, cherry-pick, or fast-forward the stage into docs/gsd-codebase-map or any other branch (D-06)."
    - "MUST NOT rewrite history atop e0755067 or force-push any ref."
    - "MUST NOT push any ref to the upstream remote (mims-harvard/ToolUniverse)."
    - "MUST NOT run uv sync, tu build, or pytest against the main checkout's .venv; every environment operation targets the stage."
    - "MUST NOT run git checkout, git switch, git stash, or git reset in the main checkout -- concurrent sessions share it."
    - "MUST NOT modify, stage, or delete .planning/config.json or ralph-specs/fleet/results/."
    - "MUST NOT regenerate uv.lock unless the merge resolution itself forces it."
    - "MUST NOT open a criterion-2 finding for a pyproject.toml dependency-version difference; that routes to Phase 5 / COMP-01."
---

<objective>
Finish the re-derived merge. Resolve the source layer -- the 11 conflicted Python adapter and core modules plus the 5 conflicted test files -- under D-08's upstream-canonical-plus-fork-additive rule, commit the stage, pin it, then regenerate the lazy registry and prove the registration chain holds.

Purpose: this is the judgment-heavy half of the re-merge and the half where a fork-only capability can actually be lost. The data/config layer plan `02-02` resolved is set arithmetic; a source-module conflict is a decision about whether a fork function body is a divergent copy of an upstream definition (upstream wins) or a fork-only addition (retained). Getting that wrong is exactly the failure mode criterion 2 exists to catch, so every decision is recorded per file rather than left implicit in the resulting blob.

Output: a committed re-merge stage pinned at `refs/audit/remerge`, a completed `remerge.json` resolution table, a regenerated `_lazy_registry_static.py`, and a green registry-integrity run inside the stage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-upstream-main-integration/02-CONTEXT.md
@.planning/phases/02-upstream-main-integration/02-RESEARCH.md
@.planning/phases/02-upstream-main-integration/02-PATTERNS.md
@.planning/phases/02-upstream-main-integration/02-02-SUMMARY.md
</context>

<flagged_assumptions>
Restated so this plan is self-contained. None silently dropped.

| # | Assumption | Status |
|---|---|---|
| EC-SYNC-01 | Edge coverage `unclassified` / `unresolved`; no backstop authored, and none may be auto-added. | UNRESOLVED (flagged) -- plan 02-02 Task 1 owns the SYNC-01 edge evidence. |
| EC-SYNC-02 | Edge coverage `unclassified` / `unresolved`. | UNRESOLVED (flagged) -- this plan's per-file resolution record completes the SYNC-02 edge evidence begun by plans 02-01 and 02-02. |
| EC-PRES-02 | Edge coverage `unclassified` / `unresolved`. | UNRESOLVED (flagged) -- Task 3's registry-integrity gate is one strand; plans 02-04 and 02-05 provide the rest. |
| A1 | D-08 does not explicitly classify JSON tool-definition arrays as "structural files". | RESOLVED ON EVIDENCE in plan 02-01; no effect on the source layer. |
| OQ1 | `pyproject.toml` in D-07 full-tree scope. | DECIDED -- resolved in plan 02-02, routed to Phase 5 / COMP-01, never a criterion-2 finding here. |
| OQ2 | Fresh `uv sync` in the stage vs. reusing the main `.venv`. | DECIDED -- fresh `uv sync` inside the stage, in Task 3 of this plan, the first task in the phase that actually needs a Python environment. |
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the 11 conflicted Python adapter and core modules</name>

  <read_first>
    - `.planning/phases/02-upstream-main-integration/02-CONTEXT.md` section Decisions D-07, D-08 -- the resolution rule applied literally.
    - `.planning/phases/02-upstream-main-integration/evidence/staging/remerge.json` -- `stage_path`, `handoff_state`, `unresolved_paths`, and the resolution records already written by plan 02-02.
    - `scripts/audit_upstream_merge.py` -- `run_git`, `load_json_at`, and the `remerge` subcommand's existing `--layer` dispatch.
    - Inside the stage, for each conflicted module: `git -C <stage> show :2:<path>` (the fork side) and `git -C <stage> show :3:<path>` (the upstream side), plus the working-tree file carrying the conflict markers. Read all three before resolving any hunk -- the marker file alone does not show which side a hunk came from.
    - `.planning/codebase/ARCHITECTURE.md` -- the shared-core execution path, so a change to `execute_function.py`'s collaborators is weighed against how every transport reaches them.
  </read_first>

  <files>scripts/audit_upstream_merge.py</files>

  <action>
Resume the merge-in-progress stage plan `02-02` left behind. First assert the handoff: `MERGE_HEAD` exists in the stage's git dir, `remerge.json`'s `handoff_state` is `merge_in_progress`, and `git -C <stage> diff --name-only --diff-filter=U` still lists source-layer paths. If `MERGE_HEAD` is absent, do not improvise -- re-run `python3 scripts/audit_upstream_merge.py remerge --repo . --layer data-config --json` from a fresh stage, which is idempotent, then proceed.

Resolve these eleven modules under rule `upstream_canonical_fork_additive`:
`src/tooluniverse/agentic_tool.py`, `base_tool.py`, `brenda_tool.py`, `cli.py`, `llm_clients.py`, `sabdab_tool.py`, `smcp.py`, `therasabdab_tool.py`, `tool_discovery_tools.py`, `tool_finder_embedding.py`, `unified_guideline_tools.py`.

Per file, work definition by definition rather than hunk by hunk. Extract the top-level and class-level definition names from both sides with `ast.parse` over `git show :2:<path>` and `git show :3:<path>` -- classes, functions, and module-level assignments. Then:
- A definition name present on **both** sides takes upstream's body outright. This is D-08's shared-definition branch and it is the expected, correct outcome even when the fork's version looked better; a disagreement is recorded, not silently preserved.
- A definition name present **only on the fork side** is retained verbatim. A fork-only definition that ends up absent from the resolved file is a finding, not a resolution.
- A definition name present only on the upstream side is taken as-is.

Record per file into `remerge.json`'s `resolutions` array: `path`, `rule: "upstream_canonical_fork_additive"`, `decision`, `rationale`, `fork_only_retained` (sorted definition names), `shared_taken_from_upstream` (sorted), `upstream_only_added` (sorted), and `fork_only_dropped` -- which must be empty, and any entry in it is carried into plan `02-04`'s `findings.json` as a finding candidate.

After resolving each file, parse the result with `ast.parse` to confirm it is syntactically valid and that its definition-name set equals `fork_only_retained | shared_taken_from_upstream | upstream_only_added`. A file that still contains conflict markers will fail that parse, which is the point. Stage each resolved file with an explicit `git -C <stage> add <path>` -- never `git add -A` while other conflicts remain open.

Run `ruff format` and `ruff check` on the eleven resolved files using the **stage's** `pyproject.toml` settings, not a global config.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && STAGE="$(jq -r '.stage_path' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json)" && test -z "$(git -C "$STAGE" diff --name-only --diff-filter=U -- 'src/tooluniverse/*.py' ':!src/tooluniverse/_lazy_registry_static.py')" && jq -e '[.resolutions[] | select(.rule == "upstream_canonical_fork_additive") | select(.path | startswith("src/tooluniverse/"))] | length == 11 and (map((.fork_only_dropped | length) == 0) | all) and (map(.rationale != null and .decision != null) | all)' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json && (cd "$STAGE" && python3 -c 'import ast,sys;[ast.parse(open(p).read()) for p in sys.argv[1:]]' src/tooluniverse/agentic_tool.py src/tooluniverse/base_tool.py src/tooluniverse/brenda_tool.py src/tooluniverse/cli.py src/tooluniverse/llm_clients.py src/tooluniverse/sabdab_tool.py src/tooluniverse/smcp.py src/tooluniverse/therasabdab_tool.py src/tooluniverse/tool_discovery_tools.py src/tooluniverse/tool_finder_embedding.py src/tooluniverse/unified_guideline_tools.py)</automated>
  </verify>

  <acceptance_criteria>
    - `git -C <stage> diff --name-only --diff-filter=U -- 'src/tooluniverse/*.py' ':!src/tooluniverse/_lazy_registry_static.py'` produces no output.
    - `jq -r '[.resolutions[] | select(.rule == "upstream_canonical_fork_additive") | select(.path | startswith("src/tooluniverse/"))] | length' remerge.json` equals 11.
    - Every one of those 11 records has non-null `decision` and `rationale`, and arrays `fork_only_retained`, `shared_taken_from_upstream`, `upstream_only_added`, `fork_only_dropped`.
    - `jq -e '[.resolutions[] | select(.rule == "upstream_canonical_fork_additive")] | map((.fork_only_dropped | length) == 0) | all' remerge.json` is true; any non-empty `fork_only_dropped` is carried into `02-04`'s `findings.json` and this criterion is satisfied only by that carry-forward existing.
    - All 11 files parse with `ast.parse` inside the stage -- no residual conflict markers.
    - `ruff check` and `ruff format --check` on the 11 files, run from the stage, both exit 0.
    - `git -C <stage> diff --name-only --diff-filter=U` still lists the test files and `_lazy_registry_static.py`, confirming Task 2's work is intact and untouched.
    - `git rev-parse --abbrev-ref HEAD` in the main checkout still reports `docs/gsd-codebase-map`.
  </acceptance_criteria>

  <reversibility rating="reversible">All edits are inside the throwaway stage worktree; `git worktree remove --force` erases them with no effect on any branch.</reversibility>

  <done>All 11 conflicted Python modules are resolved definition by definition under a recorded D-08 rule, each parses cleanly, and no fork-only definition was dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the conflicted test files, commit the stage, and pin it</name>

  <read_first>
    - `.planning/phases/02-upstream-main-integration/evidence/staging/remerge.json` -- the resolution table so far and `stage_path`.
    - Inside the stage, for each conflicted test file: `git -C <stage> show :2:<path>` and `git -C <stage> show :3:<path>`.
    - `tests/unit/test_registry_integrity.py` -- one of the conflicted files and also the phase's acceptance gate; a fork-only assertion dropped here would silently weaken every later check.
    - `pytest.ini` -- the default selection, so a resolution does not accidentally move a test outside it.
    - `.planning/phases/02-upstream-main-integration/02-CONTEXT.md` D-06, D-06b -- the throwaway-instrument posture and where corrective commits eventually land.
  </read_first>

  <files>scripts/audit_upstream_merge.py</files>

  <action>
Resolve these five conflicted test files under the same `upstream_canonical_fork_additive` rule, applied to test functions and fixtures rather than production definitions:
`tests/integration/test_compose_tool.py`, `tests/integration/test_tool_integration.py`, `tests/tools/test_brenda_tool.py`, `tests/unit/test_registry_integrity.py`, `tests/unit/test_tool_composition.py`.

A test function present on both sides takes upstream's body; a fork-only test function or fixture is retained. Pay particular attention to `tests/unit/test_registry_integrity.py`: it is simultaneously a merge input and the acceptance gate for Tasks 1 and 3, so a fork-only assertion silently dropped here would weaken every downstream check in the phase. Record its `fork_only_retained` list explicitly in the summary, not just in the JSON.

Record per file into `remerge.json`'s `resolutions` array with the same field set as Task 1. Stage each with an explicit `git -C <stage> add <path>`.

With every path except `_lazy_registry_static.py` now resolved, clear that file's conflict without hand-editing it -- take either side wholesale to satisfy git, since Task 3 overwrites it entirely -- and confirm its resolution record still reads `decision: deferred_to_regeneration`.

Commit inside the stage: `git -C <stage> commit --no-edit -m "audit: re-derived merge of 56adcfd9 into e0755067 (review instrument, never merged)"`. Capture the resulting OID as `stage_merge_oid` and pin it against the **main** repository with `git update-ref refs/audit/remerge <oid>` so garbage collection cannot reclaim it. Do not create a branch, do not check anything out in the main checkout, and do not merge this commit anywhere. Set `handoff_state` to `merged_pending_regeneration` and clear `unresolved_paths`.

Write the updated record via `write_staging_artifact`, regenerating the sorted `SHA256SUMS`.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && STAGE="$(jq -r '.stage_path' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json)" && test -z "$(git -C "$STAGE" status --porcelain)" && git rev-parse refs/audit/remerge && test "$(git rev-parse refs/audit/remerge)" = "$(jq -r '.stage_merge_oid' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json)" && test -z "$(git branch --contains refs/audit/remerge)" && jq -e '.handoff_state == "merged_pending_regeneration" and (.unresolved_paths | length) == 0 and ([.resolutions[] | select(.path | startswith("tests/"))] | length == 5)' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json</automated>
  </verify>

  <acceptance_criteria>
    - `git -C <stage> status --porcelain` produces no output -- the merge is fully resolved and committed.
    - `git rev-parse refs/audit/remerge` prints a 40-character OID equal to `jq -r '.stage_merge_oid' remerge.json`.
    - `git branch --contains refs/audit/remerge` produces no output -- the stage is unmerged.
    - `git rev-list --parents -n 1 refs/audit/remerge` lists exactly two parents, `e0755067ebe7cc5374f033c5c28160980c5eddfa` and `56adcfd9c299078d0c40fde642b0be006510ccf3`, matching `f81448f2`'s parentage.
    - `jq -r '[.resolutions[] | select(.path | startswith("tests/"))] | length' remerge.json` equals 5, each with the full Task 1 field set.
    - `jq -r '[.resolutions[] | select(.path == "tests/unit/test_registry_integrity.py")][0].fork_only_retained | length' remerge.json` is recorded and repeated in the plan summary.
    - `jq -r '.handoff_state' remerge.json` equals `merged_pending_regeneration` and `.unresolved_paths` is empty.
    - `jq -r '[.resolutions[] | select(.path == "src/tooluniverse/_lazy_registry_static.py")][0].decision' remerge.json` equals `deferred_to_regeneration`.
    - `git rev-parse --abbrev-ref HEAD` in the main checkout still reports `docs/gsd-codebase-map`, and `git status --porcelain` still lists only `.planning/` work and `ralph-specs/fleet/results/`.
  </acceptance_criteria>

  <reversibility rating="reversible">`git update-ref -d refs/audit/remerge` and `git worktree remove --force <stage>` erase both the commit and the worktree; the ref lives outside `refs/heads/` so no branch operation can pick it up.</reversibility>

  <done>All five conflicted test files are resolved under a recorded rule, the stage commits cleanly with the same two parents as the landed merge, and its OID is pinned outside `refs/heads/` with no branch containing it.</done>
</task>

<task type="auto">
  <name>Task 3: Sync a fresh environment, regenerate the lazy registry, and prove the registration chain</name>

  <read_first>
    - `src/tooluniverse/_lazy_registry_static.py` lines 1-11 -- the generated-file header that forbids hand-resolution.
    - `src/tooluniverse/cli.py` around `cmd_build` (line ~1705) -- what `tu build` step 1 invokes (`tooluniverse.generate_lazy_registry.main()`) and step 2 (coding-API wrappers).
    - `src/tooluniverse/generate_lazy_registry.py` -- the AST discovery that produces `STATIC_LAZY_REGISTRY`.
    - `tests/unit/test_registry_integrity.py` -- the ghost-reference and `type`-to-class assertions that gate this task.
    - `pytest.ini` -- the default selection this run must respect.
    - `uv.lock` and `pyproject.toml` in the stage -- the environment contract for the fresh sync.
  </read_first>

  <files>scripts/audit_upstream_merge.py</files>

  <action>
Inside the re-merge stage only -- never in the main checkout -- bring up a fresh Python environment and regenerate the generated registry, then prove the registration chain.

Run `uv sync` with the stage as the working directory so it creates its own `.venv` there. Per OQ2 this is a deliberate fresh sync rather than reuse of the main checkout's `.venv`: an audit exercise on a throwaway stage must not be able to mutate the working environment. If `uv sync` fails to resolve, record the failure verbatim in `remerge.json` under `env_sync` and mark `routed_to: "Phase 5 / COMP-01"` -- a resolution failure does not block criteria 1 through 4, which are about code content, and Phase 5 owns install certification.

With the environment up, regenerate `src/tooluniverse/_lazy_registry_static.py` by running `tu build` from the stage's environment (equivalently `<stage>/.venv/bin/python -m tooluniverse.generate_lazy_registry` for step 1 alone). This must run **after** every source-layer conflict is settled by Tasks 1 and 2 -- regenerating against a partially resolved tree produces a registry reflecting an inconsistent intermediate state, which is threat T-02-06.

Record into `remerge.json` under `lazy_registry`:
- `regenerated_key_count` -- number of entries in the regenerated `STATIC_LAZY_REGISTRY`.
- `landed_key_count` -- number of entries in `git show f81448f2:src/tooluniverse/_lazy_registry_static.py`, parsed with `ast.literal_eval` over the extracted assignment node.
- `missing_vs_landed` -- sorted names present in the landed version but absent from the regenerated one.
- `added_vs_landed` -- sorted names present in the regenerated version but absent from the landed one.
- `module_target_diffs` -- names whose module-name string value differs between the two.

**A non-empty `missing_vs_landed` is expected information, not a failure.** The regenerated registry comes from AST discovery over the re-merged source tree; `f81448f2`'s copy reflects whatever build state existed then. Every name in it is a criterion-2 finding candidate -- either a tool class failed AST discovery or a source file was lost in resolution -- and is carried into plan `02-04`'s `findings.json` for classification, where the D-06a pin recheck decides whether it matters. Do not hand-edit the generated file to make the list empty; that would forge the evidence this phase exists to produce.

Then run `<stage>/.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q` with the stage as the working directory and record its exit code and summary line under `registry_integrity`. This test already collects every defined tool `name` from `data/*.json` and every referenced name from configs, skills, and rules, and asserts no ghost references plus a live `type`-to-class mapping -- it is the mechanical PRES-02 and SYNC-02 gate, so do not write a parallel checker. **This one is a hard gate:** a red run means the resolved tree is wrong, not merely different.

Amend the stage commit to include the regenerated registry with `git -C <stage> add -A` followed by `git -C <stage> commit --amend --no-edit`, re-point `refs/audit/remerge` to the amended OID, update `stage_merge_oid`, set `handoff_state` to `merged_complete`, and regenerate the sorted `SHA256SUMS`.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && STAGE="$(jq -r '.stage_path' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json)" && (cd "$STAGE" && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q) && jq -e '.registry_integrity.exit_code == 0 and (.lazy_registry.regenerated_key_count > 0) and (.lazy_registry | has("missing_vs_landed")) and (.lazy_registry | has("added_vs_landed")) and (.lazy_registry | has("module_target_diffs")) and .handoff_state == "merged_complete"' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json && test "$(git rev-parse refs/audit/remerge)" = "$(jq -r '.stage_merge_oid' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json)" && test -z "$(git branch --contains refs/audit/remerge)" && (cd .planning/phases/02-upstream-main-integration/evidence/staging && shasum -a 256 -c SHA256SUMS)</automated>
  </verify>

  <acceptance_criteria>
    - `<stage>/.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q` exits 0, and `jq -r '.registry_integrity.exit_code' remerge.json` equals 0. This is a hard gate.
    - `jq -r '.lazy_registry.regenerated_key_count' remerge.json` is greater than 0.
    - `remerge.json` has all four of `lazy_registry.missing_vs_landed`, `.added_vs_landed`, `.module_target_diffs`, and `.landed_key_count` present as arrays or numbers. Their contents are recorded, not gated -- a non-empty `missing_vs_landed` is carried into plan `02-04`'s `findings.json` and the plan summary names each entry.
    - `git show refs/audit/remerge:src/tooluniverse/_lazy_registry_static.py | head -4` contains the generated-file header, confirming regeneration rather than hand-editing.
    - `git rev-parse refs/audit/remerge` equals `jq -r '.stage_merge_oid' remerge.json`, and `git branch --contains refs/audit/remerge` produces no output.
    - `jq -r '.handoff_state' remerge.json` equals `merged_complete`.
    - `shasum -a 256 -c SHA256SUMS` from inside `evidence/staging/` exits 0.
    - The main checkout's `.venv` is unchanged: `ls /Users/davis/code/ToolUniverse/.venv/bin/python` resolves and `git -C /Users/davis/code/ToolUniverse status --porcelain` shows no source or environment change.
  </acceptance_criteria>

  <reversibility rating="reversible">The environment and regeneration exist only inside the throwaway stage; removing the worktree and the audit ref erases them.</reversibility>

  <done>The stage has its own synced environment, carries a regenerated `_lazy_registry_static.py`, records its full key-set delta against the landed version without gating on it, and passes the registry-integrity gate.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
See `02-01-PLAN.md` section "Artifacts this phase produces" for the complete phase-wide list. Symbols first created by **this** plan:

- `scripts/audit_upstream_merge.py` -- `resolve_source_module_conflict`, `extract_definition_names`, `REMERGE_REF`; `remerge --layer source` dispatch.
- Git ref `refs/audit/remerge` (never merged, never pushed)
- The stage's own `.venv` and its regenerated `src/tooluniverse/_lazy_registry_static.py`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream source -> fork source | Upstream definition bodies replace fork ones under D-08 |
| stage -> `uv sync` / `tu build` | Third-party dependency resolution and code generation execute against merged source |
| stage -> main repository refs | Only `refs/audit/remerge` and the evidence JSON cross back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | merge policy reverting a fork-only security fix | high | mitigate | D-08's "upstream wins for shared definitions" can revert a fork-only hardening change. Task 1 records per module the fork-only definitions retained and the shared definitions taken from upstream, so a reverted fork fix is visible in the artifact and surfaces in plan `02-04` as `landed_dropped_or_altered`. |
| T-02-06 | Tampering / Denial of Service | `tu build` regeneration timing | high | mitigate | Regeneration runs only in Task 3, after Tasks 1 and 2 settle every source conflict. `tests/unit/test_registry_integrity.py` is a hard gate before the tree becomes a comparison point -- a registry built against a partial tree silently degrades tools to per-tool unavailability rather than crashing. |
| T-02-27 | Tampering | forging an empty `missing_vs_landed` by hand-editing the generated registry | high | mitigate | The generated file is regenerated wholesale and never hand-edited; the acceptance criteria record the delta rather than gating on it, removing the incentive to make the list empty. `git show refs/audit/remerge:...` is checked for the generated-file header. |
| T-02-28 | Tampering | residual conflict markers surviving into a resolved module | high | mitigate | Every resolved file is re-parsed with `ast.parse` and its definition-name set is compared against the recorded union; a file with markers fails to parse, and a dropped definition fails the set comparison. |
| T-02-08 | Elevation of Privilege | `uv sync` executing upstream-changed dependency resolution | medium | accept | Dependencies arrived through the upstream project's own release process, not a researcher recommendation; RESEARCH.md's Package Legitimacy Audit records zero `[ASSUMED]`/`[SUS]` packages. The sync runs in a throwaway stage with its own `.venv`. Install certification is Phase 5 / COMP-01. |
| T-02-09 | Repudiation | throwaway stage silently merged or pushed | high | mitigate | Acceptance criteria assert `git branch --contains refs/audit/remerge` produces no output and that the commit has exactly the same two parents as `f81448f2`. The ref lives outside `refs/heads/`. No push to any remote occurs. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | `uv sync` installs only what the committed `uv.lock` and upstream's `pyproject.toml` already declare; no new package is introduced. No legitimacy checkpoint is required. |
</threat_model>

<verification>
1. All 11 Python modules and 5 test files resolved, each parsing cleanly with no residual markers.
2. Per-file records name the fork-only definitions retained and the shared definitions taken from upstream; `fork_only_dropped` is empty or carried forward as a finding.
3. The stage commits with the same two parents as `f81448f2`, pinned outside `refs/heads/`, contained by no branch.
4. `_lazy_registry_static.py` regenerated, its full key-set delta recorded.
5. `tests/unit/test_registry_integrity.py` green inside the stage -- hard gate.
6. Main checkout branch, status, and `.venv` unchanged.
7. `evidence/staging/SHA256SUMS` verifies.
</verification>

<success_criteria>
- Criterion 2 (SYNC-02) has complete re-derived evidence: every conflicted definition across both layers resolved by an explicitly recorded rule, with no fork-only definition dropped unrecorded.
- The registration chain (PRES-02) is proven intact at catalog level inside the stage.
- The stage is a committed, pinned, unmerged, unpushed review instrument ready for plans `02-04` and `02-05`.
</success_criteria>

<output>
Create `.planning/phases/02-upstream-main-integration/02-03-SUMMARY.md` when done.
</output>
