---
phase: 02-upstream-main-integration
plan: 05
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - scripts/probe_custom_tools.py
  - tests/unit/test_probe_custom_tools.py
  - .planning/phases/02-upstream-main-integration/evidence/staging/probes/summary.json
  - .planning/phases/02-upstream-main-integration/evidence/staging/probes/symlinks.json
  - .planning/phases/02-upstream-main-integration/evidence/staging/probes/SHA256SUMS
autonomous: true
requirements: [PRES-02]

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A recorded, justified sample of preservation-flagged fork-only tools loads and executes inside the re-merge stage, passing on its own terms rather than being diffed against any Phase 1 probe JSON (D-04, criterion 4)."
    - "Each probed tool completes the discover -> inspect -> execute workflow through ToolUniverse.run_one_function(), the shared core every transport converges on."
    - "A credential-gated tool is recorded as gated with its missing key names, not as a failure and not as a pass -- the probe distinguishes 'the registration chain works' from 'the remote service answered'."
    - "The probe sample selection and its justification are written into the evidence artifact, so a reviewer can see which preservation-flagged assets were covered and which were not."
  artifacts:
    - scripts/probe_custom_tools.py
    - tests/unit/test_probe_custom_tools.py
    - .planning/phases/02-upstream-main-integration/evidence/staging/probes/summary.json
    - .planning/phases/02-upstream-main-integration/evidence/staging/probes/SHA256SUMS
  key_links:
    - "This plan owns evidence/staging/probes/ exclusively and writes a probes-scoped SHA256SUMS; the staging-root SHA256SUMS belongs to plan 02-04 in the same wave, and plan 02-06 regenerates one bundle-wide checksum set at publication."
    - "probe_tool_python() calls ToolUniverse.run_one_function() from src/tooluniverse/execute_function.py -- the one execution path all five transports route through, so a per-transport shortcut would prove less."
    - "The probe runs inside the re-merge stage worktree built by plans 02-02 and 02-03, using the stage's own .venv created in plan 02-03 Task 3, never the main checkout's environment."
    - "PROBE_SAMPLE is a module-level recorded list keyed to preservation.json classes, replacing capture_sync_baseline.py's single hardcoded REFERENCE_TOOL."
  prohibitions:
    - "MUST NOT diff any probe result against Phase 1's probes/*.json, catalog.json, or tests/*.json -- D-03 excludes them from the binding surface and D-04 requires these probes pass on their own terms."
    - "MUST NOT certify discovery/inspection/execution across Python, CLI, MCP stdio, MCP HTTP, and REST -- that is Phase 5 / SURF-01. This plan probes targeted tools through Python plus the tu CLI only."
    - "MUST NOT treat a missing API key as a probe failure; record it as gated with the missing key names."
    - "MUST NOT write API key values, credential material, or full tool response payloads into the evidence bundle."
    - "MUST NOT run the probes against the main checkout's .venv or mutate it."
    - "MUST NOT modify, stage, or delete .planning/config.json or ralph-specs/fleet/results/."
    - "MUST NOT merge, push, or delete refs/audit/remerge."
    - "MUST NOT add a network, slow, or require_api_keys marked test to the default pytest selection."
---

<objective>
Prove criterion 4: representative preserved custom tools still load and execute after the upstream-main integration. Per D-04 these are **fresh probes that are not diffed against Phase 1's probe JSONs** -- they must pass on their own terms, targeting the assets `preservation.json` actually flags rather than Phase 1's broader representative sample.

Purpose: the union sweep and the findings classification prove content survived. Neither proves the registration chain still *runs*. A tool definition can be present, indexed in `default_config.py`, and listed in the regenerated lazy registry, yet fail at first invocation on a typo'd module string -- lazy loading defers that failure past catalog load, so only execution catches it.

Output: `scripts/probe_custom_tools.py`, unit coverage for its pure contract logic, and a `probes/summary.json` recording the sample, its justification, and a per-tool result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-upstream-main-integration/02-CONTEXT.md
@.planning/phases/02-upstream-main-integration/02-RESEARCH.md
@.planning/phases/02-upstream-main-integration/02-PATTERNS.md
@.planning/phases/02-upstream-main-integration/02-03-SUMMARY.md
@docs/dev_docs/Interaction_Surfaces.md
</context>

<flagged_assumptions>
Restated so this plan is self-contained. None silently dropped.

| # | Assumption | Status |
|---|---|---|
| EC-SYNC-01 | Edge coverage `unclassified` / `unresolved`; no backstop authored. | UNRESOLVED (flagged) -- plan 02-02 Task 1 owns it. |
| EC-SYNC-02 | Edge coverage `unclassified` / `unresolved`. | UNRESOLVED (flagged) -- plans 02-01, 02-03, and 02-04 own it. |
| EC-PRES-02 | Edge coverage `unclassified` / `unresolved`. | UNRESOLVED (flagged) -- this plan's probe suite plus plan 02-04's preservation join are the PRES-02 edge evidence. |
| A2 (RESEARCH.md, Claude's Discretion) | `preservation.json` has no `at_risk` boolean field; the only path-record keys are `must_survive`, `new_mode`, `status`, `old_mode`, `symlink`, `path`, `new_oid`, `class`, `old_oid`. So "at-risk" must be a planner-chosen, recorded selection rule. | RESOLVED HERE -- the sample and its rule are fixed in Task 1 and written into the artifact. |
| OQ2 | Fresh `uv sync` in the stage vs. reusing the main `.venv`. | DECIDED -- the probes run against the stage's own `.venv` created in plan 02-03 Task 3. |
</flagged_assumptions>

<probe_sample_selection>
Claude's Discretion per CONTEXT.md, resolved here and recorded so it is reviewable. Selection rule, applied in this order:

1. **Fork-only tool entries inside a hand-resolved conflicted JSON array** -- the single highest-risk class, because Git's line merge could have dropped them without a conflict marker. Measured during planning: `uspto_tools.json` carries exactly two such names, present at `e0755067`, absent at `56adcfd9`, and surviving at both `f81448f2` and the pin.
2. **Tools whose implementing module is classified `tool_definition` in `preservation.json`** -- only four paths carry that class, and three are modified fork implementations of the discovery/finder layer.
3. **One offline control with no credential requirement**, so a total probe failure can be distinguished from an environment problem.

| Tool | Why in sample | Preservation linkage | Credential expectation |
|---|---|---|---|
| `USPTO_get_patent_assignment` | Fork-only entry in the hand-resolved `data/uspto_tools.json` array; type `USPTOOpenDataPortalTool`; required parameter `applicationNumberText` | rule 1 | gated on `USPTO_API_KEY`; gated result is a pass for registration-chain purposes |
| `USPTO_get_patent_transactions` | Second fork-only entry in the same hand-resolved array | rule 1 | same |
| `Tool_Finder_Keyword` | Implemented by `src/tooluniverse/tool_finder_keyword.py`, `class: tool_definition`, `status: M` | rule 2 | none -- offline BM25 |
| `Tool_Finder_LLM` | Implemented by `src/tooluniverse/tool_finder_llm.py`, `class: tool_definition`, `status: M` | rule 2 | LLM backend may be gated; schema load must still succeed |
| `Tool_RAG` | Defined in `data/finder_tools.json`; exercises the discovery layer that `src/tooluniverse/tool_discovery_tools.py` (`class: tool_definition`, `status: M`, and one of the 22 hand-resolved files) implements | rule 2 | embedding assets may be absent; gated result acceptable |
| `DegreesOfUnsaturation_calculate` | Phase 1's reference tool; fully offline, deterministic, no credentials | rule 3 (control) | none -- a failure here means the environment, not the merge |

**Not covered, and deliberately so:** the 512 `custom_code` and 240 `plugin_asset` preservation entries are covered by plan 02-04's path-level disposition, not by execution probes; full cross-surface certification (MCP stdio, MCP HTTP, REST) is Phase 5 / SURF-01; catalog-wide and test-level regression certification is Phase 5 / TEST-01. Record these exclusions in the artifact so the coverage boundary is explicit rather than implied.
</probe_sample_selection>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterized custom-tool probe harness running inside the re-merge stage</name>

  <read_first>
    - `scripts/capture_sync_baseline.py` -- `REFERENCE_TOOL` / `REFERENCE_ARGUMENTS` (lines 65-66), `_stage` (~69), `_probe_contract` (~85), `_error_stage` (~106), `_assert_reference` (~117), `run_python_probe` (~134), `_run_command` (~175), `run_cli_probe` (~187), `normalize_probe_result` (~1037), `validate_probe_invariants` (~1109). These are single-fixed-tool probes to generalize, not to rewrite.
    - `.planning/phases/02-upstream-main-integration/02-PATTERNS.md` section 3 -- the four specific changes needed to turn those into a parameterized harness.
    - `src/tooluniverse/execute_function.py` -- the `ToolUniverse` class, `load_tools`, `find_tools_by_pattern`, `tool_specification`, `run_one_function`, `close`, and the `_excluded_api_key_tools` mapping that records credential gating.
    - `src/tooluniverse/data/uspto_tools.json` -- the two fork-only entries and their required parameters.
    - `src/tooluniverse/data/finder_tools.json` -- `Tool_RAG`, `Tool_Finder`, `Tool_Finder_LLM`, `Tool_Finder_Keyword`.
    - `docs/dev_docs/Interaction_Surfaces.md` section 2 -- the discover -> inspect -> execute workflow the probes must exercise.
    - `.planning/phases/02-upstream-main-integration/evidence/staging/remerge.json` -- `stage_path` and `stage_merge_oid`.
  </read_first>

  <files>scripts/probe_custom_tools.py</files>

  <behavior>
    - `assert_probe_contract(stages)` returns `pass` when discover found the tool, inspect returned a specification with a `parameter` or `parameters` object, and execute returned without an unhandled exception.
    - It returns `gated` when inspect succeeded but execute failed with a missing-credential condition, carrying the missing key names.
    - It returns `fail` when discover found nothing, or inspect returned no specification, or execute raised an import or attribute error -- the exact signature of a broken lazy-registry module string.
    - It returns `fail`, not `gated`, when execute returns an empty result with no gating signal, so an empty-as-success outcome cannot masquerade as a pass.
    - `probe_tool_python(tool_name, arguments)` never leaves a `ToolUniverse` instance open -- `close` runs in a `finally` block.
  </behavior>

  <action>
Create `scripts/probe_custom_tools.py` as a standalone CLI script (no `scripts/__init__.py`; keep it directly runnable). Load `_stage`, `_probe_contract`, `_error_stage`, `_run_command`, `normalize_probe_result`, and `validate_probe_invariants` from `scripts/capture_sync_baseline.py` via `importlib.util.spec_from_file_location`, the same idiom `tests/unit/test_sync_baseline_git.py` uses. Reuse the existing result-shape helpers rather than inventing a second probe contract.

Define `PROBE_SAMPLE` as a module-level list of records, one per tool in this plan's `<probe_sample_selection>` table, each carrying `name`, `arguments`, `selection_rule`, `preservation_linkage`, and `credential_expectation`. This replaces `capture_sync_baseline.py`'s module-level `REFERENCE_TOOL` / `REFERENCE_ARGUMENTS` constants, which are baked into the calculator-specific `_assert_reference` and cannot serve a heterogeneous tool list.

Implement:
- `probe_tool_python(tool_name, arguments) -> dict` -- parameterized from `run_python_probe`. Instantiate `ToolUniverse`, `load_tools(include_tools=[tool_name])`, `find_tools_by_pattern(tool_name, search_in="name", case_sensitive=True)` for discover, `tool_specification(tool_name)` for inspect, `run_one_function({"name": tool_name, "arguments": arguments})` for execute. Close the instance in a `finally` block. Before concluding, read `universe._excluded_api_key_tools` to detect whether the tool was excluded for missing credentials and carry the missing key names into the result.
- `probe_tool_cli(tool_name, arguments) -> dict` -- parameterized from `run_cli_probe`, reusing its invocation shapes verbatim: `["tu", "grep", tool_name, "--json", "--field", "name", "--limit", "5"]`, `["tu", "info", tool_name, "--json"]`, `["tu", "run", tool_name, json.dumps(arguments), "--json"]`. Resolve `tu` from the stage's own `.venv/bin` rather than PATH so the main checkout's installation cannot answer for the stage.
- `assert_probe_contract(stages) -> dict` -- the generic replacement for `_assert_reference`, per `<behavior>`. It must be a pure function over the stage dicts so it is unit-testable with no ToolUniverse import. Explicitly reject the empty-as-success pattern: an execute stage returning an empty list or empty dict with no gating signal is `fail`, not `pass`.
- `run_probe_suite(stage_path, sample, out_dir) -> dict` -- runs both probe flavors per sample entry with the stage as working directory and the stage's `.venv` as the interpreter, writing one `probes/<tool>.json` per tool plus a `probes/summary.json`. The summary carries `stage_path`, `stage_merge_oid`, `sample` (the full `PROBE_SAMPLE` records including selection rules and linkage, so the selection is recorded per CONTEXT.md's Claude's Discretion requirement), `exclusions` (the deliberately-not-covered list from `<probe_sample_selection>`), per-tool `contract` results, and counts `passed`, `gated`, `failed`.
- `main(argv=None) -> int` with `--stage` (defaulting to `remerge.json`'s `stage_path`), `--out`, `--json`, and `--tool` for probing a single name. Exit non-zero when any tool result is `fail`; `gated` does not fail the run.

Record names, paths, stage names, contract verdicts, and missing-credential key **names** only. Never write key values, credential material, or full tool response payloads into the artifact -- reuse the `_contains_secret` guard from `scripts/capture_sync_baseline.py` before writing, and reuse `normalize_probe_result` so volatile timestamps and generated IDs are normalized while structural drift stays visible.

Do not diff any result against Phase 1's `probes/*.json`. D-03 excludes them from the binding surface and D-04 requires these probes pass on their own terms.

Format with `ruff format` using the repository `pyproject.toml` settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && STAGE="$(jq -r '.stage_path' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json)" && python3 scripts/probe_custom_tools.py --stage "$STAGE" --out .planning/phases/02-upstream-main-integration/evidence/staging/probes --json && jq -e '.failed == 0 and (.sample | length) == 6 and ([.contract[] | .verdict] | map(. == "pass" or . == "gated") | all) and ([.contract[] | select(.name == "DegreesOfUnsaturation_calculate") | .verdict] | all(. == "pass"))' .planning/phases/02-upstream-main-integration/evidence/staging/probes/summary.json</automated>
  </verify>

  <acceptance_criteria>
    - `python3 scripts/probe_custom_tools.py --stage <stage> --json` exits 0.
    - `jq -r '.failed' probes/summary.json` equals 0.
    - `jq -r '.sample | length' probes/summary.json` equals 6, and each entry has non-null `selection_rule`, `preservation_linkage`, and `credential_expectation`.
    - `jq -e '[.contract[] | select(.name == "DegreesOfUnsaturation_calculate")] | map(.verdict == "pass") | all' probes/summary.json` is true -- the offline control passes outright.
    - `jq -e '[.contract[] | select(.name == "USPTO_get_patent_assignment" or .name == "USPTO_get_patent_transactions")] | map(.verdict == "pass" or .verdict == "gated") | all' probes/summary.json` is true, and any `gated` verdict carries a non-empty `missing_keys` list.
    - `jq -e '[.contract[] | has("python") and has("cli")] | all' probes/summary.json` is true -- both flavors ran per tool.
    - `jq -r '.exclusions | length' probes/summary.json` is at least 3, naming the `custom_code` / `plugin_asset` path-level coverage, Phase 5 / SURF-01, and Phase 5 / TEST-01.
    - `jq -r '.stage_merge_oid' probes/summary.json` equals `git rev-parse refs/audit/remerge`.
    - `grep -riE 'sk-[A-Za-z0-9]|api[_-]?key["'"'"']?\s*[:=]\s*["'"'"'][A-Za-z0-9]' .planning/phases/02-upstream-main-integration/evidence/staging/probes/` returns no match.
    - `ruff format --check scripts/probe_custom_tools.py` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Read-only against the throwaway stage; deleting the probes directory undoes it.</reversibility>

  <done>Every tool in the recorded sample completes discover -> inspect -> execute through both the Python core and the `tu` CLI inside the stage, no result is `fail`, the offline control passes outright, and the selection plus its exclusions are written into the artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit coverage for the probe contract, including the empty-as-success rejection</name>

  <read_first>
    - `scripts/probe_custom_tools.py` -- `assert_probe_contract` and `PROBE_SAMPLE` as actually written in Task 1.
    - `tests/unit/test_sync_baseline_git.py` lines 1-25 -- the `importlib.util.spec_from_file_location` loader block.
    - `scripts/capture_sync_baseline.py` `_stage` (~69) and `_probe_contract` (~85) -- the stage dict shape the test fixtures must construct.
    - `pytest.ini` -- the default selection this test must run under, with no marker.
  </read_first>

  <files>tests/unit/test_probe_custom_tools.py</files>

  <behavior>
    - A complete stage set (discover found, inspect returned a spec, execute returned substantive content) yields `pass`.
    - Inspect succeeded and execute reported a missing credential yields `gated` with the missing key names carried through.
    - Discover finding nothing yields `fail`.
    - Inspect returning no `parameter` and no `parameters` object yields `fail`.
    - Execute raising `ImportError` or `AttributeError` yields `fail` -- the signature of a broken lazy-registry module string.
    - Execute returning `[]` or `{}` with no gating signal yields `fail`, not `pass`.
    - Every `PROBE_SAMPLE` record carries non-empty `name`, `selection_rule`, `preservation_linkage`, and `credential_expectation`, and the sample contains no duplicate `name`.
  </behavior>

  <action>
Create `tests/unit/test_probe_custom_tools.py`. Load the module under test with `importlib.util.spec_from_file_location("probe_custom_tools", Path(__file__).parents[2] / "scripts/probe_custom_tools.py")` and bind `assert_probe_contract` and `PROBE_SAMPLE`.

Build stage-dict fixtures by hand matching the shape `_stage` produces in `scripts/capture_sync_baseline.py`; do not import `tooluniverse` and do not instantiate `ToolUniverse` -- `assert_probe_contract` is pure and must be provable without the package.

Write the cases in `<behavior>` as a `pytest.mark.parametrize` verdict table. Give the empty-as-success rejection its own explicitly named test rather than folding it into the table -- this repository has a recorded history of empty results passing as success, and the case deserves a named regression guard.

Add a separate test asserting `PROBE_SAMPLE`'s shape invariants and uniqueness.

Add no pytest marker; the file must run under `pytest.ini`'s default selection with no network, credentials, or heavy imports. Format with `ruff format` using the repository settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && .venv/bin/python -m pytest tests/unit/test_probe_custom_tools.py -q && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_probe_custom_tools.py -q` exits 0 with zero skipped tests.
    - `.venv/bin/python -m pytest tests/unit/test_probe_custom_tools.py --collect-only -q` lists a case for each of the six `assert_probe_contract` outcomes plus the `PROBE_SAMPLE` shape test.
    - A test node name references the empty-result rejection explicitly, and it passes.
    - `grep -c -E '^[[:space:]]*(import|from)[[:space:]]+tooluniverse' tests/unit/test_probe_custom_tools.py` returns 0 -- the contract logic is provable without the package.
    - `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q` exits 0.
    - `ruff format --check tests/unit/test_probe_custom_tools.py` exits 0.
  </acceptance_criteria>

  <done>Every probe-contract outcome has passing unit coverage, the empty-result rejection has a named regression guard, and `PROBE_SAMPLE`'s shape invariants are asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Verify preserved plugin symlinks and the registration chain hold in the stage</name>

  <read_first>
    - `scripts/capture_sync_baseline.py` -- `inspect_symlink` (line ~887), `prove_plugin_link_mapping` (~992), `_index_records` (~914). Reuse them; do not write a second symlink inspector.
    - `.planning/phases/01-protected-sync-baseline/evidence/21945440c9f2a15537ba878500a800d9e330eab0/preservation.json` -- query with `jq` for records carrying a `symlink` object; these are the three `plugin/skills/*-workspace` links Phase 1 repaired from PR #161 evidence.
    - `.planning/codebase/STRUCTURE.md` -- the `plugin/skills/` symlinked-workspace layout.
    - `tests/unit/test_registry_integrity.py` -- the ghost-reference and `type`-to-class gate.
    - `.planning/phases/02-upstream-main-integration/evidence/staging/remerge.json` -- `stage_path`.
  </read_first>

  <files>scripts/probe_custom_tools.py</files>

  <action>
Add a `--symlinks` mode to `scripts/probe_custom_tools.py` that proves the plugin-asset half of PRES-02, which execution probes cannot reach.

Read every `preservation.json` path record carrying a `symlink` object. For each, inspect the corresponding path inside the re-merge stage using `inspect_symlink` from `scripts/capture_sync_baseline.py` -- `lstat` and `readlink` only, never traversal. A link replaced by a real directory, or a link whose target changed, is a finding, not a pass. Phase 1 repaired three `plugin/skills/*-workspace` links from authoritative PR #161 evidence; the re-merge must not have destructively written through them.

Record per link into `probes/symlinks.json`: `path`, `phase1_target`, `stage_is_symlink`, `stage_target`, `target_matches`, `in_root` (whether the resolved target stays inside the repository root, using path arithmetic rather than following the link), and `verdict` of `preserved` / `retargeted` / `replaced_by_regular_file` / `absent`.

Then re-run the registration-chain gate inside the stage to close the loop between what the probes exercised and what the catalog declares: `<stage>/.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q` with the stage as working directory. Record the exit code and summary line under `probes/summary.json` key `registry_integrity_at_probe_time`. This is deliberately the same gate plan 02-02 Task 3 ran; re-running it after the probes confirms the probe run itself did not mutate the stage's catalog state.

Write a sorted `probes/SHA256SUMS` covering every file under `probes/` and nothing else, in the `shasum -a 256` output format so `shasum -a 256 -c SHA256SUMS` verifies from inside that directory. Do **not** touch the staging-root `SHA256SUMS`: plan 02-03 runs in the same wave and owns that file, and plan 02-05 regenerates one bundle-wide checksum set at publication. Two plans rewriting the same checksum file concurrently is a lost-update, not a merge.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && STAGE="$(jq -r '.stage_path' .planning/phases/02-upstream-main-integration/evidence/staging/remerge.json)" && python3 scripts/probe_custom_tools.py --stage "$STAGE" --symlinks --out .planning/phases/02-upstream-main-integration/evidence/staging/probes --json && jq -e '([.links[] | .verdict] | map(. == "preserved") | all) and ([.links[] | .in_root] | all) and (.links | length) >= 3' .planning/phases/02-upstream-main-integration/evidence/staging/probes/symlinks.json && jq -e '.registry_integrity_at_probe_time.exit_code == 0' .planning/phases/02-upstream-main-integration/evidence/staging/probes/summary.json && (cd .planning/phases/02-upstream-main-integration/evidence/staging/probes && shasum -a 256 -c SHA256SUMS)</automated>
  </verify>

  <acceptance_criteria>
    - `jq -r '.links | length' probes/symlinks.json` is at least 3 and equals the count of `preservation.json` records carrying a `symlink` object.
    - `jq -e '[.links[] | .verdict] | map(. == "preserved") | all' probes/symlinks.json` is true. This is a hard gate, not a record-and-carry: Phase 1 repaired these three links from authoritative PR #161 evidence, so a `retargeted`, `replaced_by_regular_file`, or `absent` verdict means the re-merge destroyed a preserved asset and the task fails. Fix the resolution in plan 02-03 and re-run rather than recording it as a finding.
    - `jq -e '[.links[] | .stage_is_symlink] | all' probes/symlinks.json` is true -- no link was replaced by a regular file or directory.
    - `jq -e '[.links[] | .in_root] | all' probes/symlinks.json` is true.
    - `jq -r '.registry_integrity_at_probe_time.exit_code' probes/summary.json` equals 0.
    - `shasum -a 256 -c SHA256SUMS` from inside `evidence/staging/probes/` exits 0 and its file list covers every `*.json` under `probes/`.
    - `git diff --name-only -- .planning/phases/02-upstream-main-integration/evidence/staging/SHA256SUMS` shows no change from this plan -- the staging-root checksum file belongs to plan 02-04.
    - `git -C <stage> status --porcelain` produces no output -- the probe run did not mutate the stage.
  </acceptance_criteria>

  <reversibility rating="reversible">Read-only inspection; deleting the probes artifacts undoes it.</reversibility>

  <done>Every preservation-recorded symlink is still a link with its recorded target inside the repository root in the re-merge stage, and the registration-chain gate is green after the probe run.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
See `02-01-PLAN.md` section "Artifacts this phase produces" for the complete phase-wide list. Symbols first created by **this** plan:

- `scripts/probe_custom_tools.py` -- `PROBE_SAMPLE`, `probe_tool_python`, `probe_tool_cli`, `assert_probe_contract`, `run_probe_suite`, `main`; flags `--stage`, `--out`, `--tool`, `--symlinks`, `--json`.
- `tests/unit/test_probe_custom_tools.py`
- `.planning/phases/02-upstream-main-integration/evidence/staging/probes/<tool>.json`, `probes/summary.json`, `probes/symlinks.json`, `probes/SHA256SUMS`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool definitions -> `ToolUniverse.run_one_function` | Merged tool configs drive dynamic module import and execution |
| probe process -> external providers | A non-gated tool may issue a network request to a third-party API |
| probe results -> evidence bundle | Tool responses and environment state are recorded to a committed artifact |
| `preservation.json` symlink records -> filesystem | Recorded link targets are compared against on-disk links |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Information Disclosure | `probes/*.json` evidence output | high | mitigate | Record tool names, stage names, contract verdicts, and missing-credential key **names** only. Never key values, credential material, or full response payloads. Run the `_contains_secret` guard from `scripts/capture_sync_baseline.py` before every write; an acceptance criterion greps the probes directory for credential-shaped strings. |
| T-02-17 | Tampering | empty result masquerading as success | high | mitigate | `assert_probe_contract` returns `fail`, not `pass`, for an empty execute result with no gating signal, with a dedicated named regression test. This repository has a recorded history of empty-as-success passing a health gate. |
| T-02-18 | Spoofing | symlink verification | medium | mitigate | Links are inspected with `lstat` / `readlink` via `inspect_symlink` and never traversed; `in_root` is computed by path arithmetic, not by following the link, so a link retargeted outside the repository cannot pass as preserved. |
| T-02-19 | Elevation of Privilege | dynamic tool module import during execution | medium | mitigate | Probes execute only the six named `PROBE_SAMPLE` tools inside the throwaway stage's own `.venv`, never the main checkout's environment. `tu` is resolved from the stage's `.venv/bin`, not PATH. |
| T-02-20 | Denial of Service | network calls to third-party providers | low | accept | Bounded to six tools, one execution each; credential-gated tools never reach the network. `_run_command`'s existing timeout applies. Live-provider validation is explicitly out of scope per REQUIREMENTS.md. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero packages; the stage's environment was created in plan 02-03 from the committed `uv.lock`. No legitimacy checkpoint is required. |
</threat_model>

<verification>
1. All six sampled tools complete discover -> inspect -> execute through both the Python core and the `tu` CLI inside the stage.
2. `failed` is 0; the offline control passes outright; every `gated` result names its missing keys.
3. The sample selection, its rules, and its deliberate exclusions are recorded in the artifact.
4. Every preservation-recorded symlink is still a link with its recorded in-root target.
5. `tests/unit/test_registry_integrity.py` green inside the stage after the probe run.
6. No credential-shaped string appears anywhere under `evidence/staging/probes/`.
7. `evidence/staging/probes/SHA256SUMS` verifies; the staging-root `SHA256SUMS` is untouched by this plan.
</verification>

<success_criteria>
- Criterion 4 has fresh, non-diffed artifact evidence: a recorded sample of preservation-flagged fork-only tools that load and execute after the integration.
- The registration chain is proven at execution time, not only at catalog-load time.
- The plugin-asset half of PRES-02 is proven by non-traversing symlink inspection.
- The coverage boundary is explicit: what was probed, and what was deferred to Phase 5.
</success_criteria>

<output>
Create `.planning/phases/02-upstream-main-integration/02-05-SUMMARY.md` when done.
</output>
