---
phase: 02-upstream-main-integration
plan: 06
type: execute
wave: 5
depends_on: ["02-04", "02-05"]
files_modified:
  - .planning/phases/02-upstream-main-integration/02-FINDINGS.md
  - .planning/phases/02-upstream-main-integration/evidence/staging/SHA256SUMS
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false

requirements: [SYNC-01, SYNC-02, PRES-02]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A human has reviewed the corrective-commit candidate list and explicitly approved, rejected, or amended it before any source file on docs/gsd-codebase-map is touched (D-06, one-way reversibility)."
    - "Every approved corrective commit is individually attributable, is scoped to exactly one finding, and has survived the D-06a pin recheck (a finding already present at 21945440 earns no commit)."
    - "The evidence bundle is published to an immutable evidence/<remerge-oid>/ directory with a sorted SHA256SUMS that verifies, following Phase 1's convention."
    - "All four phase success criteria have named artifact evidence, and SYNC-01, SYNC-02, and PRES-02 are each traceable to a specific artifact assertion."
  artifacts:
    - .planning/phases/02-upstream-main-integration/02-FINDINGS.md
    - ".planning/phases/02-upstream-main-integration/evidence/<remerge-oid>/SHA256SUMS"
  key_links:
    - "The decision checkpoint sits BEFORE the first corrective commit, not before the re-merge: D-06's throwaway-branch posture is what keeps the phase reversible, and landing a corrective commit on the shared docs/gsd-codebase-map branch is the one step that is not."
    - "publish_evidence() and verify_checksums() from scripts/capture_sync_baseline.py:1317-1408 are reused verbatim for the final bundle, matching Phase 1's evidence/<full-OID>/ layout."
    - "REQUIREMENTS.md's traceability table and ROADMAP.md's Phase 2 entry are the downstream consumers of this phase's completion state."
  prohibitions:
    - "MUST NOT land any corrective commit before the checkpoint:decision resolves with explicit human approval."
    - "MUST NOT land a corrective commit for a finding classified self_healed_downstream -- the pinned tree already has it (D-06a)."
    - "MUST NOT land a corrective commit for a dependency-version disagreement; route it to Phase 5 / COMP-01."
    - "MUST NOT merge, rebase, cherry-pick, or fast-forward refs/audit/remerge into any branch. Corrective commits are hand-authored, individually attributable changes, not a merge of the audit stage."
    - "MUST NOT push any ref to the upstream remote (mims-harvard/ToolUniverse); origin is the boundary."
    - "MUST NOT commit corrective changes to any branch other than docs/gsd-codebase-map (D-06b)."
    - "MUST NOT record a finding as accepted debt; Phase 1's D-02 posture is fully green with no accepted debt, so a real loss gets fixed."
    - "MUST NOT modify, stage, or delete .planning/config.json or ralph-specs/fleet/results/."
    - "MUST NOT resolve PR #161 here; Phase 1 evidence already answers it and SYNC-03 belongs to Phase 3."
    - "MUST NOT expand scope into catalog certification, cross-surface certification, or documentation updates; those are Phases 3, 4, and 5."
---

<objective>
Close the phase. Present the corrective-commit candidates for an explicit human decision, land only the approved ones on `docs/gsd-codebase-map`, publish the immutable evidence bundle, and update the roadmap and requirements traceability.

Purpose: this is the only plan in the phase that writes to a shared branch. Every prior plan was reversible by deleting a temporary worktree and some JSON. A corrective commit is not -- it lands on a branch concurrent sessions share, and reversing D-06's posture later means re-litigating the commits already landed under it. That asymmetry is why the decision checkpoint sits here and nowhere earlier.

Output: approved corrective commits (possibly zero -- that is a legitimate and expected outcome), a published `evidence/<remerge-oid>/` bundle, and updated `ROADMAP.md` / `REQUIREMENTS.md` traceability.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-upstream-main-integration/02-CONTEXT.md
@.planning/phases/02-upstream-main-integration/02-FINDINGS.md
@.planning/phases/02-upstream-main-integration/02-04-SUMMARY.md
@.planning/phases/02-upstream-main-integration/02-05-SUMMARY.md
@.planning/phases/01-protected-sync-baseline/01-VERIFICATION.md
</context>

<flagged_assumptions>
Restated so this plan is self-contained. None silently dropped.

| # | Assumption | Status |
|---|---|---|
| EC-SYNC-01 | Edge coverage `unclassified` / `unresolved`; no backstop authored, and none may be auto-added. | UNRESOLVED (flagged) -- Task 3's criteria table names the containment diff as SYNC-01's sole edge evidence. |
| EC-SYNC-02 | Edge coverage `unclassified` / `unresolved`. | UNRESOLVED (flagged) -- the union sweep, the resolution record, and the full-tree diff are SYNC-02's edge evidence. |
| EC-PRES-02 | Edge coverage `unclassified` / `unresolved`. | UNRESOLVED (flagged) -- the preservation join, the probe suite, and the symlink verification are PRES-02's edge evidence. |
| A1 | D-08 does not explicitly classify JSON tool-definition arrays as "structural files". | RESOLVED ON EVIDENCE -- union applied; recorded in `02-FINDINGS.md` for the reviewer to confirm or overturn at the checkpoint. |
| A2 | CONTEXT.md D-03 misstates the `preservation.json` class distribution. | CONTRADICTED BY PRIMARY SOURCE -- the measured distribution is recorded in `preservation-reclass.json` and surfaced at the checkpoint. |
| A3 | `preservation.json` is an upstream<->pinned-fork delta, so the join crosses two comparison bases. | FLAGGED -- named in `02-FINDINGS.md` so the reviewer weighs each `lost` disposition with that in view. |
| OQ1 | `pyproject.toml` dependency changes. | DECIDED -- routed to Phase 5 / COMP-01, never a corrective commit here. |
| OQ2 | Fresh `uv sync` in the stage. | DECIDED -- done in plan 02-03; no effect on this plan. |
</flagged_assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Human decision on the corrective-commit set</name>

  <read_first>
    - `.planning/phases/02-upstream-main-integration/02-FINDINGS.md` -- the full review surface.
    - `.planning/phases/02-upstream-main-integration/evidence/staging/findings.json` -- every disagreement with its verdict, pin recheck, and repair-commit evidence.
    - `.planning/phases/02-upstream-main-integration/evidence/staging/preservation-reclass.json` -- the 1,392 dispositions; specifically every record with `phase2_disposition: "lost"`.
    - `.planning/phases/02-upstream-main-integration/02-CONTEXT.md` D-06, D-06a, D-06b -- the governing posture.
    - `.planning/phases/01-protected-sync-baseline/01-CONTEXT.md` D-02 -- Phase 1's fully-green, no-accepted-debt posture, which governs whether a real loss may be recorded rather than fixed.
  </read_first>

  <decision>Which of the proposed corrective commits should land on `docs/gsd-codebase-map`, and does the reviewer accept the audit's resolution of the flagged assumptions?</decision>

  <context>
Everything up to this point has been reversible: a detached worktree under `${TMPDIR}`, an audit-namespace ref outside `refs/heads/`, and JSON under `.planning/`. Removing them leaves no trace.

A corrective commit is different. It lands on `docs/gsd-codebase-map`, which concurrent Claude sessions share and which carries the D-01 pin, Phase 1's evidence, and `git.json`. CONTEXT.md rates D-06 itself **one-way**: reversing the findings-only posture later means re-litigating every corrective commit already landed under it. Landing a wrong commit is also the one way this audit could re-add something already present or revert a later deliberate change -- which is precisely what D-06a's pin recheck exists to prevent.

A **zero-commit outcome is legitimate and expected.** Planning already measured that the union held at the name level across all 213 both-sides `data/*.json` files, that both fork-only `uspto_tools.json` entries survived, and that the two upstream-deleted files were relocated rather than lost. If the audit confirms the landed merge was correct throughout, the correct action is to record that and commit nothing to source.

Present to the reviewer, from `02-FINDINGS.md`:
- The count of each finding verdict, and for every `landed_dropped_or_altered` candidate: the path, the diff summary, the pin recheck result, and the repair-commit evidence.
- Every `preservation-reclass.json` record with `phase2_disposition: "lost"`, with its Phase 1 `class` and `must_survive` text.
- The three flagged assumptions the audit resolved on evidence (A1's union reading, A2's CONTEXT.md class-distribution correction, A3's base-crossing join) so the reviewer can overturn any of them.
- The two decided open questions (OQ1 `pyproject.toml` routing, OQ2 fresh `uv sync`).
  </context>

  <options>
    <option id="approve-all">
      <name>Approve every proposed corrective commit as listed</name>
      <pros>Restores every confirmed real loss in one reviewed pass; keeps Phase 1's fully-green, no-accepted-debt posture intact.</pros>
      <cons>Each commit is a write to a shared branch; a mistaken diagnosis lands as a real change.</cons>
    </option>
    <option id="approve-subset">
      <name>Approve a named subset; defer or reject the rest with recorded reasons</name>
      <pros>Highest-confidence items land now; ambiguous ones stay as recorded findings for a follow-up decision.</pros>
      <cons>Leaves an open item that must be tracked so it does not silently become accepted debt, which D-02 forbids.</cons>
    </option>
    <option id="approve-none">
      <name>Land nothing; record the audit result as findings only</name>
      <pros>Zero blast radius on the shared branch. Correct outcome if the audit confirms the landed merge preserved everything, which the planning measurements suggest is likely.</pros>
      <cons>If a real loss exists and is not fixed, criterion 2 or 3 fails at verification.</cons>
    </option>
    <option id="amend-and-rerun">
      <name>Overturn one of the resolved assumptions and re-run the affected audit stage</name>
      <pros>Correct response if the reviewer rejects A1's union reading or A3's join treatment; the audit is cheap to re-run.</pros>
      <cons>Costs another wave; the stage must be rebuilt or the affected script re-run.</cons>
    </option>
  </options>

  <resume-signal>Select: approve-all, approve-subset (name the paths), approve-none, or amend-and-rerun (name the assumption). Then confirm the branch to commit on is `docs/gsd-codebase-map`.</resume-signal>

  <acceptance_criteria>
    - The chosen option, the exact approved path list (possibly empty), and any overturned assumption are recorded verbatim in `02-FINDINGS.md` under a `## Corrective-commit decision` heading with an ISO-8601 millisecond UTC stamp.
    - No file under `src/`, `tests/`, or the repository root has been modified before this checkpoint resolves: `git status --porcelain -- src/ tests/ pyproject.toml uv.lock` produces no output at the moment the checkpoint is presented.
  </acceptance_criteria>

  <reversibility rating="one-way">A corrective commit lands on the shared `docs/gsd-codebase-map` branch that concurrent sessions build on; CONTEXT.md rates D-06's posture one-way because reversing it means re-litigating every commit landed under it. This checkpoint is the gate that keeps the rest of the phase reversible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Land the approved corrective commits, one finding per commit</name>

  <read_first>
    - `.planning/phases/02-upstream-main-integration/02-FINDINGS.md` section "Corrective-commit decision" -- the approved list from Task 1.
    - `.planning/phases/02-upstream-main-integration/evidence/staging/findings.json` -- the per-path blob OIDs backing each approved change.
    - `.planning/phases/02-upstream-main-integration/02-CONTEXT.md` D-06b -- corrective commits land on `docs/gsd-codebase-map`, named explicitly because two branches are live.
    - `pyproject.toml` -- the repository's `ruff` configuration; the global `~/.ruff.toml` must not be used.
    - The current content of each approved path in the main checkout, so the change is authored against present state rather than against the audit stage's copy.
  </read_first>

  <files>(determined by the approved list from Task 1; empty if approve-none)</files>

  <action>
If Task 1 resolved to `approve-none`, skip all editing and proceed directly to the verification below; a zero-commit outcome is a valid completion of this task and must be recorded as such in the summary.

Otherwise, for each approved path, author the corrective change by hand against the current content of that file in the main checkout. Do **not** merge, cherry-pick, or copy wholesale from `refs/audit/remerge` -- the stage is a review instrument, and 31-plus commits of legitimate work separate it from the current tree. Use the stage as the reference for *what the correct content is*, and write the minimal change that restores the lost fork behavior into the present file.

Before the first write and again before each commit, assert the working context: `git rev-parse --abbrev-ref HEAD` is `docs/gsd-codebase-map`, and `git status --porcelain` shows nothing unexpected beyond `.planning/` work and the two user-owned untracked paths `.planning/config.json` and `ralph-specs/fleet/results/`. Concurrent Claude sessions share this checkout and auto-commit each other's edits; if another session's modification appears, halt and report rather than staging over it.

One finding, one commit. Each commit message names the finding path, states which fork behavior was restored, and cites the evidence: the `findings.json` verdict and the pin recheck result showing the pinned tree did **not** already have it. Stage explicitly with `git add <path>` -- never `git add -A`, never `git add .`, and never `git add -p` interactively.

For every touched Python file run `ruff format` and `ruff check` using the repository's `pyproject.toml` settings, then run the affected test files plus `tests/unit/test_registry_integrity.py`. If a corrective change touches a `data/*.json` tool array, re-run plan 02-01's union subcommand afterward and confirm the file's verdict is still `union_ok`.

Record each landed commit's OID back into `02-FINDINGS.md` beside its finding, so every corrective action is individually attributable.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && test "$(git rev-parse --abbrev-ref HEAD)" = "docs/gsd-codebase-map" && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py tests/unit/test_sync_baseline_git.py tests/unit/test_audit_upstream_merge.py tests/unit/test_probe_custom_tools.py -q && .venv/bin/python -m pytest -q && ruff check $(git diff --name-only HEAD~${APPROVED_COUNT:-0}..HEAD -- '*.py' | tr '\n' ' ') && ruff format --check $(git diff --name-only HEAD~${APPROVED_COUNT:-0}..HEAD -- '*.py' | tr '\n' ' ') && python3 scripts/audit_upstream_merge.py union --repo . --out .planning/phases/02-upstream-main-integration/evidence/staging --json && jq -e '.summary.net_removed_fork_entries == 0 and .summary.duplicate_name_files == 0' .planning/phases/02-upstream-main-integration/evidence/staging/union.json</automated>
  </verify>

  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` reports `docs/gsd-codebase-map`.
    - The number of new commits touching `src/` or `tests/` equals the length of the approved list from Task 1 -- zero if `approve-none` was chosen.
    - Every such commit message names exactly one finding path and cites its `findings.json` verdict.
    - `git log --format='%H %s' -n <approved-count>` shows no commit whose subject indicates a merge, and `git rev-list --merges HEAD -n 1 --since=<checkpoint-time>` is empty.
    - `git branch --contains refs/audit/remerge` still lists no branch -- the stage was not merged.
    - `.venv/bin/python -m pytest -q` exits 0 under `pytest.ini`'s default selection.
    - `ruff check` and `ruff format --check`, scoped to the `*.py` paths this plan's commits touched, both exit 0. Do **not** run either across the whole tree: pre-existing violations unrelated to this phase would red the gate and are out of scope.
    - `python3 scripts/audit_upstream_merge.py union --repo . --json` still reports zero net-removed fork entries and zero duplicate-name files.
    - `git status --porcelain -- .planning/config.json ralph-specs/` produces no staged or modified entry for those paths.
  </acceptance_criteria>

  <reversibility rating="costly">Each corrective commit lands on a branch concurrent sessions build on. Reverting is possible with `git revert` but requires coordinating with any session that has already built on the commit, so undo is real work rather than free.</reversibility>

  <done>Exactly the approved corrective commits exist, each scoped to one finding and individually attributable, the full default test suite and both ruff gates are green, and the union sweep still holds.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the immutable evidence bundle and update traceability</name>

  <read_first>
    - `scripts/capture_sync_baseline.py` -- `publish_evidence` (line ~1317), `verify_checksums` (~1372), `_canonical_json` (~1305), `_contains_secret` (~1312). Reuse them verbatim; do not write a second checksum scheme.
    - `.planning/phases/01-protected-sync-baseline/evidence/21945440c9f2a15537ba878500a800d9e330eab0/` -- the directory layout and `SHA256SUMS` format this bundle follows.
    - `.planning/ROADMAP.md` section "Phase 2: Upstream Main Integration" -- the four success criteria and the `**Plans**:` line to finalize.
    - `.planning/REQUIREMENTS.md` section Traceability -- the SYNC-01 / SYNC-02 / PRES-02 rows to mark.
    - `.planning/phases/02-upstream-main-integration/evidence/staging/` -- every artifact to publish.
  </read_first>

  <files>.planning/phases/02-upstream-main-integration/02-FINDINGS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <action>
Publish `evidence/staging/` to an immutable `.planning/phases/02-upstream-main-integration/evidence/<remerge-oid>/` directory, where `<remerge-oid>` is the full 40-character OID from `git rev-parse refs/audit/remerge`. Use `publish_evidence` from `scripts/capture_sync_baseline.py` so the temporary-sibling-tree write and the sorted `SHA256SUMS` generation match Phase 1's convention exactly. Run its `_contains_secret` guard over every artifact before publication. The bundle contains `union.json`, `remerge.json`, `findings.json`, `preservation-reclass.json`, `probes/*.json`, and `SHA256SUMS`.

Verify the published bundle with `verify_checksums` and independently with `shasum -a 256 -c SHA256SUMS` run from inside the published directory.

Complete `02-FINDINGS.md` with a **Success criteria evidence** table mapping each of the four ROADMAP criteria and each of SYNC-01 / SYNC-02 / PRES-02 to a specific artifact assertion -- the exact `jq` expression or command whose output backs it, not a prose claim. Include the three unresolved edge-coverage rows as an explicit flagged-assumptions section so the verification step sees them rather than inferring silence as coverage. Keep the document ASCII-only with `-` and `--` in place of dashes, `->` in place of arrow glyphs, and padded table pipes.

Update `.planning/ROADMAP.md` with a scoped `Edit` -- never a whole-file `Write`, because concurrent sessions share the file and a full rewrite destroys phase entries outside the diff window. Set the Phase 2 `**Plans**:` line to `5 plans` and add the plan list with each objective in one line. Leave the Goal and the four Success Criteria unchanged; they are already correct and the phase met them as an audit rather than as a merge.

Update `.planning/REQUIREMENTS.md` with a scoped `Edit`: mark the SYNC-01, SYNC-02, and PRES-02 checkboxes and set their Traceability rows to Complete. Do not touch SYNC-03's row -- Phase 1 evidence already answers PR #161 ancestry (`pr161_ancestor: true`, merge OID `16af425c053c306a658c96e254b4c4114338dd11`) but SYNC-03 remains Phase 3's requirement to close.

Commit the published bundle, `02-FINDINGS.md`, `ROADMAP.md`, and `REQUIREMENTS.md` together in one documentation commit on `docs/gsd-codebase-map`. Then remove the throwaway stage worktree with `git worktree remove --force <stage_path>` and `git worktree prune`, leaving `refs/audit/remerge` in place so the audited tree stays inspectable. Record in the summary that the ref is retained deliberately and that no branch contains it.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && OID="$(git rev-parse refs/audit/remerge)" && BUNDLE=".planning/phases/02-upstream-main-integration/evidence/$OID" && test -d "$BUNDLE" && (cd "$BUNDLE" && shasum -a 256 -c SHA256SUMS) && test -f "$BUNDLE/union.json" && test -f "$BUNDLE/remerge.json" && test -f "$BUNDLE/findings.json" && test -f "$BUNDLE/preservation-reclass.json" && test -f "$BUNDLE/probes/summary.json" && grep -q 'SYNC-01' .planning/phases/02-upstream-main-integration/02-FINDINGS.md && grep -q 'PRES-02' .planning/phases/02-upstream-main-integration/02-FINDINGS.md && grep -qE '^\| SYNC-02 \| Phase 2 \| Complete' .planning/REQUIREMENTS.md && perl -ne 'exit 1 if /[^\x00-\x7F]/' .planning/phases/02-upstream-main-integration/02-FINDINGS.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02-upstream-main-integration/evidence/$(git rev-parse refs/audit/remerge)/` exists and contains `union.json`, `remerge.json`, `findings.json`, `preservation-reclass.json`, `probes/summary.json`, and `SHA256SUMS`.
    - `shasum -a 256 -c SHA256SUMS` from inside the published bundle exits 0 and reports OK for every listed file.
    - `grep -rlE 'sk-[A-Za-z0-9]{12}|BEGIN [A-Z ]*PRIVATE KEY' <bundle>` returns no match.
    - `02-FINDINGS.md` contains a "Success criteria evidence" table with one row per ROADMAP criterion (4) and one per requirement (SYNC-01, SYNC-02, PRES-02), each citing a runnable command or `jq` expression rather than prose.
    - `02-FINDINGS.md` contains a flagged-assumptions section naming EC-SYNC-01, EC-SYNC-02, and EC-PRES-02 as unresolved edge-coverage rows.
    - `perl -ne 'exit 1 if /[^\x00-\x7F]/' .planning/phases/02-upstream-main-integration/02-FINDINGS.md` exits 0 (BSD `grep` on macOS has no `-P`; do not use it).
    - `.planning/ROADMAP.md` Phase 2 entry reads `**Plans**: 5 plans` and lists all five plan files; `git diff HEAD~1 -- .planning/ROADMAP.md` shows changes confined to the Phase 2 block.
    - `.planning/REQUIREMENTS.md` shows `[x]` for SYNC-01, SYNC-02, PRES-02 and `Complete` in their Traceability rows, with SYNC-03 still `Pending`.
    - `git worktree list` no longer lists the stage path, and `git rev-parse refs/audit/remerge` still resolves.
    - `git branch --contains refs/audit/remerge` lists no branch.
    - `git remote -v` confirms no push occurred to `upstream`; `git reflog show upstream/main` shows no local update from this phase.
  </acceptance_criteria>

  <reversibility rating="costly">Publishing evidence and marking requirements complete lands on the shared branch; reverting requires a coordinated revert plus reopening the requirement rows.</reversibility>

  <done>The immutable evidence bundle is published and checksum-verified, `02-FINDINGS.md` maps every criterion and requirement to a runnable assertion, ROADMAP and REQUIREMENTS traceability is updated by scoped edits, and the throwaway stage is removed while its ref remains inspectable and unmerged.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
See `02-01-PLAN.md` section "Artifacts this phase produces" for the complete phase-wide list. Symbols first created by **this** plan:

- `.planning/phases/02-upstream-main-integration/evidence/<remerge-oid>/` -- the published immutable bundle with its own sorted `SHA256SUMS`
- `02-FINDINGS.md` section "Corrective-commit decision" and section "Success criteria evidence"
- Corrective commits on `docs/gsd-codebase-map` (count determined by the Task 1 decision; zero is a valid outcome)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit stage -> shared branch | Reviewed content crosses from a throwaway instrument into `docs/gsd-codebase-map` |
| human decision -> automated action | An approval list drives file writes |
| evidence bundle -> committed repository | Audit artifacts become permanent, checksummed record |
| local repository -> remotes | Push targets `origin` only; `upstream` is out of bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21 | Tampering | corrective commits on the shared branch | high | mitigate | A blocking `checkpoint:decision` precedes any source write; each commit is scoped to one finding, hand-authored against present content rather than copied from the stage, staged with an explicit `git add <path>`, and cites its `findings.json` verdict plus its pin-recheck result. |
| T-02-22 | Repudiation | audit stage silently merged into the shared branch | high | mitigate | Acceptance criteria assert `git branch --contains refs/audit/remerge` lists no branch and that no merge commit was created after the checkpoint. The ref lives outside `refs/heads/`, so no ordinary branch operation picks it up. |
| T-02-23 | Tampering | concurrent session interleaving during commit | high | mitigate | Branch and porcelain status are asserted before the first write and again before each commit; an unexpected modification halts the task. `git add -A` and `git add .` are prohibited so another session's in-flight edit cannot be swept into a corrective commit. |
| T-02-24 | Information Disclosure | published evidence bundle | high | mitigate | `_contains_secret` from `scripts/capture_sync_baseline.py` runs over every artifact before publication, and an acceptance criterion greps the published bundle for credential-shaped strings. Artifacts carry names, paths, OIDs, counts, and verdicts only. |
| T-02-25 | Tampering | push to the wrong remote | high | mitigate | No push occurs in this plan. An acceptance criterion confirms `upstream` received nothing. PROJECT.md and the recorded project feedback make `origin` the hard boundary. |
| T-02-26 | Tampering | whole-file rewrite of shared planning documents | medium | mitigate | `ROADMAP.md` and `REQUIREMENTS.md` are updated with scoped `Edit` operations only; an acceptance criterion asserts the ROADMAP diff is confined to the Phase 2 block. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero packages; no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. The decision checkpoint resolved with an explicit human selection recorded in `02-FINDINGS.md`.
2. The corrective commit count equals the approved list length; zero is valid.
3. No merge commit was created and no branch contains `refs/audit/remerge`.
4. Full default pytest suite is green, and `ruff check` / `ruff format --check` are green over the touched `*.py` paths only.
5. The union sweep still reports zero net-removed fork entries.
6. The published bundle exists at `evidence/<remerge-oid>/` and its `SHA256SUMS` verifies.
7. `02-FINDINGS.md` maps all four criteria and all three requirements to runnable assertions and names the three unresolved edge-coverage rows.
8. `ROADMAP.md` and `REQUIREMENTS.md` updated by scoped edits; SYNC-03 untouched.
9. Nothing was pushed to `upstream`.
</verification>

<success_criteria>
- Criterion 1: an isolated integration stage was inspectable and provably excluded unrelated pre-existing worktree changes.
- Criterion 2: shared canonical definitions follow upstream and fork-only definitions remain, proven by the 213-file union sweep, the recorded D-08 resolution table, and the full-tree disagreement classification.
- Criterion 3: all 1,392 preservation entries carry a Phase 2 disposition, with every `lost` item either corrected or explicitly decided at the checkpoint.
- Criterion 4: the recorded custom-tool sample loads and executes in the stage, and every preservation-recorded symlink is intact.
- The phase ends fully green with no accepted debt, per Phase 1's D-02 posture.
</success_criteria>

<output>
Create `.planning/phases/02-upstream-main-integration/02-06-SUMMARY.md` when done.
</output>
