---
phase: 03-follow-up-and-catalog-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit_registration_chain.py
  - tests/unit/test_audit_registration_chain.py
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/git.json
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/worktree_fingerprint.json
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/chain_tracer.json
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/SHA256SUMS
autonomous: true
requirements: [SYNC-03, CAT-01, CAT-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SYNC-03 is answered by a re-derivation at the HEAD this phase actually executes against, not by copying Phase 1's git.json: the evidence records the live HEAD OID, the PR #161 merge OID 16af425c053c306a658c96e254b4c4114338dd11, and the git merge-base --is-ancestor exit code (D-01). A non-zero exit halts the phase as a planning-time finding rather than being worked around (D-02)."
    - "One representative credential-free tool traverses all six registration links end to end -- canonical JSON definition, implementing Python class, live category wiring in default_config.py, lazy-registry type entry, generated module plus tools/__init__.py import, and a test reference -- and is then located by grep_tools and returns a real parameter schema from get_tool_info (CAT-01 link coverage plus the CAT-02 discovery surface, proven on one path before any horizontal expansion)."
    - "The registration-chain audit over an empty name set exits 0 and writes a well-formed report containing zero records rather than raising, and a single-name audit writes exactly one record."
    - "Audit records and every evidence JSON are emitted in deterministic sorted order (by tool name, then by source path), so two runs at the same commit produce byte-identical files and an identical SHA256SUMS."
    - "The pre-existing uncommitted working-tree state is fingerprinted (sorted path list, count, and a digest over that list) before any other Phase 3 task runs, so every later task can prove it neither swept that state into a commit nor reverted it (D-06)."
    - statement: "For any tool whose required_api_keys are unmet in the execution environment, chain integrity is asserted structurally (definition, module file, __init__ import, lazy-registry type) rather than by live get_tool_info schema inspection, and the tool is recorded as gated with its missing key names rather than counted as a pass or a failure."
      verification: backstop
  artifacts:
    - scripts/audit_registration_chain.py
    - tests/unit/test_audit_registration_chain.py
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/git.json
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/worktree_fingerprint.json
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/chain_tracer.json
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/SHA256SUMS
  key_links:
    - "Link 3 is resolved by parsing src/tooluniverse/default_config.py's category map, treating a commented-out entry that carries an archived marker as intentionally excluded rather than as a chain break -- this is the single place the audit can mistake catalog hygiene for a missing link."
    - "The discovery half of the tracer reads the same ToolUniverse.all_tool_dict collection the codegen scripts read (execute_function.py builds it, tool_discovery_tools.py consumes it), so a credential-gated tool vanishes from codegen and discovery together; the tracer tool is deliberately credential-free so a red result means a real chain break, not a missing key."
    - "Evidence is written under evidence/staging/tracer/ with a tracer-scoped SHA256SUMS. Plans 03-02 and 03-03 own sibling staging subdirectories and plan 03-04 regenerates one bundle-wide checksum set at publication -- two plans writing one checksum file is a lost update, not a merge."
  prohibitions:
    - "MUST NOT discard, revert, commit, stash, or overwrite the pre-existing uncommitted src/tooluniverse/tools/** and .tool_metadata.json working-tree state left by another session -- its disposition is a human decision routed to plan 03-04 (D-06), never an executor action."
    - "MUST NOT report a credential-gated tool as nonexistent or as a catalog-integrity failure; record it as gated with its missing key names so a reader can tell 'needs a key' from 'is not in the catalog'."
    - "MUST NOT mark SYNC-03, CAT-01, or CAT-02 satisfied on evidence copied from a prior phase, from RESEARCH.md prose, or from this plan's own text; every claim must be re-derived against the HEAD the phase actually executes at. Phase 2 marked PRES-02 complete prematurely on exactly this error."
---

<objective>
Prove the whole Phase 3 spine on one thin path before expanding it: re-derive PR #161's ancestry against the live HEAD (SYNC-03, D-01/D-02), walk a single credential-free tool through all six registration links (CAT-01), confirm that same tool is discoverable via `grep_tools` and inspectable via `get_tool_info` (CAT-02), and publish the result as a checksummed evidence artifact -- plus fingerprint the hazardous pre-existing dirty working tree so every later task can prove it did not touch it (D-06).

Purpose: Phase 2 established that a chain can look intact in configuration and still be broken at the far end. Walking every layer for one tool first means an architectural mistake in the audit's link model surfaces after one commit, not after the full-catalog pass in Wave 2 has already been written against it.

Output: `scripts/audit_registration_chain.py` with a single-tool mode, unit coverage for its pure link-verdict logic, and `evidence/staging/tracer/` carrying `git.json`, `worktree_fingerprint.json`, `chain_tracer.json`, and a tracer-scoped `SHA256SUMS`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-CONTEXT.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-RESEARCH.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-PATTERNS.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-VALIDATION.md
@docs/dev_docs/Interaction_Surfaces.md
</context>

<assumption_delta_decision>
Detector fired one `chosen`-family signal on the word "custom" in the phase goal ("every new upstream and preserved **custom** tool"). Decision: **no-change**. "Custom" here labels a tool's provenance (fork-authored vs upstream-authored), not a newly-chosen representation displacing a former default -- both kinds already share one identity model (the `name` field in `src/tooluniverse/data/*.json`) and one loading path. Nothing is being pluralized, made optional, or promoted; no identity-model migration applies to this phase.
</assumption_delta_decision>

<flagged_assumptions>
Restated so this plan is self-contained. No probe-surfaced item is silently dropped. The spec-less edge probe surfaced 5 items across SYNC-03/CAT-01/CAT-02: 3 classified rows were auto-resolved to `covered` acceptance criteria (CAT-01 adjacency -> plan 03-02; CAT-01 empty and CAT-01 ordering -> this plan's `must_haves.truths`), and the 2 `unclassified` rows stay `unresolved` and are carried here as explicit flagged assumptions. 5 = 3 covered + 2 flagged.

| # | Assumption | Status |
|---|---|---|
| EC-SYNC-03 | Edge probe returned `unclassified` / `unresolved` for SYNC-03; no explicit edge criterion and no backstop was auto-authored. The de-facto edge for a boolean ancestry fact is the false branch, which D-02 already governs (halt, do not work around). | UNRESOLVED (flagged) -- this plan's Task 1 precondition is the operational answer; recorded, not silently closed. |
| EC-CAT-02 | Edge probe returned `unclassified` / `unresolved` for CAT-02; no explicit edge criterion and no backstop was auto-authored. | UNRESOLVED (flagged) -- plan 03-03 owns CAT-02's representative-sample edges; the gated-tool backstop truth in this plan's `must_haves` covers the one edge research proved is real on this machine. |
| A1 (RESEARCH.md) | No concurrent process currently writes to the dirty `src/tooluniverse/tools/` set; inferred from a >2h mtime gap plus absent `lsof`/lock evidence, not from a direct cwd read. | MITIGATED HERE -- Task 1's fingerprint turns the assumption into a checkable fact every later task re-asserts. |
| A2 (RESEARCH.md) | The dirty state was produced by `generate_coding_api.py` or `tu build --output src/tooluniverse/tools`, not some third path. | CARRIED -- plan 03-04's regeneration guard tests this mechanically by reproducing a scratch regeneration and diffing it against the dirty tree. |
| Measured baseline drift | At HEAD, 279 names defined in `src/tooluniverse/data/*.json` (excluding `skill:`-prefixed entries) have no import in the committed `src/tooluniverse/tools/__init__.py`, and 0 imported names are undefined. Measured during planning against `git show HEAD:src/tooluniverse/tools/__init__.py`, not the dirty tree. | RECORDED -- this is a pre-existing baseline, not damage from Phase 2. Plan 03-02 encodes it as a reviewed baseline file so new drift hard-fails while the existing gap stays visible as an audit finding. |
</flagged_assumptions>

<tracer_tool_selection>
`DegreesOfUnsaturation_calculate` -- Phase 1's reference tool and Phase 2 plan 02-05's offline control, reused here for the same reason: it is fully offline and deterministic, declares no `required_api_keys`, and is present at every link at HEAD (`src/tooluniverse/data/degrees_of_unsaturation_tools.json`, `src/tooluniverse/tools/DegreesOfUnsaturation_calculate.py`, and one import line in the committed `tools/__init__.py`, all confirmed during planning). A red tracer therefore means a genuine chain break, never a missing credential and never a provider outage.
</tracer_tool_selection>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end single-tool registration-chain proof, ancestry re-derivation, and dirty-tree fingerprint</name>

  <precondition>`git merge-base --is-ancestor 16af425c053c306a658c96e254b4c4114338dd11 HEAD` exits 0 in `/Users/davis/code/ToolUniverse`. A non-zero exit means PR #161 is no longer contained by the current history; halt the task and report it as a planning-time finding per D-02 rather than pivoting to an integration stage inside this plan.</precondition>

  <read_first>
    - `scripts/capture_sync_baseline.py` -- `publish_evidence` (~line 1317) and `verify_checksums` (~1370): the atomic temp-stage-then-rename plus sorted `SHA256SUMS` convention this phase must reuse, and `_canonical_json` for deterministic serialization. Also `_contains_secret`, used as the pre-write guard.
    - `scripts/audit_upstream_merge.py` -- `extract_definition_names` (lines 1043-1053, AST-based top-level and class-level definition extraction) and `classify_finding` (from ~1523, the two-stage verdict shape: primary comparison, then a recheck before finalizing). Reuse both; do not write a second AST walker or a second verdict idiom.
    - `tests/unit/test_registry_integrity.py` -- `_load_defined_tool_names`, `_load_type_names`, `_load_lazy_registry_class_names` (lines 34-77). These already resolve links 1 and 4; the audit must agree with them, not contradict them.
    - `src/tooluniverse/default_config.py` lines 755-770 and 1135-1160 -- the category map, the commented-out `oxo` entry with its archived marker, and the multi-line `os.path.join` entries (`uniprot_proteomes_tools.json` at ~1142 and `open_meteo_airquality_tools.json` at ~1154) that a single-line regex would miss.
    - `src/tooluniverse/execute_function.py` lines 895-936 (`load_tools` signature) and 1263-1300, 1339 -- the `required_api_keys` exclusion `continue` and the `self.all_tools = dedup_all_tools` assignment that makes both codegen and discovery credential-sensitive.
    - `src/tooluniverse/tool_discovery_tools.py` lines 99-114, 220-228, 735-819 -- `grep_tools` and `GetToolInfoTool` reading `all_tool_dict`, and the gated-tool error shape `{"name": ..., "error": "requires API key(s) not set: ..."}`.
    - `src/tooluniverse/data/degrees_of_unsaturation_tools.json` -- the tracer tool's `name`, `type`, and parameter schema.
    - `.planning/phases/01-protected-sync-baseline/evidence/21945440c9f2a15537ba878500a800d9e330eab0/git.json` -- the `git.json` field shape to mirror (`pr161_ancestor`, `pr161_merge_oid`).
    - `.planning/phases/03-follow-up-and-catalog-reconciliation/03-PATTERNS.md` -- the per-file analog assignments and the evidence-artifact convention.
  </read_first>

  <files>scripts/audit_registration_chain.py</files>

  <action>
Create `scripts/audit_registration_chain.py` as a standalone runnable CLI script (no `scripts/__init__.py`), loading `publish_evidence`, `verify_checksums`, `_canonical_json`, and `_contains_secret` from `scripts/capture_sync_baseline.py` via `importlib.util.spec_from_file_location`, the same idiom `tests/unit/test_sync_baseline_git.py` uses. Load `extract_definition_names` from `scripts/audit_upstream_merge.py` the same way. Do not reimplement either.

Implement the six-link model as pure, individually testable functions over an explicit repository root, so Wave 2 can call the same functions across the full catalog without change:

- `load_definitions(data_dir, recursive)` -- returns a mapping of tool name to a sorted list of defining JSON paths, plus each entry's `type` and `required_api_keys`. Honor `tests/unit/test_registry_integrity.py`'s existing exclusion of `api_keys_catalog.json`. The `recursive` flag selects `*.json` versus `**/*.json`; Wave 2 needs the recursive form to see `data/broken_apis/`, and the non-recursive form is what the existing test uses.
- `load_live_categories(default_config_path)` -- returns two sets: category-to-JSON-path entries that are live, and entries that are commented out. Parse the file as text and accept the multi-line `os.path.join(...)` form; a single-line regex silently misses the `uniprot_proteomes` and `open_meteo_airquality` entries. Classify a commented-out entry that carries an archived marker in a nearby comment as intentionally excluded rather than missing.
- `check_link_definition`, `check_link_implementation`, `check_link_category`, `check_link_lazy_metadata`, `check_link_generated_module`, `check_link_tests` -- one function per link, each returning a small record with a boolean and the concrete evidence it used (the resolving path, the class name, the category key, or the matching import line). Link 2 resolves a JSON `type` to a defining class using `extract_definition_names` over the candidate `src/tooluniverse/*_tool.py` module, falling back to `tooluniverse.tool_registry._tool_registry` and `_lazy_registry_static.STATIC_LAZY_REGISTRY` exactly as the existing test does, and honoring that test's `special` allowance set (`BaseRESTTool`, `VisualizationTool`, `ClaudeCodeSkill`, `SpecialTool`). Link 5 checks both that `src/tooluniverse/tools/<Name>.py` exists at HEAD and that `tools/__init__.py` imports the name; read both from `git show HEAD:<path>` rather than from the working tree, because the working tree is dirty with another session's regeneration output. Link 6 checks whether the name is referenced anywhere under `tests/`.
- `classify_chain(links, gated)` -- the two-stage verdict function, shaped after `classify_finding`: primary verdict from the link booleans, then a second-stage recheck that downgrades a missing-link verdict to `gated` when the tool's `required_api_keys` are unmet in the running environment, so a credential gap is never reported as a broken chain and never as a clean pass. Verdicts: `intact`, `gated`, `archived`, `broken`. It must be pure over its arguments so it is unit-testable with no `tooluniverse` import.
- `audit_names(names, repo_root)` -- maps the link checks over a name collection and returns records sorted by tool name then source path. An empty `names` collection returns an empty record list and is not an error; a single name returns exactly one record.

Implement the SYNC-03 stage as `derive_pr161_ancestry(repo_root)`: run `git rev-parse HEAD` and `git merge-base --is-ancestor 16af425c053c306a658c96e254b4c4114338dd11 HEAD`, capture the exit code, and return `{head_oid, pr161_merge_oid, is_ancestor, exit_code, derived_at}` mirroring Phase 1's `git.json` field names (`pr161_ancestor`, `pr161_merge_oid`). Re-derive it live every run against the passed repository root; never read the value out of Phase 1's evidence file, out of RESEARCH.md, or out of this plan. Exit non-zero from `main` when `is_ancestor` is false, with a message naming D-02.

Implement `fingerprint_worktree(repo_root)`: run `git status --porcelain` scoped to the repository, record the sorted list of modified paths, the count, and a SHA256 digest over the newline-joined sorted list, plus a per-prefix rollup so the `src/tooluniverse/tools/` cluster is visible as its own count. Write it to `worktree_fingerprint.json`. This is a read-only observation; the script must never stage, restore, stash, clean, or commit anything.

Implement the discovery stage as `probe_discovery(tool_name)`: instantiate `ToolUniverse`, `load_tools(include_tools=[tool_name])`, call the `grep_tools` path via `find_tools_by_pattern(tool_name, search_in="name", case_sensitive=True)`, call `tool_specification(tool_name)` for the `get_tool_info` schema, and close the instance in a `finally` block. Record whether the returned specification carries a `parameter` or `parameters` object. Read `_excluded_api_key_tools` before concluding and, when the tool is gated, record the missing key names and the verdict `gated` rather than a failure. An empty result with no gating signal is a failure, not a pass -- this repository has a recorded history of empty-as-success passing a health gate.

Give `main(argv=None)` the flags `--tool` (single-name mode, the tracer path), `--out` (evidence directory), `--repo` (defaults to the current repository root), and `--json`. In single-name mode write `chain_tracer.json` carrying the tool name, the per-link records, the classify verdict, and the discovery stage result; write `git.json` and `worktree_fingerprint.json` alongside; then produce a sorted `SHA256SUMS` covering exactly the files under the output directory, in `shasum -a 256` output format so `shasum -a 256 -c SHA256SUMS` verifies from inside that directory. Serialize every JSON with the canonical-JSON helper and sorted keys so re-running at the same commit is byte-identical. Run the secret guard over every payload before writing; record key names only, never key values, and never a full tool response payload.

Write the output under `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/`. Do not write to the staging root and do not create or modify any sibling staging subdirectory -- plans 03-02 and 03-03 own those, and plan 03-04 regenerates one bundle-wide checksum set at publication.

Stage and commit only the two paths this task creates, enumerated explicitly on the command line: the script and the tracer evidence directory. Never use a whole-tree staging flag, and never revert or clean anything -- 2,663 files from another session's regeneration sit uncommitted in this checkout and their disposition belongs to plan 03-04's human checkpoint (D-06).

Format with `ruff format` using the repository `pyproject.toml` settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && git merge-base --is-ancestor 16af425c053c306a658c96e254b4c4114338dd11 HEAD && python3 scripts/audit_registration_chain.py --tool DegreesOfUnsaturation_calculate --out .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer --json && jq -e '.pr161_ancestor == true and .exit_code == 0 and (.head_oid | length) == 40 and .pr161_merge_oid == "16af425c053c306a658c96e254b4c4114338dd11"' .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/git.json && jq -e '.verdict == "intact" and ([.links[] | .ok] | all) and (.links | length) == 6 and .discovery.grep_found == true and .discovery.schema_has_parameters == true' .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/chain_tracer.json && jq -e '.count > 0 and (.digest | length) == 64' .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/worktree_fingerprint.json && (cd .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer && shasum -a 256 -c SHA256SUMS)</automated>
  </verify>

  <acceptance_criteria>
    - `python3 scripts/audit_registration_chain.py --tool DegreesOfUnsaturation_calculate --out <dir> --json` exits 0.
    - `jq -r '.pr161_ancestor' git.json` is `true`, `jq -r '.exit_code' git.json` is `0`, and `jq -r '.head_oid' git.json` equals the output of `git rev-parse HEAD` run at verification time -- proving re-derivation at the executing HEAD, not a copied Phase 1 value.
    - `jq -r '.links | length' chain_tracer.json` equals 6, every `links[].ok` is true, and every link record carries a non-empty evidence field naming the concrete path, class, category key, or import line it resolved.
    - `jq -r '.verdict' chain_tracer.json` is `intact`.
    - `jq -e '.discovery.grep_found and .discovery.schema_has_parameters and (.discovery.gated == false)' chain_tracer.json` is true -- the tracer tool is found by the `grep_tools` path and returns a real parameter schema, not a gated error object.
    - `jq -r '.count' worktree_fingerprint.json` is greater than 0, `jq -r '.paths | length' worktree_fingerprint.json` equals that count, and `jq -r '.digest' worktree_fingerprint.json` is a 64-character hex string.
    - `jq -r '.by_prefix["src/tooluniverse/tools/"]' worktree_fingerprint.json` is greater than 2000 -- the pre-existing regeneration cluster is captured, not silently absent.
    - Running the script twice at the same commit produces byte-identical `chain_tracer.json` and an identical `SHA256SUMS` (compare with `cmp`).
    - `shasum -a 256 -c SHA256SUMS` from inside the tracer evidence directory exits 0, and its file list covers every `*.json` in that directory and nothing outside it.
    - `git status --porcelain` after the task lists the same `src/tooluniverse/tools/` and `.tool_metadata.json` entries recorded in `worktree_fingerprint.json`, with no entry added or removed -- the pre-existing dirty state is neither committed nor reverted.
    - `git log -1 --name-only --pretty=format:` lists only `scripts/audit_registration_chain.py` and files under the tracer evidence directory.
    - Searching the tracer evidence directory for credential-shaped strings (a value assigned to a key-named field, or an `sk-` prefixed token) returns no match.
    - `ruff format --check scripts/audit_registration_chain.py` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Read-only against the repository plus two new files; deleting the script and the tracer evidence directory undoes it entirely.</reversibility>

  <done>PR #161's ancestry is re-derived at the live HEAD and recorded with its exit code; one credential-free tool is proven intact across all six registration links and is both findable by `grep_tools` and inspectable by `get_tool_info`; the hazardous dirty working tree is fingerprinted; and all three artifacts verify under a tracer-scoped `SHA256SUMS`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit coverage for the pure link-verdict and category-parsing logic</name>

  <precondition>`.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/worktree_fingerprint.json` exists and the current `git status --porcelain` path set matches its recorded `paths` array. A mismatch means the dirty working tree changed between tasks; stop and report rather than proceeding.</precondition>

  <read_first>
    - `scripts/audit_registration_chain.py` -- `classify_chain`, `load_live_categories`, `load_definitions`, and the six `check_link_*` functions exactly as written in Task 1.
    - `tests/unit/test_sync_baseline_git.py` lines 1-25 -- the `importlib.util.spec_from_file_location` loader block for testing a `scripts/` module.
    - `tests/unit/test_registry_integrity.py` lines 138-198 -- the established assertion-message convention in this repository's registry tests: collect into a `missing` list, then `assert not missing, "<message>:\n" + "\n".join(sorted(...))`, never a bare equality assert.
    - `pytest.ini` -- the default `addopts` selection (`-m "not slow and not require_api_keys and not network"`) this test must run under with no marker.
    - `src/tooluniverse/default_config.py` lines 755-770 and 1135-1160 -- the commented-out archived entry and the multi-line `os.path.join` entries the parser fixtures must reproduce.
  </read_first>

  <files>tests/unit/test_audit_registration_chain.py</files>

  <behavior>
    - All six links present and no unmet keys yields verdict `intact`.
    - A missing generated module or a missing `__init__` import, on a tool whose `required_api_keys` are unmet, yields `gated` and carries the missing key names -- not `broken`.
    - The same missing link on a tool with no `required_api_keys` yields `broken`.
    - A tool whose only defining JSON sits under an archived, commented-out category yields `archived`, not `broken`.
    - `load_live_categories` classifies a single-line category entry as live, a multi-line `os.path.join` entry spanning three lines as live, and a commented-out entry carrying an archived marker as intentionally excluded.
    - `audit_names` over an empty collection returns an empty list and does not raise; over a single name it returns exactly one record.
    - `audit_names` returns records in sorted order by tool name, and a definition mapping's per-name path lists are sorted, so two runs over the same input are byte-identical.
    - A discovery stage that returned an empty result with no gating signal is a failure, not a pass.
  </behavior>

  <action>
Create `tests/unit/test_audit_registration_chain.py`. Load the module under test with `importlib.util.spec_from_file_location("audit_registration_chain", Path(__file__).parents[2] / "scripts" / "audit_registration_chain.py")` and bind `classify_chain`, `load_live_categories`, `load_definitions`, and `audit_names`.

Build all fixtures by hand -- link-record dicts constructed in the test, and a `tmp_path` mini repository containing a small `default_config.py` fragment plus two or three `data/*.json` files. Do not import `tooluniverse` and do not instantiate `ToolUniverse`; the verdict and parsing logic must be provable without the package, exactly as plan 02-05 proved `assert_probe_contract`.

Write the `classify_chain` cases from `<behavior>` as a `pytest.mark.parametrize` verdict table. Give the credential-gated downgrade its own explicitly named test rather than folding it into the table: research proved that on this machine the same root cause (`load_tools` skipping tools with unmet `required_api_keys`) makes both codegen and discovery under-report, so a chain audit that reads a missing key as a broken chain would manufacture roughly eighty false findings in Wave 2.

Give the empty-result rejection its own named test as well, matching the named regression guard plan 02-05 established for the same failure mode.

Write a separate test for `load_live_categories` covering the three entry forms in `<behavior>`, with the multi-line fixture reproducing the three-line `os.path.join` shape used by the proteomes and air-quality categories -- a single-line regex passes the easy cases and silently drops these two, which is precisely how a live duplicate-name collision would be missed.

Write a determinism test asserting `audit_names` output ordering and the sortedness of the per-name defining-path lists.

Add no pytest marker; the file must run under `pytest.ini`'s default selection with no network, no credentials, and no heavy imports. Format with `ruff format` using the repository settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && .venv/bin/python -m pytest tests/unit/test_audit_registration_chain.py -q && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_audit_registration_chain.py -q` exits 0 with zero skipped tests.
    - `.venv/bin/python -m pytest tests/unit/test_audit_registration_chain.py --collect-only -q` lists a case for each of the four `classify_chain` verdicts, the three `load_live_categories` entry forms, the empty and single-name `audit_names` cases, and the ordering case.
    - A test node name references the credential-gated downgrade explicitly, and it passes.
    - A test node name references the empty-result rejection explicitly, and it passes.
    <!-- planner-discipline-allow: tooluniverse -->
    - The test file contains no package import: filter comment lines out before counting, so header prose cannot satisfy or invalidate the count -- `grep -v '^[[:space:]]*#' tests/unit/test_audit_registration_chain.py | grep -c -E '^[[:space:]]*(import|from)[[:space:]]+tooluniverse'` returns 0.
    - `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q` still exits 0 -- the pre-existing registry gate is unaffected.
    - `git status --porcelain` path set still matches `worktree_fingerprint.json`'s `paths` array.
    - `ruff format --check tests/unit/test_audit_registration_chain.py` exits 0.
  </acceptance_criteria>

  <done>Every `classify_chain` verdict, every category-entry form, and the empty/single/ordering contracts of `audit_names` have passing unit coverage that runs in the default pytest lane without importing the package.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Complete phase-wide list of symbols and paths **first created by Phase 3** (plans 03-01 through 03-04). The plan-review source-grounding pass should treat every entry here as new, not as drift against existing code.

**Scripts and their public symbols**

- `scripts/audit_registration_chain.py` (created by 03-01, extended by 03-02 and 03-04) -- `load_definitions`, `load_live_categories`, `check_link_definition`, `check_link_implementation`, `check_link_category`, `check_link_lazy_metadata`, `check_link_generated_module`, `check_link_tests`, `classify_chain`, `audit_names`, `derive_pr161_ancestry`, `fingerprint_worktree`, `probe_discovery`, `tier1_scope`, `run_full_audit`, `compare_import_sets`, `run_regeneration_guard`, `main`; CLI flags `--tool`, `--out`, `--repo`, `--json`, `--tier1`, `--tier2`, `--baseline`, `--regen-guard`, `--regen-scratch`, `--with-env`.
- `scripts/smoke_discovery_sample.py` (created by 03-03) -- `DISCOVERY_SAMPLE`, `select_sample`, `probe_grep_tools`, `probe_get_tool_info`, `classify_discovery`, `run_discovery_suite`, `main`; CLI flags `--out`, `--json`, `--tool`.

**Tests and fixtures**

- `tests/unit/test_audit_registration_chain.py` (03-01)
- `tests/unit/test_smoke_discovery_sample.py` (03-03)
- `tests/unit/registration_chain_baseline.json` (03-02) -- reviewed known-drift baseline
- New test functions added to the existing `tests/unit/test_registry_integrity.py` (03-02): `test_generated_module_exists_for_defined_names`, `test_defined_names_imported_in_tools_init`, `test_no_new_registration_chain_drift`, `test_baseline_has_no_stale_entries`, `test_no_duplicate_names_across_live_categories`

**Evidence artifacts**

- `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/{git.json,worktree_fingerprint.json,chain_tracer.json,SHA256SUMS}` (03-01)
- `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/{registration_chain.json,duplicates.json,SHA256SUMS}` (03-02)
- `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery/{discovery.json,SHA256SUMS}` (03-03)
- `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/regeneration_guard.json` (03-04)
- `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/<HEAD-oid>/` published bundle with one bundle-wide `SHA256SUMS` (03-04)
- `.planning/phases/03-follow-up-and-catalog-reconciliation/03-FINDINGS.md` (03-04)

Symbols first created by **this plan**: everything listed above under `scripts/audit_registration_chain.py` except `tier1_scope`, `run_full_audit`, `compare_import_sets`, `run_regeneration_guard` and the flags `--tier1`, `--tier2`, `--baseline`, `--regen-guard`, `--regen-scratch`, `--with-env`; plus `tests/unit/test_audit_registration_chain.py` and the four tracer evidence files.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared working tree -> git index | 2,663 uncommitted files from another session sit under every commit this phase makes |
| `data/*.json` definitions -> dynamic class resolution | JSON `type` strings drive module import and class lookup during link-2 resolution |
| `ToolUniverse` runtime environment -> audit verdicts | Which tools load depends on ambient credentials, so a verdict can silently encode the environment rather than the catalog |
| audit results -> committed evidence bundle | Tool metadata and environment state are written into a committed artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | git index vs. the pre-existing dirty working tree | critical | mitigate | `fingerprint_worktree` records the sorted dirty-path set, count, and digest before any other Phase 3 task runs; every task in every plan carries an acceptance criterion asserting the set is unchanged, which catches both accidental inclusion in a commit and accidental reversion. Staging is by explicit path enumeration only. |
| T-03-02 | Tampering | credential-gated `load_tools` filtering | high | mitigate | `classify_chain`'s second stage downgrades a missing-link verdict to `gated` with the missing key names when `required_api_keys` are unmet, so the environment cannot masquerade as catalog damage; the tracer tool is chosen credential-free so a red tracer is unambiguous. |
| T-03-03 | Spoofing | SYNC-03 evidence provenance | high | mitigate | `derive_pr161_ancestry` re-runs `git rev-parse` and `git merge-base --is-ancestor` live against the executing repository every run; an acceptance criterion compares the recorded `head_oid` against `git rev-parse HEAD` at verification time, so a value copied from Phase 1's evidence or from research prose cannot pass. |
| T-03-04 | Information Disclosure | tracer evidence JSON | high | mitigate | The secret guard from `scripts/capture_sync_baseline.py` runs over every payload before writing; only key *names* are recorded, never values, and no full tool response payload is stored. An acceptance criterion searches the evidence directory for credential-shaped strings. |
| T-03-05 | Repudiation | non-deterministic evidence | medium | mitigate | Canonical JSON with sorted keys plus sorted record ordering makes two runs at one commit byte-identical, and the sorted `SHA256SUMS` is verified with `shasum -a 256 -c`, so an evidence file cannot be silently edited after publication. |
| T-03-06 | Elevation of Privilege | dynamic import during link-2 and discovery resolution | medium | mitigate | Link 2 resolves classes by AST inspection (`extract_definition_names`) rather than importing arbitrary modules; only the single named tracer tool is loaded at runtime, via `load_tools(include_tools=[...])`. |
| T-03-07 | Denial of Service | network calls from the discovery probe | low | accept | The tracer tool is fully offline and deterministic; exactly one tool is loaded and inspected. Live-provider validation is explicitly out of scope per REQUIREMENTS.md. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero packages; pytest, ruff, uv, and git are already present per RESEARCH.md's Environment Availability table. No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `git merge-base --is-ancestor 16af425c... HEAD` exits 0 and the result is recorded with the live HEAD OID (SYNC-03, D-01/D-02).
2. `DegreesOfUnsaturation_calculate` is `intact` across all six links, each with concrete resolving evidence (CAT-01).
3. The same tool is found by the `grep_tools` path and returns a real parameter schema from `get_tool_info` (CAT-02).
4. The dirty working tree is fingerprinted and unchanged after both tasks (D-06).
5. Unit coverage proves the gated downgrade, the archived classification, the empty/single/ordering contracts, and the three category-entry forms, without importing the package.
6. `shasum -a 256 -c SHA256SUMS` verifies inside the tracer evidence directory; re-running the script at the same commit is byte-identical.
</verification>

<success_criteria>
- The full Phase 3 spine -- ancestry, six links, discovery, evidence -- is proven on one path and committed before any horizontal expansion begins.
- SYNC-03 has re-derived, checksummed evidence at the executing HEAD rather than an inherited claim.
- The audit's link model, verdict tiers, and category parsing are unit-proven, so Wave 2 can scale them across ~2,900 names without re-litigating the model.
- The concurrent dirty working tree is a recorded, checkable fact rather than an ambient hazard.
</success_criteria>

<output>
Create `.planning/phases/03-follow-up-and-catalog-reconciliation/03-01-SUMMARY.md` when done.
</output>
