---
phase: 03-follow-up-and-catalog-reconciliation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/audit_registration_chain.py
  - tests/unit/test_registry_integrity.py
  - tests/unit/registration_chain_baseline.json
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/registration_chain.json
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/duplicates.json
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/SHA256SUMS
autonomous: true
requirements: [CAT-01, CAT-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The six-link audit runs at two tiers per D-03: Tier 1 over every tool touched by Phase 2's actual merge (the 22 paths git diff-tree --cc reports for f81448f2 plus the 213 both-sides data/*.json files recorded in Phase 2's union.json), and Tier 2 mechanically over the entire catalog, both emitting records on one shared verdict field so the two tiers stay joinable rather than requiring a re-derived comparison."
    - "A tool name defined in exactly one live (non-archived) data JSON file yields exactly one catalog entry; a name defined in two live files is emitted as an explicit collision finding carrying both source paths and both category keys, never silently merged and never resolved by whichever file loads last. A name whose second definition sits under an archived, commented-out category is classified as hygiene debt, not as a collision."
    - "The mechanical registration-chain check lives in tests/unit/test_registry_integrity.py rather than in a parallel script (D-04), extending it from the 2 links it covers today to all six, and it runs in pytest's default fast lane with no network, credential, or slow marker."
    - "The 279-name pre-existing gap between JSON-defined names and tools/__init__.py imports at HEAD is captured in a reviewed, checked-in baseline file, so any newly-introduced drift hard-fails while the recorded gap stays visible as an audit finding rather than being either ignored or turned into a permanently red gate."
    - "The baseline cannot silently become a dumping ground: a name that is listed in the baseline but is no longer drifting fails the suite with an instruction to prune it, so the recorded gap can only shrink deliberately."
  artifacts:
    - scripts/audit_registration_chain.py
    - tests/unit/test_registry_integrity.py
    - tests/unit/registration_chain_baseline.json
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/registration_chain.json
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/duplicates.json
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/SHA256SUMS
  key_links:
    - "Tier 1's scope is derived from Phase 2's own recorded artifacts -- git diff-tree --cc against the landed merge f81448f2 for the hand-resolved set, and the files array in Phase 2's union.json for the 213 both-sides definitions -- not from a hand-copied list in this plan, so the scope cannot drift from what Phase 2 actually resolved."
    - "The duplicate-name check must walk data/**/*.json recursively and cross-reference default_config.py's live category map; the existing _load_defined_tool_names glob is non-recursive and would miss data/broken_apis/ entirely, while a JSON-only comparison would misreport five archived copies as live collisions."
    - "Link 5 is read from git show HEAD:<path>, never from the working tree, because 2,660 generated modules and .tool_metadata.json are uncommitted in this checkout from another session's regeneration; reading the working tree would encode that stale state into the baseline."
  prohibitions:
    - "MUST NOT discard, revert, commit, stash, or overwrite the pre-existing uncommitted src/tooluniverse/tools/** and .tool_metadata.json working-tree state -- its disposition is a human decision routed to plan 03-04 (D-06), never an executor action."
    - "MUST NOT silently reconcile, delete, or pick a winner for a duplicate public tool name defined in two live categories; record it as a finding for plan 03-04's review gate, per D-05's higher-risk collision class and Phase 2's findings-first posture."
    - "MUST NOT report a credential-gated tool as a broken chain; classify it as gated with its missing key names."
    - "MUST NOT fork a parallel registry-integrity test module or script alongside tests/unit/test_registry_integrity.py; extend the existing file, per D-04."
---

<objective>
Expand the tracer's proven link model horizontally to the whole catalog: Tier 1 explicit six-link verification over every tool Phase 2's merge actually touched, Tier 2 a mechanical pass over all ~2,900 defined names, an archived-category-aware duplicate-name check, and the mechanical half of that work landed inside `tests/unit/test_registry_integrity.py` so it becomes a standing regression gate rather than a one-off audit (CAT-01, CAT-02, D-03, D-04).

Purpose: CONCERNS.md's "Generated and hand-maintained registries coexist" tech debt is exactly the failure this closes -- a tool can exist in configuration yet be undiscoverable, import under the wrong name, or expose a stale schema. Phase 2 proved content survived the merge; nothing yet proves the chain that makes that content reachable is complete.

Output: a two-tier audit report on one joinable verdict field, a duplicate/collision report separating live collisions from archived hygiene debt, a reviewed known-drift baseline, and four new test functions extending the canonical registry gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-CONTEXT.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-RESEARCH.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-PATTERNS.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-01-SUMMARY.md
@.planning/phases/02-upstream-main-integration/02-FINDINGS.md
@.planning/codebase/CONCERNS.md
</context>

<assumption_delta_decision>
No-change, as recorded in plan 03-01: the detector's `chosen`-family hit on "custom tool" labels provenance, not a newly-chosen representation displacing a default. Upstream and fork-authored tools already share one identity model (the `name` field in `src/tooluniverse/data/*.json`) and one loading path, and this plan audits both through the identical six-link check rather than introducing a second representation.
</assumption_delta_decision>

<flagged_assumptions>
Restated so this plan is self-contained. See plan 03-01 for the full 5 = 3 covered + 2 flagged edge-probe accounting.

| # | Assumption | Status |
|---|---|---|
| EC-CAT-01-adjacency | Edge probe asked, for CAT-01: "when two things are exactly equal or just touch, do they merge, collide, or separate?" | RESOLVED HERE (`covered`) -- the second `must_haves.truths` entry is the acceptance criterion, and Task 3 is its mechanical check. |
| EC-CAT-02 | Edge probe returned `unclassified` / `unresolved` for CAT-02. | UNRESOLVED (flagged) -- plan 03-03 owns CAT-02's sample edges; this plan covers CAT-02's duplicate/stale half only. |
| A3 (RESEARCH.md) | Whether the `OxO_*` duplicate is hygiene debt or a genuine D-05 collision requiring a human gate. | RESOLVED HERE, deliberately -- **hygiene debt**. `default_config.py` line 765 comments out the `oxo` category with an `# Archived at:` marker, so only one copy is ever loadable and no runtime collision exists. The archived-aware rule in Task 3 encodes this, and the human gate is reserved for genuine both-copies-live collisions. Two such collisions were measured during planning and are named below -- the checkpoint now covers cases research had not already resolved, rather than re-litigating one it had. |
| Measured duplicate set | Seven names are defined in more than one `data/**/*.json` file at HEAD. Five are archived copies (`HMDB_get_metabolite`, `HMDB_search`, `HMDB_get_diseases` in `broken_apis/hmdb_tools.json`; `OxO_get_ontology_mappings`, `OxO_search_ontology_mappings` in `broken_apis/oxo_tools.json`). Two are defined in **two live categories each** and are genuine D-05 collisions: `UniProt_get_proteome` (`uniprot_tools.json` via category `uniprot`, and `uniprot_proteomes_tools.json` via a live multi-line category entry) and `OpenMeteo_get_air_quality` (`open_meteo_tools.json` via category `open_meteo`, and `open_meteo_airquality_tools.json` via a live multi-line category entry). | RECORDED -- Task 3 must reproduce this classification mechanically, not hard-code it; the two live collisions become findings for plan 03-04's review gate. |
| Measured baseline drift | 279 names defined in `src/tooluniverse/data/*.json` (excluding `skill:`-prefixed entries) have no import in the committed `tools/__init__.py`; 0 imported names are undefined. | RECORDED -- Task 2 turns this into a reviewed baseline file so the new gate hard-fails on new drift without going red on a pre-existing condition. |
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Two-tier full-catalog six-link audit on one joinable verdict field</name>

  <precondition>The current `git status --porcelain` path set matches the `paths` array recorded in `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/worktree_fingerprint.json` by plan 03-01. A mismatch means the pre-existing dirty working tree changed; stop and report rather than proceeding.</precondition>

  <read_first>
    - `scripts/audit_registration_chain.py` -- the six `check_link_*` functions, `classify_chain`, `audit_names`, `load_definitions`, and `load_live_categories` exactly as plan 03-01 wrote them. This task adds scope selection and reporting on top; it must not change the link model those functions implement.
    - `.planning/phases/03-follow-up-and-catalog-reconciliation/03-01-SUMMARY.md` -- what the tracer actually proved and any deviation from this plan's assumptions.
    - `.planning/phases/02-upstream-main-integration/evidence/a4d3d95a096a14ce4d147faa20334d24f8db9f9a/union.json` -- keys `files` (the 213 both-sides `data/*.json` entries that define half of Tier 1's scope), `both_sides_total`, `base_oid`, `fork_oid`, `upstream_oid`, `merged_oid`.
    - `.planning/phases/02-upstream-main-integration/02-FINDINGS.md` -- the four trees in play and the disagreement classification table; Tier 1's other half is the hand-resolved set that `git diff-tree --cc f81448f2047a6f35bd552956a0d9990019a39eb1 --name-only` reports.
    - `scripts/audit_upstream_merge.py` -- `classify_finding` (from ~1523) for the two-stage verdict shape, and `scripts/forensic_trace_findings.py` for the "a stage-only name may be renamed rather than absent" discipline that keeps a rename from being reported as a break.
    - `scripts/capture_sync_baseline.py` -- `publish_evidence`, `verify_checksums`, `_canonical_json`, `_contains_secret`.
  </read_first>

  <files>scripts/audit_registration_chain.py</files>

  <action>
Extend `scripts/audit_registration_chain.py` with tier selection and full-catalog reporting. Reuse the link functions plan 03-01 wrote unchanged; this task adds scope, aggregation, and output.

Add `tier1_scope(repo_root, union_json_path)`: derive Tier 1's file scope from Phase 2's own recorded artifacts rather than from any list typed into a plan. Run `git diff-tree --cc f81448f2047a6f35bd552956a0d9990019a39eb1 --name-only` for the hand-resolved paths, and read the `files` array from Phase 2's `union.json` for the both-sides `data/*.json` paths. Return the union of those paths plus, for each `data/*.json` path in scope, every tool name it defines. Record the two source counts separately in the output so a reader can see that Tier 1 covers what Phase 2 actually resolved. Fail loudly if either source yields zero paths; a silently empty Tier 1 would look green while proving nothing.

Add `run_full_audit(repo_root, tier)`: for `tier1`, run `audit_names` over the names `tier1_scope` resolved; for `tier2`, run it over every name from `load_definitions(recursive=True)`. Both tiers emit records with the same schema and the same `verdict` field produced by `classify_chain`, so the two tiers join on tool name without any downstream re-derivation -- this follows Phase 2's `join_preservation` precedent, where consulting the recorded verdict rather than re-deriving a comparison was what stopped the same noise being misattributed twice.

Exclude from both tiers the entries that are not Python tool modules at all: names containing a colon (the `skill:`-prefixed catalog entries; 126 of them at HEAD). Record the excluded count and the exclusion rule in the report rather than dropping them silently.

Write `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/registration_chain.json` containing: the HEAD OID the audit ran against; a `tier1` object with its derived scope (both source counts plus the resolved path and name counts) and its records; a `tier2` object with its records; a `summary` rollup of verdict counts per tier (`intact`, `gated`, `archived`, `broken`); and the exclusion rule with its count. Sort records by tool name and serialize with the canonical-JSON helper so re-running at one commit is byte-identical. Run the secret guard before writing.

Write a `chain`-scoped sorted `SHA256SUMS` covering exactly the files in that directory, in `shasum -a 256` output format. Do not touch the staging root, the `tracer/` directory, or a `discovery/` directory -- plan 03-01 owns `tracer/`, plan 03-03 runs concurrently in this wave and owns `discovery/`, and plan 03-04 regenerates one bundle-wide checksum set at publication.

Add CLI flags `--tier1` and `--tier2` (both may be given in one run) alongside the existing `--tool`, `--out`, `--repo`, and `--json`. Exit non-zero only on an execution error, never on a `broken` or `gated` verdict count: this is a findings-first audit, and the review gate for what it finds is plan 03-04's checkpoint, per Phase 2's D-06 posture.

Stage and commit only the script and the `chain/` evidence directory, enumerated explicitly on the command line. Never use a whole-tree staging flag and never revert or clean anything.

Format with `ruff format` using the repository `pyproject.toml` settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && python3 scripts/audit_registration_chain.py --tier1 --tier2 --out .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain --json && jq -e '.head_oid == (input_line_number|tostring|.="'"$(git rev-parse HEAD)"'") or (.head_oid == "'"$(git rev-parse HEAD)"'")' .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/registration_chain.json && jq -e '(.tier1.scope.union_files | length) == 213 and (.tier1.scope.hand_resolved_files | length) >= 20 and (.tier1.records | length) > 0 and (.tier2.records | length) > 2000 and ([.tier1.records[], .tier2.records[]] | map(.verdict) | map(. == "intact" or . == "gated" or . == "archived" or . == "broken") | all) and (.exclusions.rule | length) > 0' .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/registration_chain.json && (cd .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain && shasum -a 256 -c SHA256SUMS)</automated>
  </verify>

  <acceptance_criteria>
    - `python3 scripts/audit_registration_chain.py --tier1 --tier2 --out <dir> --json` exits 0.
    - `jq -r '.head_oid' registration_chain.json` equals `git rev-parse HEAD` at verification time.
    - `jq -r '.tier1.scope.union_files | length' registration_chain.json` equals 213, matching `both_sides_total`-derived `files` array length in Phase 2's `union.json`, and `jq -r '.tier1.scope.hand_resolved_files | length'` is at least 20 and equals the line count of `git diff-tree --cc f81448f2047a6f35bd552956a0d9990019a39eb1 --name-only` minus its commit header line.
    - `jq -r '.tier1.records | length' registration_chain.json` is greater than 0 and every record carries all six link results plus a `verdict`.
    - `jq -r '.tier2.records | length' registration_chain.json` is greater than 2000 -- the mechanical pass genuinely covers the catalog rather than a subset.
    - Every record's `verdict` is one of `intact`, `gated`, `archived`, `broken`; there is no fifth value and no null.
    - `jq -e '[.tier1.records[] | .name] as $t1 | [.tier2.records[] | select(.name | IN($t1[])) | .name] | length == ($t1 | length)' registration_chain.json` is true -- every Tier 1 name also appears in Tier 2, proving the two tiers join on one verdict field rather than diverging.
    - `jq -r '.exclusions.count' registration_chain.json` equals the number of defined names containing a colon (126 at HEAD), and `jq -r '.exclusions.rule'` is a non-empty description rather than an empty string.
    - `jq -e '[.tier2.records[] | select(.verdict == "broken") | select(.links.generated_module.source | test("^git show HEAD:"))] | length == [.tier2.records[] | select(.verdict == "broken")] | length' registration_chain.json` is true, or each `broken` record otherwise records that link 5 was read from the committed tree rather than the working tree.
    - Running the command twice at the same commit produces a byte-identical `registration_chain.json` (compare with `cmp`).
    - `shasum -a 256 -c SHA256SUMS` from inside the `chain/` directory exits 0, and `git diff --name-only -- .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/` shows no change from this plan.
    - `git status --porcelain` path set still matches the tracer fingerprint's `paths` array.
    - `ruff format --check scripts/audit_registration_chain.py` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Additive functions plus a new evidence directory; reverting the commit removes both with no runtime effect.</reversibility>

  <done>Both tiers run from Phase 2's own recorded scope artifacts, emit records on one shared verdict field, join on tool name, and verify under a `chain`-scoped `SHA256SUMS`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the canonical registry gate to links 5 and 6 against a reviewed drift baseline</name>

  <precondition>The current `git status --porcelain` path set matches the `paths` array in the tracer's `worktree_fingerprint.json`, and `evidence/staging/chain/registration_chain.json` from Task 1 exists -- the baseline file is generated from its Tier 2 records, not hand-written.</precondition>

  <read_first>
    - `tests/unit/test_registry_integrity.py` -- the whole file (198 lines). Specifically `_load_defined_tool_names` (34-50), `_load_type_names` (53-69), `_load_lazy_registry_class_names` (72-77), and the `TestRegistryIntegrity` class (138-197) whose assertion convention every new test must follow: collect into a `missing` list, then `assert not missing, "<message>:\n" + "\n".join(sorted(...))`.
    - `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/registration_chain.json` -- Task 1's Tier 2 records, the source for the baseline file.
    - `pytest.ini` -- the default `addopts` selection these tests must run under with no marker.
    - `.planning/phases/03-follow-up-and-catalog-reconciliation/03-RESEARCH.md` § "Common Pitfalls" Pitfall 4 -- `RULES_DIR = REPO / "claude" / "rules"` has no leading dot and is correct for this repository; do not "fix" it while editing the file.
  </read_first>

  <files>tests/unit/test_registry_integrity.py, tests/unit/registration_chain_baseline.json</files>

  <behavior>
    - `test_generated_module_exists_for_defined_names`: every JSON-defined name that is not in the baseline and is not colon-prefixed has a `src/tooluniverse/tools/<Name>.py` file in the committed tree.
    - `test_defined_names_imported_in_tools_init`: every such name has a matching import line in the committed `tools/__init__.py`.
    - `test_no_new_registration_chain_drift`: the set of drifting names minus the baseline is empty; a newly-introduced drift fails with the offending names listed.
    - `test_baseline_has_no_stale_entries`: the baseline minus the currently-drifting set is empty; a name that no longer drifts fails with an instruction to prune it from the baseline.
    - The baseline file is valid JSON with a documented shape: a `generated_at_oid`, a `rationale` string, and a sorted `names` array with no duplicates.
    - All four tests run under the default pytest selection with no `network`, `require_api_keys`, or `slow` marker.
  </behavior>

  <action>
Generate `tests/unit/registration_chain_baseline.json` from Task 1's Tier 2 records: the sorted, de-duplicated set of names whose link-5 checks fail at the current HEAD (measured at 279 during planning, excluding the 126 colon-prefixed catalog entries). Include `generated_at_oid` set to the HEAD the audit ran against and a `rationale` string stating plainly that this is a pre-existing gap recorded at Phase 3 planning time, not damage introduced by the Phase 2 merge, and that the gate's purpose is to prevent new drift while the recorded gap is triaged. Review the generated list before committing: spot-check that a sample of entries genuinely have no module file in the committed tree, so the baseline records a real condition rather than a bug in the check.

Extend `tests/unit/test_registry_integrity.py` in place -- do not create a second registry-integrity module (D-04). Add module-level helpers alongside the existing ones, following their exact shape:

- A helper returning the committed `tools/__init__.py` import-name set, read via `git show HEAD:src/tooluniverse/tools/__init__.py` rather than from the working tree. State the reason in the helper's docstring: this checkout carries thousands of uncommitted generated modules from a concurrent session, so reading the working tree would encode that stale state into the gate.
- A helper returning the committed `src/tooluniverse/tools/` module-file name set, read the same way.
- A helper loading the baseline JSON and returning its `names` as a set.
- A helper returning the currently-drifting name set: defined names, minus colon-containing names, minus names with both a module file and an import.

Add the four test functions from `<behavior>` to the existing `TestRegistryIntegrity` class, each using the file's established collect-then-`assert not missing` message convention so a failure prints a readable, sorted list rather than a set-difference dump. Give `test_no_new_registration_chain_drift`'s failure message an explicit instruction: a new name here means either a definition was added without regenerating its module, or a module was removed without removing its definition.

Give `test_baseline_has_no_stale_entries`'s failure message an explicit instruction to prune the named entries from the baseline file, so the baseline can only shrink deliberately and cannot become a permanent dumping ground.

Add no pytest marker to any new test; all four are static file-and-name-set comparisons that need no network, no credentials, and no `load_tools` call. Leave the four existing tests unchanged.

Stage and commit only the test file and the baseline file, enumerated explicitly. Format with `ruff format` using the repository settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q && jq -e '(.names | length) > 0 and (.names == (.names | sort)) and ((.names | unique | length) == (.names | length)) and (.generated_at_oid | length) == 40 and (.rationale | length) > 40' tests/unit/registration_chain_baseline.json</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q` exits 0 with zero skipped tests.
    - `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py --collect-only -q` lists the four pre-existing tests plus exactly the four new ones named in `<behavior>`.
    - `jq -r '.names | length' tests/unit/registration_chain_baseline.json` is greater than 0 and equals the number of Tier 2 records in `registration_chain.json` whose link-5 checks fail and whose name contains no colon.
    - `jq -e '.names == (.names | sort) and ((.names | unique | length) == (.names | length))' tests/unit/registration_chain_baseline.json` is true.
    - `jq -r '.generated_at_oid' tests/unit/registration_chain_baseline.json` equals `git rev-parse HEAD` at generation time, and `.rationale` is a non-empty sentence naming the gap as pre-existing.
    - Temporarily appending an invented name to the baseline's `names` array makes `test_baseline_has_no_stale_entries` fail; restoring the file makes the suite green again -- proving the stale-entry gate has teeth rather than passing vacuously.
    - The four existing tests in the file are unchanged: `git diff -U0 tests/unit/test_registry_integrity.py` shows additions only within the helper block and the test class, with no deletion of an existing test body.
    - `.venv/bin/python -m pytest tests/unit/ -q -k "registry or audit_registration"` exits 0.
    - `git status --porcelain` path set still matches the tracer fingerprint's `paths` array.
    - `ruff format --check tests/unit/test_registry_integrity.py` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Additive test functions plus one new fixture file; reverting removes the gate with no production-code effect.</reversibility>

  <done>Links 5 and 6 are covered by the canonical registry gate, the 279-name pre-existing gap is a reviewed checked-in baseline rather than an invisible condition or a permanently red test, and the baseline is itself gated against becoming stale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Archived-aware duplicate-name check separating live collisions from hygiene debt</name>

  <precondition>The current `git status --porcelain` path set matches the `paths` array in the tracer's `worktree_fingerprint.json`.</precondition>

  <read_first>
    - `scripts/audit_registration_chain.py` -- `load_definitions` (with its `recursive` flag) and `load_live_categories` as written in plan 03-01, including the multi-line `os.path.join` handling and the archived-marker classification.
    - `src/tooluniverse/default_config.py` -- the category map. Confirm at minimum: `uniprot` at ~line 125 and the multi-line `uniprot_proteomes_tools.json` entry at ~1142; `open_meteo` at ~1010 and the multi-line `open_meteo_airquality_tools.json` entry at ~1154; `metabolite` at ~518; the commented-out `oxo` entry with its archived marker at ~763-765.
    - `src/tooluniverse/data/oxo_tools.json` and `src/tooluniverse/data/broken_apis/oxo_tools.json` -- the archived-duplicate shape.
    - `.planning/phases/03-follow-up-and-catalog-reconciliation/03-RESEARCH.md` § Pitfall 3 -- why a JSON-only comparison misclassifies archived copies as collisions.
    - `tests/unit/test_registry_integrity.py` as extended by Task 2 -- the helper block and assertion convention the new duplicate test must follow.
  </read_first>

  <files>scripts/audit_registration_chain.py, tests/unit/test_registry_integrity.py</files>

  <behavior>
    - A name defined once in one live category is not reported.
    - A name defined in two files where only one file's category is live is classified `archived_duplicate` (hygiene debt), not a collision.
    - A name defined in two files whose categories are both live is classified `live_collision`.
    - The classifier finds the live category for a multi-line `os.path.join` entry, not only for a single-line entry.
    - `test_no_duplicate_names_across_live_categories` fails, with both source paths and both category keys named, when a live collision exists, and passes when every duplicate is archived.
  </behavior>

  <action>
Add `find_duplicate_names(repo_root)` to `scripts/audit_registration_chain.py`. Walk `src/tooluniverse/data/**/*.json` recursively via `load_definitions(recursive=True)` -- the existing non-recursive glob in `tests/unit/test_registry_integrity.py` cannot see `data/broken_apis/` at all -- and for every name defined in more than one file, resolve each defining file against `load_live_categories`'s live and commented-out sets. Emit one record per duplicated name carrying the name, every defining path, the category key each path maps to, whether that category is live, and a `class` of `live_collision` or `archived_duplicate`.

Write `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/duplicates.json` with those records sorted by name, plus a summary of counts per class. Regenerate the `chain`-scoped `SHA256SUMS` so it covers both `registration_chain.json` and `duplicates.json`. Record findings only: do not delete an orphaned definition, do not comment out a category, and do not pick a winner for a live collision -- D-05 routes a genuine collision to review, and plan 03-04 owns that gate.

The classification must be derived mechanically from the two files, never hard-coded from this plan's text. Planning measured seven duplicated names at HEAD: five `archived_duplicate` (`HMDB_get_metabolite`, `HMDB_search`, `HMDB_get_diseases`, `OxO_get_ontology_mappings`, `OxO_search_ontology_mappings`) and two `live_collision` (`UniProt_get_proteome`, `OpenMeteo_get_air_quality`). Those counts are the expected result used to verify the classifier, not an input to it.

Then add `test_no_duplicate_names_across_live_categories` to `tests/unit/test_registry_integrity.py`'s `TestRegistryIntegrity` class. It reimplements nothing: import the classifier from the script with the same `importlib.util.spec_from_file_location` idiom the repository already uses for `scripts/` modules, filter to `live_collision`, and assert with the file's established collect-then-`assert not missing` convention, listing each collision's name, both paths, and both category keys.

Because two live collisions exist at HEAD, this test would be red on arrival. Do not weaken the test and do not fix the collisions here -- both are D-05 review decisions. Instead mark it `pytest.mark.xfail(strict=True)` with a reason naming the two collisions and pointing at plan 03-04's review gate, so the suite stays green, the finding stays loud in the test output, and the marker fails the build the moment the collisions are resolved and the `xfail` becomes obsolete. Record in the summary line of `duplicates.json` that this test is currently `xfail` and why.

Stage and commit only the script, the test file, and the `chain/` evidence directory, enumerated explicitly. Format both files with `ruff format` using the repository settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && python3 scripts/audit_registration_chain.py --tier2 --out .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain --json && jq -e '(.records | length) == 7 and ([.records[] | select(.class == "live_collision") | .name] | sort == ["OpenMeteo_get_air_quality","UniProt_get_proteome"]) and ([.records[] | select(.class == "archived_duplicate")] | length) == 5' .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/duplicates.json && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q -rx && (cd .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain && shasum -a 256 -c SHA256SUMS)</automated>
  </verify>

  <acceptance_criteria>
    - `jq -r '.records | length' duplicates.json` equals 7, matching the duplicate count measured during planning.
    - `jq -r '[.records[] | select(.class == "live_collision") | .name] | sort | join(",")' duplicates.json` equals `OpenMeteo_get_air_quality,UniProt_get_proteome`, and each of those records names two defining paths and two distinct live category keys.
    - `jq -r '[.records[] | select(.class == "archived_duplicate")] | length' duplicates.json` equals 5, and each of those records has exactly one live category key and at least one path under `data/broken_apis/`.
    - Every record's `class` is `live_collision` or `archived_duplicate`; there is no third value and no null.
    - `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q -rx` exits 0, and the report section shows `test_no_duplicate_names_across_live_categories` as `xfail` with a reason naming both collisions and plan 03-04's review gate.
    - The `xfail` is strict: `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q --runxfail` reports that test as a failure listing both collisions, proving it is a real red finding held open rather than a disabled test.
    - No `data/*.json` file, no `default_config.py` line, and no collision is modified by this task: `git diff --name-only -- src/tooluniverse/` shows no path outside the pre-existing dirty set recorded in the tracer fingerprint.
    - `shasum -a 256 -c SHA256SUMS` from inside the `chain/` directory exits 0 and its file list covers both `registration_chain.json` and `duplicates.json`.
    - `git status --porcelain` path set still matches the tracer fingerprint's `paths` array.
    - `ruff format --check scripts/audit_registration_chain.py tests/unit/test_registry_integrity.py` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Additive classifier, additive test, and one new evidence file; nothing in the catalog is modified.</reversibility>

  <done>Duplicate names are classified mechanically against live category wiring, the five archived copies are separated from the two genuine live collisions, and the collisions are held open as a strict `xfail` finding routed to plan 03-04's review gate rather than silently reconciled.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
See `03-01-PLAN.md` section "Artifacts this phase produces" for the complete phase-wide list. Symbols and paths first created by **this** plan:

- `scripts/audit_registration_chain.py` -- `tier1_scope`, `run_full_audit`, `find_duplicate_names`; CLI flags `--tier1`, `--tier2`.
- `tests/unit/test_registry_integrity.py` -- new helpers for the committed-tree import set, the committed-tree module-file set, the baseline loader, and the drifting-name set; new tests `test_generated_module_exists_for_defined_names`, `test_defined_names_imported_in_tools_init`, `test_no_new_registration_chain_drift`, `test_baseline_has_no_stale_entries`, `test_no_duplicate_names_across_live_categories`.
- `tests/unit/registration_chain_baseline.json` -- keys `generated_at_oid`, `rationale`, `names`.
- `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/chain/{registration_chain.json,duplicates.json,SHA256SUMS}`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared working tree -> git index | 2,663 uncommitted files from another session sit under every commit this plan makes |
| `data/**/*.json` on disk -> the loadable catalog | Presence on disk is not the same as being wired into `default_config.py`; the gap between them is where a false finding is manufactured |
| committed tree vs. working tree for links 5 and 6 | Reading generated modules from the dirty working tree would encode another session's stale regeneration into a committed baseline |
| duplicate-name findings -> catalog mutation | A collision resolved automatically silently picks a winner between two live tool definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | duplicate public tool names across two live categories | high | mitigate | `find_duplicate_names` records findings only. A `live_collision` is never auto-resolved, never deleted, and never has a winner picked; it is held open as a strict `xfail` naming both source paths and both category keys, and routed to plan 03-04's D-05 review gate. |
| T-03-09 | Tampering | baseline file as a silent suppression channel | high | mitigate | `test_baseline_has_no_stale_entries` fails when a baseline entry no longer drifts, so the baseline can only shrink deliberately; an acceptance criterion proves the gate has teeth by appending an invented name and observing the failure. |
| T-03-10 | Spoofing | link-5 evidence sourced from the dirty working tree | high | mitigate | The committed-tree helpers read `git show HEAD:<path>` and state the reason in their docstrings; an acceptance criterion requires `broken` records to record that link 5 was read from the committed tree. |
| T-03-11 | Tampering | Tier 1 scope drifting from what Phase 2 actually resolved | medium | mitigate | `tier1_scope` derives scope from `git diff-tree --cc` against the landed merge and from Phase 2's recorded `union.json`, fails loudly on an empty result, and records both source counts in the report; acceptance criteria pin the 213-file count and the hand-resolved count. |
| T-03-12 | Information Disclosure | audit evidence JSON | medium | mitigate | The secret guard from `scripts/capture_sync_baseline.py` runs over every payload before writing; records carry names, paths, category keys, and verdicts only -- never key values and never response payloads. |
| T-03-13 | Repudiation | non-deterministic audit output | medium | mitigate | Canonical JSON with sorted keys and name-sorted records makes two runs at one commit byte-identical, verified by an acceptance criterion using `cmp`, and the sorted `SHA256SUMS` is verified with `shasum -a 256 -c`. |
| T-03-14 | Denial of Service | full-catalog pass over ~2,900 names | low | accept | Every check is a static file or name-set comparison with no network and no `load_tools` call; the pass completes well inside the phase's 180s wave-gate budget. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero packages. No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
1. Tier 1 covers exactly what Phase 2 resolved -- 213 both-sides files from `union.json` plus the `git diff-tree --cc` hand-resolved set -- with both counts recorded.
2. Tier 2 covers more than 2,000 catalog names on the same verdict schema, and every Tier 1 name joins into Tier 2.
3. Links 5 and 6 are gated in `tests/unit/test_registry_integrity.py` against a reviewed baseline that itself cannot go stale.
4. Duplicate names are classified against live category wiring: five archived copies, two live collisions, both collisions held open as a strict `xfail`.
5. Nothing in `src/tooluniverse/` is modified, and the pre-existing dirty path set is byte-identical to the tracer fingerprint.
6. `shasum -a 256 -c SHA256SUMS` verifies inside `chain/`, and re-running the audit at one commit is byte-identical.
</verification>

<success_criteria>
- CAT-01 has two-tier evidence: explicit six-link verification over Phase 2's touched set, and a mechanical pass over the whole catalog, joinable on one verdict field.
- The mechanical half is a standing regression gate inside the canonical test module, not a one-off script run.
- CAT-02's duplicate/stale half is answered with a live-vs-archived distinction that cannot misreport an intentionally archived copy as a collision.
- Every genuine collision is a recorded, loud finding awaiting review -- none is silently reconciled.
</success_criteria>

<output>
Create `.planning/phases/03-follow-up-and-catalog-reconciliation/03-02-SUMMARY.md` when done.
</output>
