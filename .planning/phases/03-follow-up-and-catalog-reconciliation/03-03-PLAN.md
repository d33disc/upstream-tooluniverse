---
phase: 03-follow-up-and-catalog-reconciliation
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/smoke_discovery_sample.py
  - tests/unit/test_smoke_discovery_sample.py
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery/discovery.json
  - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery/SHA256SUMS
autonomous: true
requirements: [CAT-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A recorded, justified sample containing both new-upstream tools and preserved fork-custom tools is located by grep_tools and returns a real parameter schema from get_tool_info against the loaded post-Phase-2 catalog -- the actual discovery primitives, not a re-derived file-presence check."
    - "The sample is selected mechanically from Phase 2's own recorded artifacts (the hand-resolved data JSON files and the both-sides union) plus one credential-free offline control, and the selection rule and its exclusions are written into the evidence artifact so a reviewer can see what was covered and what was not."
    - "A tool whose required_api_keys are unmet in the execution environment is excluded from the schema-inspection sample and recorded separately as gated with its missing key names -- never counted as a discovery failure and never counted as a pass. Research proved grep_tools and get_tool_info read the same credential-gated collection the codegen scripts read, so sampling a gated tool would fail CAT-02's own criterion for a reason unrelated to catalog integrity."
    - "get_tool_info returning the graceful gated error object instead of a schema is recorded as gated, and an empty result with no gating signal is recorded as a failure, not a pass."
  artifacts:
    - scripts/smoke_discovery_sample.py
    - tests/unit/test_smoke_discovery_sample.py
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery/discovery.json
    - .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery/SHA256SUMS
  key_links:
    - "The gated set is read from ToolUniverse._excluded_api_key_tools at runtime rather than from a hardcoded list of key families, so the sample adapts to whichever credentials the execution environment actually holds instead of encoding this one machine's configuration."
    - "grep_tools and get_tool_info both iterate ToolUniverse.all_tool_dict, the same collection load_tools populates after its required_api_keys skip; a tool missing from discovery is therefore either genuinely unregistered or credential-gated, and only the gated check distinguishes the two."
    - "This plan owns evidence/staging/discovery/ exclusively with a discovery-scoped SHA256SUMS; plan 03-02 runs concurrently in this wave and owns evidence/staging/chain/, and plan 03-04 regenerates one bundle-wide checksum set at publication."
  prohibitions:
    - "MUST NOT discard, revert, commit, stash, or overwrite the pre-existing uncommitted src/tooluniverse/tools/** and .tool_metadata.json working-tree state -- its disposition is a human decision routed to plan 03-04 (D-06), never an executor action."
    - "MUST NOT report a credential-gated tool as nonexistent or as a catalog-integrity failure; record it as gated with its missing key names so a reader can tell 'needs a key' from 'is not in the catalog'."
    - "MUST NOT certify discovery, inspection, or execution across the CLI, MCP stdio, MCP HTTP, and REST surfaces -- that is Phase 5 / SURF-01. This plan certifies the shared-core discovery primitives only, which is the boundary ROADMAP.md draws for Phase 3."
    - "MUST NOT write API key values, credential material, or full tool response payloads into the evidence bundle; record key names and result shape only."
---

<objective>
Certify CAT-02's user-facing half against the loaded post-Phase-2 catalog: a representative sample spanning new-upstream and preserved-custom tools is found by `grep_tools` and yields an exact schema from `get_tool_info`, with credential-gated tools excluded from the schema check and recorded separately rather than silently failing the criterion.

Purpose: file presence proves registration; only calling the discovery primitives proves reachability. Research established that both primitives read the same credential-filtered collection the codegen scripts read, so a naive sample would produce a red result on this machine for a reason that has nothing to do with the merge.

Output: `scripts/smoke_discovery_sample.py`, unit coverage for its pure selection and verdict logic, and `evidence/staging/discovery/discovery.json` recording the sample, its selection rule, its exclusions, and a per-tool result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-CONTEXT.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-RESEARCH.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-PATTERNS.md
@.planning/phases/03-follow-up-and-catalog-reconciliation/03-01-SUMMARY.md
@docs/dev_docs/Interaction_Surfaces.md
</context>

<assumption_delta_decision>
No-change, as recorded in plan 03-01: the detector's `chosen`-family hit on "custom tool" labels provenance rather than a newly-chosen representation displacing a default. This plan samples upstream-origin and fork-origin tools through the identical discovery primitives, which is the concrete expression of that single shared identity model.
</assumption_delta_decision>

<flagged_assumptions>
Restated so this plan is self-contained. See plan 03-01 for the full 5 = 3 covered + 2 flagged edge-probe accounting.

| # | Assumption | Status |
|---|---|---|
| EC-CAT-02 | Edge probe returned `unclassified` / `unresolved` for CAT-02; no explicit edge criterion and no backstop was auto-authored by the engine. | UNRESOLVED (flagged) -- carried openly. The one CAT-02 edge research proved is real on this machine (credential-gated tools cannot have their schema inspected here) is authored as a `backstop` truth in plan 03-01's `must_haves` and operationalized by this plan's gated-exclusion rule. No other CAT-02 edge is claimed as covered. |
| Gated-tool population | Research measured 80+ tools excluded by unmet `required_api_keys` across at least 8 key families on this machine, none of which are present in `.tooluniverse/.env.1password`, with no `.env.local` present at all. | CARRIED -- the script must read `_excluded_api_key_tools` at runtime rather than trusting this count, since a future run may have more or fewer credentials loaded. |
| Surface boundary | Phase 3 certifies catalog loading and the shared-core discovery primitives only; CLI, MCP stdio, MCP HTTP, and REST certification is Phase 5 / SURF-01 per ROADMAP.md and CONTEXT.md's Deferred Ideas. | RESOLVED HERE -- recorded as an explicit exclusion in the evidence artifact rather than left implied. |
</flagged_assumptions>

<sample_selection_rule>
Claude's Discretion per CONTEXT.md, fixed here and written into the artifact so it is reviewable. Applied in this order, all sources mechanical:

1. **Preserved fork-custom** -- tool names defined in the `data/*.json` files that `git diff-tree --cc f81448f2047a6f35bd552956a0d9990019a39eb1 --name-only` reports as hand-resolved (`literature_search_tools.json`, `uspto_tools.json`). These are the definitions Git's line merge could have damaged without raising a conflict marker, and Phase 2 plan 02-05 already probed the USPTO pair for execution; this plan proves the discovery half.
2. **New-upstream** -- tool names present in the upstream side of Phase 2's both-sides union (`union.json`'s `files` array) that were absent from the fork side, i.e. definitions the merge introduced.
3. **Offline control** -- `DegreesOfUnsaturation_calculate`, credential-free and deterministic, so a total sample failure can be distinguished from an environment problem. Same control plan 03-01's tracer and Phase 2 plan 02-05 used.

Cap each of pools 1 and 2 at five names, taken in sorted order so the sample is deterministic across runs. Any name appearing in `ToolUniverse._excluded_api_key_tools` is removed from the schema-inspection sample and recorded in a separate `gated` array with its missing key names; if removing gated names empties a pool, record that fact explicitly rather than substituting silently.
</sample_selection_rule>

<tasks>

<task type="auto">
  <name>Task 1: Discovery smoke suite over a mechanically selected, gated-aware representative sample</name>

  <precondition>The current `git status --porcelain` path set matches the `paths` array recorded in `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/worktree_fingerprint.json` by plan 03-01. A mismatch means the pre-existing dirty working tree changed; stop and report rather than proceeding.</precondition>

  <read_first>
    - `src/tooluniverse/tool_discovery_tools.py` -- lines 99-114 and 281-311 (`grep_tools` iterating `all_tool_dict`), 220-228 (the name-substring fallback consulting `_excluded_api_key_tools`), and 735-819 (`GetToolInfoTool`, `_not_found_error`, and the gated response shape `{"name": ..., "error": "requires API key(s) not set: ..."}`). This is the system under test; assert against its real shapes, not invented ones.
    - `src/tooluniverse/execute_function.py` -- lines 1263-1300 and 1339: the `required_api_keys` skip that populates `_excluded_api_key_tools` before `self.all_tools` is assigned, plus `find_tools_by_pattern`, `tool_specification`, and `close`.
    - `scripts/probe_custom_tools.py` -- Phase 2's probe harness: `assert_probe_contract`'s pass/gated/fail tiers and its explicit rejection of an empty result as success, and its `finally`-block `close`. Reuse the discipline; this plan probes discovery rather than execution.
    - `tests/unit/test_lazy_load_cache_consistency.py` -- `_SAFE_TOOL_NAMES` and `_SKIP_KEYWORDS`, the established pattern for choosing tool names that will not trigger GPU or model loading.
    - `scripts/capture_sync_baseline.py` -- `publish_evidence`, `verify_checksums`, `_canonical_json`, `_contains_secret`.
    - `.planning/phases/02-upstream-main-integration/evidence/a4d3d95a096a14ce4d147faa20334d24f8db9f9a/union.json` -- the `files` array and per-file name sets that define the new-upstream pool.
    - `docs/dev_docs/Interaction_Surfaces.md` section 2 -- the discovery layer CAT-02's success criteria are phrased against.
  </read_first>

  <files>scripts/smoke_discovery_sample.py</files>

  <action>
Create `scripts/smoke_discovery_sample.py` as a standalone runnable CLI script (no `scripts/__init__.py`), loading `publish_evidence`, `verify_checksums`, `_canonical_json`, and `_contains_secret` from `scripts/capture_sync_baseline.py` via `importlib.util.spec_from_file_location`, the same idiom the repository already uses for cross-script reuse.

Implement `select_sample(repo_root, excluded_api_key_tools)` as a pure function over its arguments, applying this plan's `<sample_selection_rule>` in order: resolve the hand-resolved `data/*.json` paths from `git diff-tree --cc` against the landed merge OID for the preserved-custom pool, resolve the upstream-introduced names from Phase 2's `union.json` for the new-upstream pool, add `DegreesOfUnsaturation_calculate` as the offline control, cap pools 1 and 2 at five names each in sorted order, and remove any name present in the passed gated mapping. Return the selected records -- each carrying `name`, `pool`, `selection_rule`, and `source_path` -- plus a separate list of the removed gated names with their missing key names, plus a record of any pool that was emptied by gating. Keep it pure so it is unit-testable with no package import.

Implement `probe_grep_tools(universe, tool_name)` calling `find_tools_by_pattern(tool_name, search_in="name", case_sensitive=True)` and recording whether the exact name is among the returned matches and how many matches came back.

Implement `probe_get_tool_info(universe, tool_name)` calling `tool_specification(tool_name)` and recording whether the returned object carries a `parameter` or `parameters` object, and separately whether it is the gated error object whose `error` value reports unset API keys. Record the parameter key names only -- never a full response payload.

Implement `classify_discovery(grep_result, info_result, gated_keys)` as a pure verdict function: `pass` when the exact name was found and a parameter schema came back; `gated` when the gated error object came back or the tool appears in the gated mapping, carrying the missing key names; `fail` when the name was not found, when no schema and no gating signal came back, or when an empty result arrived with no gating signal. An empty result must never classify as `pass` -- this repository has a recorded history of empty-as-success passing a health gate, and Phase 2 plan 02-05 added a named regression guard for the same failure mode.

Implement `run_discovery_suite(repo_root, out_dir)`: instantiate `ToolUniverse`, call `load_tools()` with no filter so the sample is exercised against the real loadable catalog, read `_excluded_api_key_tools`, call `select_sample`, run both probes per selected name, and close the instance in a `finally` block. Write `discovery.json` containing the loaded catalog size, the full sample records including selection rules and source paths, the gated list with missing key names, the emptied-pool record if any, the per-tool verdicts, counts of `passed` / `gated` / `failed`, and an explicit `exclusions` array naming the deferred surfaces (CLI, MCP stdio, MCP HTTP, REST -> Phase 5 / SURF-01) and the credential-gated population. Sort every array deterministically and serialize with the canonical-JSON helper.

Give `main(argv=None)` the flags `--out`, `--json`, and `--tool` for probing a single name. Exit non-zero when any verdict is `fail`; a `gated` verdict does not fail the run.

Write a `discovery`-scoped sorted `SHA256SUMS` covering exactly the files in the output directory, in `shasum -a 256` output format. Do not touch the staging root, the `tracer/` directory, or a `chain/` directory -- plan 03-02 runs concurrently in this wave and owns `chain/`, and plan 03-04 regenerates one bundle-wide checksum set at publication.

Run the secret guard over the payload before writing. Stage and commit only this script and the `discovery/` evidence directory, enumerated explicitly on the command line. Never use a whole-tree staging flag and never revert or clean anything -- the 2,663 uncommitted files from another session's regeneration sit under this commit and their disposition is plan 03-04's human checkpoint (D-06).

Format with `ruff format` using the repository `pyproject.toml` settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && .venv/bin/python scripts/smoke_discovery_sample.py --out .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery --json && jq -e '.failed == 0 and (.sample | length) >= 3 and ([.sample[] | .pool] | unique | length) >= 2 and ([.results[] | select(.name == "DegreesOfUnsaturation_calculate") | .verdict] | all(. == "pass")) and (.exclusions | length) >= 2' .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery/discovery.json && (cd .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery && shasum -a 256 -c SHA256SUMS)</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python scripts/smoke_discovery_sample.py --out <dir> --json` exits 0.
    - `jq -r '.failed' discovery.json` equals 0.
    - `jq -r '[.sample[] | .pool] | unique | sort | join(",")' discovery.json` contains both a preserved-custom pool and a new-upstream pool alongside the control -- the sample genuinely spans both provenances rather than sampling one side.
    - Every `sample[]` record carries non-empty `name`, `pool`, `selection_rule`, and `source_path`.
    - `jq -e '[.results[] | select(.name == "DegreesOfUnsaturation_calculate") | .verdict] | all(. == "pass")' discovery.json` is true -- the credential-free control passes outright, so a red sample cannot be blamed on the environment.
    - `jq -e '[.results[] | .verdict] | map(. == "pass" or . == "gated") | all' discovery.json` is true, and every `gated` verdict carries a non-empty `missing_keys` array.
    - `jq -e '[.gated[] | has("missing_keys")] | all' discovery.json` is true, and no name appears in both `sample` and `gated`.
    - `jq -e '[.results[] | select(.verdict == "pass") | .info.parameter_names | length > 0] | all' discovery.json` is true -- every pass returned an actual parameter schema, not an empty object.
    - `jq -r '.exclusions | join(" | ")' discovery.json` names the four deferred transport surfaces and Phase 5 / SURF-01, plus the credential-gated population.
    - `jq -r '.catalog_size' discovery.json` is greater than 1000 -- the probes ran against the real loaded catalog, not a filtered subset.
    - Searching the discovery evidence directory for credential-shaped strings (a value assigned to a key-named field, or an `sk-` prefixed token) returns no match.
    - `shasum -a 256 -c SHA256SUMS` from inside the `discovery/` directory exits 0, and `git diff --name-only -- .planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/tracer/` shows no change from this plan.
    - `git status --porcelain` path set still matches the tracer fingerprint's `paths` array.
    - `ruff format --check scripts/smoke_discovery_sample.py` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Read-only against the catalog plus one new script and one evidence directory; deleting both undoes it.</reversibility>

  <done>A deterministic sample spanning preserved-custom and new-upstream tools plus a credential-free control is found by `grep_tools` and returns real schemas from `get_tool_info`, gated tools are recorded separately with their missing keys, and the selection rule and deferred surfaces are written into the artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit coverage for the sample-selection and discovery-verdict logic</name>

  <precondition>The current `git status --porcelain` path set matches the `paths` array in the tracer's `worktree_fingerprint.json`.</precondition>

  <read_first>
    - `scripts/smoke_discovery_sample.py` -- `select_sample` and `classify_discovery` exactly as written in Task 1.
    - `tests/unit/test_probe_custom_tools.py` -- Phase 2's unit suite for the same class of pure verdict function, including its named empty-result regression guard and its hand-built stage fixtures.
    - `tests/unit/test_sync_baseline_git.py` lines 1-25 -- the `importlib.util.spec_from_file_location` loader block for a `scripts/` module.
    - `pytest.ini` -- the default `addopts` selection this test must run under with no marker.
  </read_first>

  <files>tests/unit/test_smoke_discovery_sample.py</files>

  <behavior>
    - Exact name found plus a parameter schema returned yields `pass`.
    - The gated error object returned by `get_tool_info` yields `gated` and carries the missing key names.
    - A name present in the gated mapping yields `gated` even when the grep stage found nothing.
    - The exact name not found, with no gating signal, yields `fail`.
    - A schema object with neither a `parameter` nor a `parameters` key, with no gating signal, yields `fail`.
    - An empty result with no gating signal yields `fail`, never `pass`.
    - `select_sample` removes every name present in the gated mapping from the returned sample and records it with its missing key names instead.
    - `select_sample` caps each non-control pool at five names, returns them in sorted order, and produces identical output for identical input across calls.
    - `select_sample` records an emptied pool explicitly rather than substituting another name when gating removes every candidate from a pool.
  </behavior>

  <action>
Create `tests/unit/test_smoke_discovery_sample.py`. Load the module under test with `importlib.util.spec_from_file_location("smoke_discovery_sample", Path(__file__).parents[2] / "scripts" / "smoke_discovery_sample.py")` and bind `select_sample` and `classify_discovery`.

Build all fixtures by hand: grep-result and info-result dicts constructed in the test, and a `tmp_path` mini repository with a small `data/` tree and a stub union artifact for `select_sample`. Do not import the package and do not instantiate `ToolUniverse` -- both functions are pure and must be provable without it, exactly as Phase 2 proved `assert_probe_contract`.

Write the `classify_discovery` cases from `<behavior>` as a `pytest.mark.parametrize` verdict table. Give the empty-result rejection its own explicitly named test rather than folding it into the table, matching the named regression guard Phase 2 established for the identical failure mode.

Give the gated-exclusion behavior its own named test as well: assert that a gated name never appears in the returned sample, that it does appear in the gated list with its missing key names, and that no substitution silently backfills the vacated slot. This is the single rule that keeps CAT-02 from failing for a credential reason instead of a catalog reason.

Write a determinism test asserting the pool cap, sorted ordering, and identical output across two calls on identical input.

Add no pytest marker; the file must run under `pytest.ini`'s default selection with no network, no credentials, and no heavy imports. Format with `ruff format` using the repository settings.
  </action>

  <verify>
    <automated>cd /Users/davis/code/ToolUniverse && .venv/bin/python -m pytest tests/unit/test_smoke_discovery_sample.py -q && .venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_smoke_discovery_sample.py -q` exits 0 with zero skipped tests.
    - `.venv/bin/python -m pytest tests/unit/test_smoke_discovery_sample.py --collect-only -q` lists a case for each of the six `classify_discovery` outcomes plus the gated-exclusion, pool-cap, and determinism tests.
    - A test node name references the empty-result rejection explicitly, and it passes.
    - A test node name references the gated exclusion explicitly, and it passes.
    <!-- planner-discipline-allow: tooluniverse -->
    - The test file contains no package import: filter comment lines out before counting, so header prose cannot satisfy or invalidate the count -- `grep -v '^[[:space:]]*#' tests/unit/test_smoke_discovery_sample.py | grep -c -E '^[[:space:]]*(import|from)[[:space:]]+tooluniverse'` returns 0.
    - `.venv/bin/python -m pytest tests/unit/test_registry_integrity.py -q` still exits 0.
    - `git status --porcelain` path set still matches the tracer fingerprint's `paths` array.
    - `ruff format --check tests/unit/test_smoke_discovery_sample.py` exits 0.
  </acceptance_criteria>

  <done>Every discovery verdict outcome, the gated-exclusion rule, and the sample's determinism have passing unit coverage that runs in the default pytest lane without importing the package.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
See `03-01-PLAN.md` section "Artifacts this phase produces" for the complete phase-wide list. Symbols and paths first created by **this** plan:

- `scripts/smoke_discovery_sample.py` -- `select_sample`, `probe_grep_tools`, `probe_get_tool_info`, `classify_discovery`, `run_discovery_suite`, `main`; CLI flags `--out`, `--json`, `--tool`.
- `tests/unit/test_smoke_discovery_sample.py`
- `.planning/phases/03-follow-up-and-catalog-reconciliation/evidence/staging/discovery/{discovery.json,SHA256SUMS}`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution environment credentials -> discovery results | Which tools are discoverable depends on ambient API keys, so a verdict can encode the environment rather than the catalog |
| tool definitions -> dynamic module resolution during `load_tools()` | Loading the full catalog resolves and instantiates tool classes named by JSON `type` strings |
| sampled tools -> external providers | A non-gated tool's schema inspection is local, but full catalog loading touches provider configuration |
| discovery results -> committed evidence bundle | Tool metadata and environment state are written into a committed artifact |
| shared working tree -> git index | 2,663 uncommitted files from another session sit under every commit this plan makes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Tampering | credential gating misread as catalog damage | high | mitigate | The sample removes every name present in `_excluded_api_key_tools` and records it separately with its missing key names; `classify_discovery` returns `gated`, never `fail`, for the gated error object. A dedicated named unit test proves the exclusion and forbids silent backfill. |
| T-03-16 | Tampering | empty discovery result masquerading as success | high | mitigate | `classify_discovery` returns `fail`, never `pass`, for an empty result with no gating signal, with its own named regression test -- the same guard Phase 2 plan 02-05 added after this repository's recorded empty-as-success incident. |
| T-03-17 | Information Disclosure | discovery evidence JSON | high | mitigate | The secret guard from `scripts/capture_sync_baseline.py` runs before writing; only parameter key names, tool names, and verdicts are recorded -- never key values and never a full response payload. An acceptance criterion searches the directory for credential-shaped strings. |
| T-03-18 | Tampering | git index vs. the pre-existing dirty working tree | critical | mitigate | Every task carries a precondition and an acceptance criterion asserting the dirty path set matches plan 03-01's fingerprint; staging is by explicit path enumeration only. |
| T-03-19 | Spoofing | a hand-picked flattering sample | medium | mitigate | `select_sample` derives both pools mechanically from `git diff-tree --cc` and Phase 2's `union.json`, caps and sorts deterministically, and writes the selection rule and every exclusion into the artifact so the coverage boundary is reviewable rather than implied. |
| T-03-20 | Elevation of Privilege | full `load_tools()` resolving tool classes | medium | mitigate | Loading is the behavior under test and is required for a meaningful `grep_tools` result; no tool is executed, only located and inspected. `close` runs in a `finally` block. |
| T-03-21 | Denial of Service | provider calls during catalog load | low | accept | No tool is executed; schema inspection is local. Live-provider validation is explicitly out of scope per REQUIREMENTS.md. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero packages. No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
1. The sample spans preserved-custom and new-upstream pools plus a credential-free control, selected mechanically from Phase 2's recorded artifacts.
2. Every sampled name is found by `grep_tools` and returns a non-empty parameter schema from `get_tool_info`, or is recorded as gated with its missing keys.
3. The credential-free control passes outright.
4. No verdict is `fail`; no empty result is recorded as a pass.
5. Deferred transport surfaces and the gated population are written into the artifact as explicit exclusions.
6. `shasum -a 256 -c SHA256SUMS` verifies inside `discovery/`, and the pre-existing dirty path set is unchanged.
</verification>

<success_criteria>
- CAT-02's discoverability criterion has real evidence from the actual discovery primitives against the real loaded catalog.
- A credential-gated tool is visibly gated, never silently absent and never a false catalog failure.
- The coverage boundary is explicit: which provenances were sampled, which tools were gated out, and which surfaces are Phase 5's.
</success_criteria>

<output>
Create `.planning/phases/03-follow-up-and-catalog-reconciliation/03-03-SUMMARY.md` when done.
</output>
