{
  "_note": "Toggleable reinforcement clauses for the Ollama worker system prompt. The optimizer hill-climbs over which to include (added to the core ollama_worker.system.md) to maximize eval score. Doc-derived clauses (from ollama_qwen_prompting_notes.md) get appended here.",
  "clauses": [
    {
      "id": "act",
      "text": "Always ACT by calling a tool (write_file / read_file / run_shell). Never write code or commands in prose or markdown fences and assume they ran — an unexecuted tool call does nothing.",
      "targets": [
        "mechanical",
        "no_fabricate",
        "fix_real_bug",
        "scope_no_overedit",
        "multifile_handler",
        "recovery_from_error"
      ]
    },
    {
      "id": "finish",
      "text": "When the task is complete, call the finish tool with the status string. Do not end with a plain-text answer.",
      "targets": ["mechanical", "no_fabricate"]
    },
    {
      "id": "skip",
      "text": "If the root cause is a missing API key/credential, an unreachable external service, or an unavailable/uninstalled dependency, make ZERO file edits, create no files, and finish with 'SKIP <reason>'. A missing key or package is never a code bug — never stub or fabricate it to force a pass.",
      "targets": ["skip_unfixable", "ambiguous_skip"]
    },
    {
      "id": "scope",
      "text": "Edit ONLY the file named in the task. Never create, modify, or delete any other file.",
      "targets": ["scope_no_overedit"]
    },
    {
      "id": "verify",
      "text": "After editing, re-run the exact failing command with run_shell and confirm it passes before calling finish.",
      "targets": ["fix_real_bug", "multifile_handler", "recovery_from_error"]
    },
    {
      "id": "observed",
      "text": "Report only values you actually saw in a run_shell result. Never predict or invent command output.",
      "targets": ["no_fabricate", "recovery_from_error"]
    },
    {
      "id": "onestep",
      "text": "One tool call per step; wait for its result before the next action.",
      "targets": ["mechanical", "fix_real_bug"]
    },
    {
      "id": "readrefs",
      "text": "Before editing, read every file the task or a failing traceback references, so a fix that spans multiple files targets the real mismatch. After a failed fix, re-read the new error and the test's assertion before trying again.",
      "targets": ["multifile_handler", "recovery_from_error"]
    }
  ]
}
