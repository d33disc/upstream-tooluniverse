[
  {
    "name": "iCanHazDadJoke_get_random_joke",
    "description": "Get a random dad joke from the icanhazdadjoke.com API. Returns a setup-punchline style joke with a unique ID. Free, no authentication required. Database of thousands of dad jokes. Useful for entertainment, conversation starters, or adding humor to applications.",
    "parameter": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "fields": {
      "endpoint": "https://icanhazdadjoke.com/",
      "params": {},
      "headers": {"Accept": "application/json"}
    },
    "type": "BaseRESTTool",
    "test_examples": [
      {},
      {},
      {}
    ],
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "description": "A random dad joke",
              "properties": {
                "id": {"type": "string", "description": "Unique joke identifier"},
                "joke": {"type": "string", "description": "The dad joke text"},
                "status": {"type": "integer", "description": "HTTP status code (200)"}
              }
            },
            "metadata": {"type": "object"}
          },
          "required": ["data"]
        },
        {
          "type": "object",
          "properties": {"error": {"type": "string"}},
          "required": ["error"]
        }
      ]
    }
  },
  {
    "name": "iCanHazDadJoke_search_jokes",
    "description": "Search for dad jokes by keyword using the icanhazdadjoke.com API. Returns matching jokes with IDs and pagination info. Free, no authentication required. Useful for finding jokes on specific topics (animals, food, science, etc.).",
    "parameter": {
      "type": "object",
      "properties": {
        "term": {
          "type": "string",
          "description": "Search term to find jokes. Examples: 'cat', 'dog', 'science', 'pizza', 'programming', 'music'"
        },
        "limit": {
          "type": ["integer", "null"],
          "description": "Number of results per page (1-30). Default: 20"
        },
        "page": {
          "type": ["integer", "null"],
          "description": "Page number for pagination. Default: 1"
        }
      },
      "required": ["term"]
    },
    "fields": {
      "endpoint": "https://icanhazdadjoke.com/search",
      "params": {},
      "headers": {"Accept": "application/json"}
    },
    "type": "BaseRESTTool",
    "test_examples": [
      {"term": "cat"},
      {"term": "programming"},
      {"term": "music", "limit": 5}
    ],
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "description": "Search results with matching dad jokes",
              "properties": {
                "current_page": {"type": "integer"},
                "limit": {"type": "integer"},
                "next_page": {"type": "integer"},
                "previous_page": {"type": "integer"},
                "results": {
                  "type": "array",
                  "description": "List of matching jokes",
                  "items": {
                    "type": "object",
                    "properties": {
                      "id": {"type": "string"},
                      "joke": {"type": "string"}
                    }
                  }
                },
                "search_term": {"type": "string"},
                "status": {"type": "integer"},
                "total_jokes": {"type": "integer"},
                "total_pages": {"type": "integer"}
              }
            },
            "metadata": {"type": "object"}
          },
          "required": ["data"]
        },
        {
          "type": "object",
          "properties": {"error": {"type": "string"}},
          "required": ["error"]
        }
      ]
    }
  }
]
