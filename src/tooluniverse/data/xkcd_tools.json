[
  {
    "name": "XKCD_get_latest_comic",
    "description": "Get the latest XKCD comic metadata including title, image URL, alt text, and transcript. XKCD is a popular webcomic of romance, sarcasm, math, and language by Randall Munroe.",
    "parameter": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "fields": {
      "endpoint": "https://xkcd.com/info.0.json"
    },
    "type": "BaseRESTTool",
    "test_examples": [
      {}
    ],
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "description": "XKCD comic metadata",
              "properties": {
                "num": {"type": "integer", "description": "Comic number"},
                "title": {"type": "string", "description": "Comic title"},
                "safe_title": {"type": "string", "description": "URL-safe comic title"},
                "alt": {"type": "string", "description": "Alt/hover text for the comic"},
                "img": {"type": "string", "description": "URL to the comic image"},
                "transcript": {"type": "string", "description": "Comic transcript text"},
                "year": {"type": "string", "description": "Publication year"},
                "month": {"type": "string", "description": "Publication month"},
                "day": {"type": "string", "description": "Publication day"},
                "link": {"type": "string", "description": "External link if any"},
                "news": {"type": "string", "description": "News text if any"}
              }
            },
            "metadata": {"type": "object"}
          },
          "required": ["data"]
        },
        {
          "type": "object",
          "properties": {
            "error": {"type": "string"}
          },
          "required": ["error"]
        }
      ]
    }
  },
  {
    "name": "XKCD_get_comic_by_number",
    "description": "Get a specific XKCD comic by its number. Returns metadata including title, image URL, alt text, and transcript. Use this to look up specific XKCD comics (e.g., #327 'Bobby Tables', #1000, #2000).",
    "parameter": {
      "type": "object",
      "properties": {
        "comic_number": {
          "type": "integer",
          "description": "The XKCD comic number to retrieve"
        }
      },
      "required": ["comic_number"]
    },
    "fields": {
      "endpoint": "https://xkcd.com/{comic_number}/info.0.json"
    },
    "type": "BaseRESTTool",
    "test_examples": [
      {"comic_number": 327},
      {"comic_number": 1000},
      {"comic_number": 2000}
    ],
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "description": "XKCD comic metadata",
              "properties": {
                "num": {"type": "integer", "description": "Comic number"},
                "title": {"type": "string", "description": "Comic title"},
                "safe_title": {"type": "string", "description": "URL-safe comic title"},
                "alt": {"type": "string", "description": "Alt/hover text for the comic"},
                "img": {"type": "string", "description": "URL to the comic image"},
                "transcript": {"type": "string", "description": "Comic transcript text"},
                "year": {"type": "string", "description": "Publication year"},
                "month": {"type": "string", "description": "Publication month"},
                "day": {"type": "string", "description": "Publication day"}
              }
            },
            "metadata": {"type": "object"}
          },
          "required": ["data"]
        },
        {
          "type": "object",
          "properties": {
            "error": {"type": "string"}
          },
          "required": ["error"]
        }
      ]
    }
  }
]
