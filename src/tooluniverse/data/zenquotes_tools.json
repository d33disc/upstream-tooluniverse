[
  {
    "name": "ZenQuotes_get_random",
    "description": "Get a random inspirational quote from ZenQuotes.io. Returns a quote with its author. No API key required. Useful for motivational apps, daily inspiration features, random quote generators, and content creation. The API returns a new random quote on each call.",
    "parameter": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "fields": {
      "endpoint": "https://zenquotes.io/api/random",
      "params": {}
    },
    "type": "BaseRESTTool",
    "test_examples": [
      {},
      {},
      {}
    ],
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "data": {
              "type": "array",
              "description": "Array containing one random quote object",
              "items": {
                "type": "object",
                "properties": {
                  "q": {"type": "string", "description": "The quote text"},
                  "a": {"type": "string", "description": "The author name"},
                  "h": {"type": "string", "description": "HTML-formatted blockquote version of the quote"}
                }
              }
            },
            "metadata": {"type": "object"}
          },
          "required": ["data"]
        },
        {
          "type": "object",
          "properties": {"error": {"type": "string"}},
          "required": ["error"]
        }
      ]
    }
  },
  {
    "name": "ZenQuotes_get_today",
    "description": "Get today's inspirational quote from ZenQuotes.io. Returns the same quote for all users on a given day, changing daily. No API key required. Useful for 'quote of the day' features, daily digest emails, and consistent daily inspiration displays.",
    "parameter": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "fields": {
      "endpoint": "https://zenquotes.io/api/today",
      "params": {}
    },
    "type": "BaseRESTTool",
    "test_examples": [
      {},
      {}
    ],
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "data": {
              "type": "array",
              "description": "Array containing today's quote object",
              "items": {
                "type": "object",
                "properties": {
                  "q": {"type": "string", "description": "The quote text"},
                  "a": {"type": "string", "description": "The author name"},
                  "h": {"type": "string", "description": "HTML-formatted blockquote version"}
                }
              }
            },
            "metadata": {"type": "object"}
          },
          "required": ["data"]
        },
        {
          "type": "object",
          "properties": {"error": {"type": "string"}},
          "required": ["error"]
        }
      ]
    }
  },
  {
    "name": "ZenQuotes_get_quotes",
    "description": "Get a batch of 50 inspirational quotes from ZenQuotes.io. Returns multiple quotes with authors for bulk processing. No API key required. Useful for populating quote databases, building quote collections, batch analysis of quotes, or seeding content with inspirational messages.",
    "parameter": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "fields": {
      "endpoint": "https://zenquotes.io/api/quotes",
      "params": {}
    },
    "type": "BaseRESTTool",
    "test_examples": [
      {},
      {}
    ],
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "data": {
              "type": "array",
              "description": "Array of 50 quote objects",
              "items": {
                "type": "object",
                "properties": {
                  "q": {"type": "string", "description": "The quote text"},
                  "a": {"type": "string", "description": "The author name"},
                  "c": {"type": ["string", "null"], "description": "Character count of the quote"},
                  "h": {"type": "string", "description": "HTML-formatted blockquote version"}
                }
              }
            },
            "metadata": {"type": "object"}
          },
          "required": ["data"]
        },
        {
          "type": "object",
          "properties": {"error": {"type": "string"}},
          "required": ["error"]
        }
      ]
    }
  }
]
