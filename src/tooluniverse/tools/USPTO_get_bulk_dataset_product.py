# AUTO-GENERATED by `tu build` — do not edit manually
"""USPTO_get_bulk_dataset_product

Get a bulk data product from the USPTO BDSS catalog by its identifier (shortName). Returns product metadata including available files and date ranges.
"""

from typing import Any, Callable, Optional

from ._shared_client import get_shared_client


def USPTO_get_bulk_dataset_product(
    productIdentifier: str,
    *,
    stream_callback: Optional[Callable[[str], None]] = None,
    use_cache: bool = False,
    validate: bool = True,
) -> dict[str, Any]:
    """
    Get a bulk data product from the USPTO BDSS catalog by its identifier (shortName). Returns product metadata including available files and date ranges.

    Parameters
    ----------
    productIdentifier : str
        Product identifier (shortName), e.g. 'PTGRXML' for patent grants XML.

    stream_callback : Callable, optional
        Callback for streaming output
    use_cache : bool, default False
        Enable caching
    validate : bool, default True
        Validate parameters

    Returns
    -------
    dict[str, Any]
    """
    return get_shared_client().run_one_function(
        {
            "name": "USPTO_get_bulk_dataset_product",
            "arguments": {"productIdentifier": productIdentifier},
        },
        stream_callback=stream_callback,
        use_cache=use_cache,
        validate=validate,
    )


__all__ = ["USPTO_get_bulk_dataset_product"]
