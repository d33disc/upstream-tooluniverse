# AUTO-GENERATED by `tu build` — do not edit manually
"""USPTO_get_petition_decision

Get a USPTO petition decision record by its identifier.
"""

from typing import Any, Callable, Optional

from ._shared_client import get_shared_client


def USPTO_get_petition_decision(
    petitionDecisionRecordIdentifier: str,
    *,
    stream_callback: Optional[Callable[[str], None]] = None,
    use_cache: bool = False,
    validate: bool = True,
) -> dict[str, Any]:
    """
    Get a USPTO petition decision record by its identifier.

    Parameters
    ----------
    petitionDecisionRecordIdentifier : str
        Petition decision record identifier.

    stream_callback : Callable, optional
        Callback for streaming output
    use_cache : bool, default False
        Enable caching
    validate : bool, default True
        Validate parameters

    Returns
    -------
    dict[str, Any]
    """
    return get_shared_client().run_one_function(
        {
            "name": "USPTO_get_petition_decision",
            "arguments": {"petitionDecisionRecordIdentifier": petitionDecisionRecordIdentifier},
        },
        stream_callback=stream_callback,
        use_cache=use_cache,
        validate=validate,
    )


__all__ = ["USPTO_get_petition_decision"]
