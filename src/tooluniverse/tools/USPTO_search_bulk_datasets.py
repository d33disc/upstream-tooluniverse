# AUTO-GENERATED by `tu build` — do not edit manually
"""USPTO_search_bulk_datasets

Search the USPTO Bulk Data Storage System (BDSS) product catalog. Returns available bulk dataset products (e.g. patent grants, trademark assignments) with identifiers and descriptions. Query with q (e.g. 'patent') or browse unfiltered.
"""

from typing import Any, Callable, Optional

from ._shared_client import get_shared_client


def USPTO_search_bulk_datasets(
    q: Optional[str] = None,
    *,
    stream_callback: Optional[Callable[[str], None]] = None,
    use_cache: bool = False,
    validate: bool = True,
) -> dict[str, Any]:
    """
    Search the USPTO Bulk Data Storage System (BDSS) product catalog. Returns available bulk dataset products (e.g. patent grants, trademark assignments) with identifiers and descriptions. Query with q (e.g. 'patent') or browse unfiltered.

    Parameters
    ----------
    q : str
        Search query, e.g. 'patent'.

    stream_callback : Callable, optional
        Callback for streaming output
    use_cache : bool, default False
        Enable caching
    validate : bool, default True
        Validate parameters

    Returns
    -------
    dict[str, Any]
    """
    return get_shared_client().run_one_function(
        {
            "name": "USPTO_search_bulk_datasets",
            "arguments": {"q": q},
        },
        stream_callback=stream_callback,
        use_cache=use_cache,
        validate=validate,
    )


__all__ = ["USPTO_search_bulk_datasets"]
